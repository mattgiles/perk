# BEGIN skills managed runtime artifacts
/.agents/state.yaml
/.agents/local.yaml
/.agents/skills/
/.claude/skills/
/.agents/cache/
# END skills managed runtime artifacts

# dev / build artifacts
.venv/
__pycache__/
*.egg-info/
dist/
build/
node_modules/
*.tgz
# perk workflow cache — transient tier-2 subtrees are never committed (contracts.md §8.1).
# (T4's `perk init` will move these into the managed block below.)
.pi/workflow/.perk-loaded
.pi/workflow/.perk-t3.json
.pi/workflow/handoff/
.pi/workflow/scratch/
.pi/workflow/markers/

# BEGIN perk managed
/.pi/npm/
/.pi/git/
# END perk managed
