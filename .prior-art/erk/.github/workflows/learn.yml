# Learn Dispatch Workflow
#
# Runs /erk:learn on a completed plan to extract insights and patterns
# from the implementation session. This workflow is triggered asynchronously
# after a PR lands via the upload-impl-session pipeline.
#
# Output: Creates a new learn plan issue containing extracted lessons learned,
# architectural decisions, and reusable patterns from the implementation.

name: learn
run-name: "${{ inputs.pr_number }}:${{ inputs.distinct_id }}"

on:
  workflow_dispatch:
    inputs:
      pr_number:
        description: "PR number to learn from"
        required: true
        type: string
      distinct_id:
        description: "Unique identifier for run discovery (base36)"
        required: true
        type: string
      learn_branch:
        description: "Branch containing learn materials in .erk/impl-context/"
        required: true
        type: string
      pr_backend:
        description: "PR backend type (github or planned_pr)"
        required: false
        type: string
        default: "github"

concurrency:
  group: learn-plan-${{ github.event.inputs.pr_number }}
  cancel-in-progress: true

jobs:
  learn:
    if: vars.CLAUDE_ENABLED != 'false'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      issues: write
    env:
      ERK_PR_BACKEND: ${{ inputs.pr_backend }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.learn_branch }}
          token: ${{ secrets.ERK_QUEUE_GH_PAT }}
          fetch-depth: 0

      - uses: ./.github/actions/erk-remote-setup
        with:
          erk-pat: ${{ secrets.ERK_QUEUE_GH_PAT }}
          anthropic-api-key: ${{ secrets.ANTHROPIC_API_KEY }}
          claude-code-oauth-token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}

      - name: Run learn workflow
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GH_TOKEN: ${{ secrets.ERK_QUEUE_GH_PAT }}
          PR_NUMBER: ${{ inputs.pr_number }}
          WORKFLOW_RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          claude --print \
            --verbose \
            --model claude-sonnet-4-6 \
            --output-format stream-json \
            --dangerously-skip-permissions \
            "/erk:learn $PR_NUMBER"

      - name: Cleanup learn branch
        if: always()
        run: git push origin --delete ${{ inputs.learn_branch }} || true
