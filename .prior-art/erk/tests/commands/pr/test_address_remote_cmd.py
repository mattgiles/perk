"""Tests for erk launch pr-address command.

Note: The old `erk pr address remote` command has been replaced by
`erk launch pr-address`. See tests/commands/launch/test_launch_cmd.py
for the new workflow tests.
"""

# This file is kept for reference. The tests have been migrated to:
# - tests/commands/launch/test_launch_cmd.py
#
# The `erk pr address remote` subcommand has been removed.
# Use `erk launch pr-address` instead.
