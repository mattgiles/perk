// The host package's UNBUNDLED entry (materialized at dist/index.js): what an exact-entry import
// reaches when the bridge does not redirect it.
export const origin = "unbundled-copy";
export class Marker {}
