// A lazily imported module under the consumer root that itself imports a census specifier.
import * as piTui from "@earendil-works/pi-tui";

export const origin = piTui.origin;
export const Marker = piTui.Marker;
