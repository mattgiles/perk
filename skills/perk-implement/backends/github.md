# Reading the plan — GitHub issue backend

The saved plan is a GitHub issue; the plan body is posted as the **first comment**. Read it with:

```
gh issue view <n> --comments
```

`<n>` is the plan-issue number from the launch prompt (`You are implementing perk plan github #<n>`).
