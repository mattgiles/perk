"""perk-dev's session-audit family (dev-only maintainer tooling; never published).

Home of the behavioral-expectation catalog and the machinery that checks it against
recorded pi/perk session history. No re-exports: consumers import
``perk_dev.audit.expectations`` (and future sibling modules) directly.
"""
