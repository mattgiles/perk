<!--
  This file is appended to every perk session's system prompt (Pi's project-scoped
  .pi/APPEND_SYSTEM.md). It holds the COMPRESSED, ambient routing index into docs/learned/ —
  the realization of the "compressed index must be ambient" finding (a retrieval-tier index is
  too brittle to rely on). Keep it SMALL: one terse routing line per durable doc/category,
  pointing into the full catalog at docs/learned/index.md (read on demand).

  Maintained by /learn-docs consolidation plans (the perk-learn-docs skill) — NEVER by `perk init`.
-->

## Durable learnings (docs/learned)

Cross-cutting reasoning captured for future agents lives in `docs/learned/`. The full catalog is
`docs/learned/index.md`; read a specific doc when its routing cue matches your task.

<!-- routing index — one terse line per doc/category; empty until the first /learn-docs plan lands -->

- **workflow/** — plan factories, plan-ref lifecycle, plan-save surfaces (fidelity gap & handoff_extra carrier), warm-door commands (read-only gating trap, drive-the-session, rendering every cold-door outcome, terminating-door drives next pass — `/land` auto-drives `/objective-reconcile`), objective lifecycle (resumable-lease nodes & authoring loop & the deterministic `perk objective run` supervisor — never-lands/never-plans, PR-existence implement-done signal, wait-then-refetch-whole-world, report-only budget; pending-first selection over live claims & `resumable_claims` coordination surface & accepted backlink race & JSON-mode≠JSON-payload & parallel-node rebase friction), config tables (own-section vs wholesale-consumed-map placement, string-only TS parser vs bool-is-int Python guard, committed-only vs overlaid read, interior-gate vs init→settings.json convergence, mirror the providers selection shape), cold-door pi-launch seam (build argv once before dry-run, --approve run-scoped trust on ephemeral worktrees, last-wins inject-before-passthrough, local stage execvpe never returns, capture nested machine_output in a --json surface, Click 8.4.1 CliRunner mix_stderr dropped → parse last line), worktree filesystem lifecycle (.resolve()-both-sides candidate match, uncertainty⇒skip, split --force, .pi/-dirty test gotcha), skill bindings (two-plane delivery & doctor validation/injection mirror), shared `shared/` contracts recipe (incl. `contracts.md` §-numbering not contiguous — grep `## §8.` before assigning), provider seam (owned-surface deferral vs always-registered substrate, registration-time vacating vs runtime deferral two-node split, injection-only adapter shim, both seams now have real foreign adapters, produced-contract tier sets bridge weight, re-derive a sibling's forward-note don't mirror, `package_filter` omission, cross-plane resolver mirror, `cache.plan-ref.provider`== issue-backend `github` not seam id, augment-posture provider — three-tier registerPlanMode & persisted-mode gate read & present-for-review save split), CLI command groups (§8.1 group-dir template, hybrid default-dispatch stage/group coexistence, SectionedGroup help taxonomy & generated-vs-dedicated launcher long-help split & Click two-paragraph short/long help, byte-identical-JSON fold discipline & re-grep-all-N-helpers, parity smoke & rendered-row help-slice tests, fold gotchas — quoted two-token bash case keys, git mv blame), cold-door client (`runColdDoor` envelope client — rollout COMPLETE, substrate mandatory for new doors; strict-iff-appended-to-workflow-state vs advisory-dropped vs fully-lenient decode criterion, fail-arm payload re-narrowing, fixture-sweep rule when strictening a decode, exported narrowing helpers, stdin-flag-at-argv-end, legacy-label byte-compat lever, non-string envelope fields fall back generic), github gateway (`perk/github.py` parse-helper family, consolidation boundary rules — byte-identical errors via per-site what/source & leave single-site wrappers alone, helper-prefix+caller-tail argv pattern, substring not-found fold residual — phantom-`None` lookups), borrowed packages (lockstep surfaces recipe — BORROWED_PACKAGES/settings/capability-summary/contracts/tests, foreign tool names inert when absent, repo-non-mutation read-only bar, borrow vs provider-seam criterion), remote-runner (declarative-correct/execution-untested CI seam, Runner contract & two run-ids, establish-before-consume dispatch, fresh-runner git identity & PAT auth, four-candidate worker-entry ladder, honest-fiction loud deferral, open follow-up — defaultCreateRuntime in-memory settings never load disk packages so a real remote launch may register zero extension tools), init/doctor division & managed-convergence SSOT & doctor-disk-vs-selfcheck-prompt & the `_GROUP_ORDER` human-render trap, init shelling out to external CLIs, templates reconvergence, Click bottom-of-file imports & register_with_aliases arity, report-only pre-flights, supervisor cancel/retry control seam, rerun reuse, GHA universal zero-spend smoke short-circuit, fail-soft overlays vs local records truth, subprocess monkeypatching traps, contracts §-numbering grep, post-merge objective roadmap reconciliation; read when working on perk internals, factories, planning/objective/binding/provider subsystems, doctor checks, contracts, or gitignore mechanics → `docs/learned/workflow/`
- **pi/** — Pi extension API (getSystemPromptOptions/ctx.mode/injected-message persistence, registerTool execute results details requirement, planning tools read-only gating traps, tsconfig strict-mode index access in tests, offline `pi.sendUserMessage` injection spy via instance-property override, headfulUIContext has no select/input), tool-param decode (`toolParams.ts` tri-state boundary seam — absent vs mistyped, strict-fail bad_input vs native-shape refusals, decode-before-side-effect ordering proofs, exported pure decode for UI tools, never reuse cold-door lenient helpers), context injection (conditional inject-and-strip, stage-field disambiguation), context system (no transclusion, ambient index split, read-only bash allowlist), structured output (pi-ai tool-calling not JSON mode, faux-provider offline tests, `PERK_NO_LLM` gate), headless session construction & driving (runtime-factory builds the loader internally, explicit `bindExtensions` `mode:"json"`, `session.subscribe` raw-AgentEvent facts, single-prompt drive + budget watchdog & premature-idle gap, structured run-event stream — additive `RunEvent` union, two fail-soft tiers, route-don't-relay, offline model-availability determinism), subagents (agentOverrides reach builtin-only & per-call inline model override, child-posts-own-mutation vs read-only-child-parent-mutates, context fresh/fork, review 422→comment fallback, agent-def consumer-delivery gap, flat agent/seam-keyed config, TS TOML backslash continuation restriction, cross-plane model/prompt parity, exported guidance offline unit-testing), extension consolidation seams (minimal structural interface — ReportTarget/BranchSource/EntrySink, pure-module-plus-effectful-seam reconciliation, `appendWorkflowState` strict read-back & verified-linkage tiering & reference-identity object compare & aspirational-comment migration signal, opt-in flag over API fork, report() owns-the-prefix de-prefixing trap, P1/P2/P3 site triage), `pi.events` unreachable from harness — pure-over-a-fake-bus split & env-var over module-constant injection & getFlags/getShortcuts non-registration asserts, faux-model real-runtime drive (nested pi-ai per-instance registry — resolve through pi-coding-agent, inMemory settings don't layer disk → extensionFactories, seed faux-provider auth, eventSink vs file-sink exclusivity) → `docs/learned/pi/`
- **toolchain/** — ruff check vs format (silent-commit trap, `RUF100` fires on a `# noqa` for a non-enabled rule, template string E501 multiline limits, SIM105 exception suppressions, preview-rule enablement drags cross-bucket fixes forward — ruff bumps too), Biome/tsc gotchas, Biome param-property type-stripping under `node --test` / `organizeImports` assist-only-under-`check` / `let x = undefined` trap / `Omit<Union,K>` collapses a discriminated union — use a distributive Omit / forEach expression-body assertion needs a block body / new files: `biome check --write` before CI, worktree node_modules stale-SDK trap & package-lock churn cleanup & stale-global-`perk`/self-converge smoke trap (use worktree `.venv/bin/perk` for `shared/` changes; init smokes in a scratch dir; fresh worktree = empty node_modules → `npm ci` before tsc/node --test), ty narrowing of untyped/JSON dict values (cast vs truthiness guard) → `docs/learned/toolchain/`
