<!--
  This file is appended to every perk session's system prompt (Pi's project-scoped
  .pi/APPEND_SYSTEM.md). It holds the COMPRESSED, ambient routing index into docs/learned/ —
  the realization of the "compressed index must be ambient" finding (a retrieval-tier index is
  too brittle to rely on). Keep it SMALL: one terse routing line per durable doc/category,
  pointing into the full catalog at docs/learned/index.md (read on demand).

  Maintained by /learn-docs consolidation plans (the perk-learn-docs skill) — NEVER by `perk init`.
-->

## Durable learnings (docs/learned)

Cross-cutting reasoning captured for future agents lives in `docs/learned/`. The full catalog is
`docs/learned/index.md`; read a specific doc when its routing cue matches your task.

<!-- routing index — one terse line per doc/category; empty until the first /learn-docs plan lands -->

- **workflow/** — plan factories (plane-specific link carriers, inbox-over-gh now a discipline), plan-ref lifecycle, plan-save surfaces (fidelity gap & handoff_extra carrier, `resolvePlanSource` artifact→param→transcript chain — param-path message byte-stable & ignored-differing-param flag & the stale-draft hazard, the warm `objective_node_claim` recovery carrier — mirror new factory-threaded params on both planes), plan review flow (backend-neutral `plan_review` dispatch — plannotator bridge vs first-party `ctx.ui.editor` default, asymmetric review tiering — artifact→param ONLY never the transcript, `approvalSave` owns the gate exit — review backends compose never own, reviewOutcomeResult renders DENIED unless approved-first routing & SKIP_TEXT/guard-ordering footguns, approved-first split mirrored to objectives & optional-param flavoring of the shared first-party core & the three-tier prose mirror, extracted-core + scripted-UI-fake testing), warm-door commands (read-only gating trap, drive-the-session, the warm gate-enter recipe — enter distributed/exit centralized, rendering every cold-door outcome, terminating-door drives next pass — `/land` auto-drives `/objective-reconcile`), objective lifecycle (resumable-lease nodes & authoring loop — JSON draft artifact & render-at-the-door & draft-module-leaf & the deterministic `perk objective run` supervisor — never-lands/never-plans, PR-existence implement-done signal, wait-then-refetch-whole-world, report-only budget; pending-first selection over live claims & `resumable_claims` coordination surface & accepted backlink race — `planning`+`pr:null` after merge is its known signature & JSON-mode≠JSON-payload & parallel-node rebase friction), config tables (own-section vs wholesale-consumed-map placement, string-only TS parser vs bool-is-int Python guard, committed-only vs overlaid read — the three-precedent committed-only recipe & the overlay-ignored test, interior-gate vs init→settings.json convergence, mirror the providers selection shape), cold-door pi-launch seam (build argv once before dry-run, --approve run-scoped trust on ephemeral worktrees, last-wins inject-before-passthrough, local stage execvpe never returns, capture nested machine_output in a --json surface, Click 8.4.1 CliRunner mix_stderr dropped → parse last line & Click ≥8.2 `.output` is a combined stream — compare `.stdout`/`.stderr` for byte-identity asserts, launch/run refactor posture — cache.py import-leaf & promote-in-place, dataclasses.replace transitions, byte-exact pins as the proof, merge-order env setdefault layering), worktree filesystem lifecycle (.resolve()-both-sides candidate match, uncertainty⇒skip, split --force, .pi/-dirty test gotcha), skill bindings (two-plane delivery & doctor validation/injection mirror & `references:` subdirectory routing with zero init/doctor wiring), shared `shared/` contracts recipe (incl. `contracts.md` §-numbering not contiguous — grep `## §8.` before assigning; cross-plane SSOT prompt-fragment helpers + lockstep parity substrings; registry vocabulary keys land with their first declaring stage; registry-writes changes trip two pytest pins; both-planes wording pinning), provider seam (owned-surface deferral vs always-registered substrate, registration-time vacating vs runtime deferral two-node split, injection-only adapter shim, both seams now have real foreign adapters, produced-contract tier sets bridge weight, re-derive a sibling's forward-note don't mirror, `package_filter` omission, cross-plane resolver mirror, `cache.plan-ref.provider`== issue-backend `github` not seam id, augment-posture provider — three-tier registerPlanMode & persisted-mode gate read & present-for-review save split, tombell state-twin read & tools-hidden prompt branch), CLI command groups (§8.1 group-dir template, hybrid default-dispatch stage/group coexistence, SectionedGroup help taxonomy & generated-vs-dedicated launcher long-help split & Click two-paragraph short/long help, byte-identical-JSON fold discipline & re-grep-all-N-helpers, parity smoke & rendered-row help-slice tests, fold gotchas — quoted two-token bash case keys, git mv blame), cold-door client (`runColdDoor` envelope client — rollout COMPLETE, substrate mandatory for new doors; strict-iff-appended-to-workflow-state vs advisory-dropped vs fully-lenient decode criterion, fail-arm payload re-narrowing, fixture-sweep rule when strictening a decode & the merge-race fixture sweep — grep ALL fixtures repo-wide after any cross-plane shape change, derive-don't-decode strongest tier — render-only-and-redundant fields derived from the strict field & the bad_output-reachability acceptance test, cross-plane skew structural for the dev flow — the substring-preserving skew-naming reword, exported narrowing helpers, stdin-flag-at-argv-end, legacy-label byte-compat lever, non-string envelope fields fall back generic), github gateway (`perk/github.py` parse-helper family, consolidation boundary rules — byte-identical errors via per-site what/source & leave single-site wrappers alone, helper-prefix+caller-tail argv pattern, substring not-found fold residual — phantom-`None` lookups, mutation-posting policies — verdict-driven /pr-review split & 👍-reaction minimal artifact & asymmetric failure ladders & structurally-unpostable fields), issue-backend seam (protocol module shape & annotated-binding conformance, late-bound GitHub adapter over the monkeypatched substrate, 21-function consumer boundary + substring import-direction guards, two-shape fixture rule, numeric-id grep tag, not-found message contract, `backend_id` stamp discipline & required-kwarg caller census & doctor arm-mapping over a collapsed error & opaque backend-owned header ids, opaque-id validator relaxation — the backend is the authority on junk & sweep ALL id consumers (isdigit/`\d+`/number-typed params), per-backend land closure — branch on `backend_id` never id shape), linear backend (plain-`Authorization` auth & RATELIMITED-as-HTTP-400 errors-first parse & `.codes`-not-substrings, dual-encoding metadata markers — transcoder byte-identity & form preservation & the both-encodings presence-check bug class, two composition disciplines, client-side comment sort & lookup-first labels & `_PERK_LABELS` tuple, one readiness probe two consumers, backend-aware prompts branch on the stamped `cache.plan-ref.provider` not config, scripted GraphQL fake & httpx MockTransport recipes, agent-session emission — internally-gated emitters keep hook sites one-line & force-gate-open/break-substrate fail-open testing, stateful `FakeLinearWorkspace` — named GraphQL operations & read-seeding & UUIDs-only-on-mutations, live-smoke deferral register — gate still unrun), session data & run identity (warm ULID mint in `session_start`'s none arm — grammar-not-subprocess cross-plane proof, two-resolver doctrine — never unify, provenance digest convention & LWW whole-map appends & two-grade refusal tiers & never-trust-stored-paths, the read-only carve-out 2-instance template & JSON-artifact variant, two-digests-two-roles & recompute-derived-flags, GC destructive-op triad & registry-derived terminal set & conservative eligibility ladder & dangling-pointers-by-design), source-scan guard tests (node:test-as-grep recipe — leading-dot patterns & vacuous-scan self-check & production-only, pattern-matches-the-seam self-check, path-construction-scoped literal bans), doc-reconciliation (roadmap `pr` field ≠ merge PR, execute-doc-examples accuracy gate, keep-and-annotate over delete, boundary-criterion check before rewording a principle), borrowed packages (lockstep surfaces recipe — BORROWED_PACKAGES/settings/capability-summary/contracts/tests, setFooter-clobber vetting & `--no-ignore` greps under .pi/npm, thrice-affirmed retirement recipe, foreign tool names inert when absent, repo-non-mutation read-only bar, borrow vs provider-seam criterion), remote-runner (declarative-correct/execution-untested CI seam, Runner contract & two run-ids, establish-before-consume dispatch, fresh-runner git identity & PAT auth, four-candidate worker-entry ladder, honest-fiction loud deferral, open follow-up — defaultCreateRuntime in-memory settings never load disk packages so a real remote launch may register zero extension tools), init/doctor division & managed-convergence SSOT & doctor-disk-vs-selfcheck-prompt & the `GROUP_ORDER` human-render trap (every new Check.group must join the render order) & fix_errors first-class report data & fail-level checks shift verify=True baselines & §8.5/§8.6 machine-surface co-owners & seam-signature ripple & the AGENTS managed-block byte-match proof, init shelling out to external CLIs (skills delivery now load-bearing — two-tier loud-failure posture flip, post-sync presence verification, config-vs-substrate check split), network repairs via the verify-gated gesture never a ManagedConvergence, templates reconvergence, Click bottom-of-file imports & register_with_aliases arity, report-only pre-flights, supervisor cancel/retry control seam, rerun reuse, GHA universal zero-spend smoke short-circuit, fail-soft overlays vs local records truth, subprocess monkeypatching traps, contracts §-numbering grep, post-merge objective roadmap reconciliation; read when working on perk internals, factories, planning/objective/binding/provider subsystems, doctor checks, contracts, or gitignore mechanics → `docs/learned/workflow/`
- **pi/** — Pi extension API (getSystemPromptOptions/ctx.mode/injected-message persistence, registerTool execute results details requirement, planning tools read-only gating traps, tsconfig strict-mode index access in tests, offline `pi.sendUserMessage` injection spy via instance-property override — mandatory for seed-turn invokeCommand tests, headfulUIContext has no select/input/editor, `ctx.ui.editor` facts — no AbortSignal & title-borne key hints & dialog arms test via extracted core, registration-time `process.cwd()` config reads make harness tests host-repo-sensitive — chdir into the scaffold, the `PERK_RUN_ID` harness leak), tool-param decode (`toolParams.ts` tri-state boundary seam — absent vs mistyped, strict-fail bad_input vs native-shape refusals, decode-before-side-effect ordering proofs, exported pure decode for UI tools, never reuse cold-door lenient helpers, fallback-chain optionality — absent flips to `undefined` & the tri-state survives, `idParam`/`idArrayParam` opaque-id params), context injection (conditional inject-and-strip, stage-field disambiguation, stringify-includes once-only dedup, two-flavors-one-customType), context system (no transclusion, ambient index split, read-only bash allowlist — gh subcommand-shaped never verb-inferred), structured output (pi-ai tool-calling not JSON mode, faux-provider offline tests — top-level vs nested registration by caller, `PERK_NO_LLM` the only offline gate — auth resolution succeeds keyless), headless session construction & driving (runtime-factory builds the loader internally, explicit `bindExtensions` `mode:"json"`, `session.subscribe` raw-AgentEvent facts, single-prompt drive + budget watchdog & premature-idle gap, structured run-event stream — additive `RunEvent` union, two fail-soft tiers, route-don't-relay, offline model-availability determinism), subagents (agentOverrides reach builtin-only & per-call inline model override, child-posts-own-mutation vs read-only-child-parent-mutates, context fresh/fork, review 422→comment fallback, agent-def consumer-delivery gap, flat agent/seam-keyed config, TS TOML backslash continuation restriction, cross-plane model/prompt parity, exported guidance offline unit-testing), extension consolidation seams (minimal structural interface — ReportTarget/BranchSource/EntrySink & narrowing plan-named SDK types to structural slices & typing an extracted core's injected dependency as a one-method slice, type-only-import cycle break when a vocabulary type moves owners, pure-module-plus-effectful-seam reconciliation, `appendWorkflowState` strict read-back & verified-linkage tiering & reference-identity object compare & object-valued custom `equals` identity-subset comparators & aspirational-comment migration signal, opt-in flag over API fork, report() owns-the-prefix de-prefixing trap, P1/P2/P3 site triage, report-rollout gotchas — local-`report` shadowing & failFor-only failure surfacing & severity-filter notify asserts), TUI surfaces (surfaces module = surfaces.ts+report.ts only sanctioned rich-UI sites, composed single `perk` status slot & headless full no-op, RPC dual-publish law — factory widgets/setFooter dropped in RPC, setFooter 0.78.1 facts — method-syntax bivariance & render-driven reactivity & once-only install & the single last-wins footer slot (load-order clobber hazard), windower/content-line budgets, truncateToWidth `...`-not-`…` & visibleWidth asserts, factory-widget/invokeCommand harness recipes, width-sweep invariant testing), `pi.events` unreachable from harness — pure-over-a-fake-bus split & env-var over module-constant injection & getFlags/getShortcuts non-registration asserts, faux-model real-runtime drive (nested pi-ai per-instance registry — resolve through pi-coding-agent, inMemory settings don't layer disk → extensionFactories, seed faux-provider auth, eventSink vs file-sink exclusivity) → `docs/learned/pi/`
- **toolchain/** — ruff check vs format (silent-commit trap, `RUF100` fires on a `# noqa` for a non-enabled rule, `RUF043` raw/escaped regex for `pytest.raises(match=...)` metacharacters, template string E501 multiline limits, SIM105 exception suppressions, preview-rule enablement drags cross-bucket fixes forward — ruff bumps too, run_ci green ≠ committable — pre-commit can reformat after CI passed), Biome/tsc gotchas, Biome param-property type-stripping under `node --test` / `organizeImports` assist-only-under-`check` / `let x = undefined` trap / `Omit<Union,K>` collapses a discriminated union — use a distributive Omit / forEach expression-body assertion needs a block body / new files: `biome check --write` before CI / template-literal backtick mangling under `--write` — review the diff, two-commit TS module mv+sweep recipe (blame survives the sweep, non-import-line audits, quoted justfile `**` glob), worktree node_modules stale-SDK trap & package-lock churn cleanup & stale-global-`perk`/self-converge smoke trap (use worktree `.venv/bin/perk` for `shared/` changes; init smokes in a scratch dir; fresh worktree = empty node_modules → `npm ci` before tsc/node --test; a -1/143 test exit with no FAILED line is a SIGTERM kill — rerun before debugging; prove pre-existing breakage with the git-stash diagnostic before assuming you caused it & the delete/edit rebase-conflict keep-deletion-verify-moved-copy recipe), ty narrowing of untyped/JSON dict values (cast vs truthiness guard & the `_require_*` narrowing-helper family for deep GraphQL payloads) & ty suppression syntax (`# ty: ignore[rule]`, mypy-style `# type: ignore` doesn't suppress) & ty enum strictness in tests (pass the member, not the literal — CI-only catch) → `docs/learned/toolchain/`
