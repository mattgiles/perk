# perk docs — research & planning index

**perk** is a Pi-native rebuild of the [erk](https://github.com/dagster-io/erk)
plan-oriented engineering workflow — a Python CLI (the session *exterior*) plus a TypeScript
Pi extension (the session *interior*), sequenced so that **perk bootstraps itself**. The
project is in the **planning stage**; the documents below are the research and the plan, not
shipped behavior.

> **Using perk on your own repo?** Start at [docs/user-docs/](./user-docs/index.md) —
> the operator-facing documentation, organized by the
> [Divio system](https://docs.divio.com/documentation-system/) (tutorials, how-to guides,
> reference, explanation). Everything below this line is perk's internal research &
> planning record, written for perk's own developers.

Read order for someone new: **ROADMAP** (the plan) → **guiding-principles/foundation-open-questions** (the
locked decisions and their rationale) → the research inputs and pattern studies as needed.

## The plan (living documents)

| Doc | What it is |
|---|---|
| [linear-smoke-gate.md](./linear-smoke-gate.md) | The **Linear live smoke gate runbook** (Objective #252, Node 4.1): the manual two-mode validation of the Linear issue backend against a real workspace (bare; GitHub-integration-installed), with a recorded-observations section feeding the documented follow-ups (`.codes` tightening, RATELIMITED posture, mutation identifier acceptance). The offline twin is `tests/test_linear_lifecycle.py`. |
| [ROADMAP.md](./ROADMAP.md) | **Start here.** The phased build plan: core thesis, context strategy, implementation-craft constraints, the seven **locked foundational decisions**, Phases 0–3 with **dogfood gates**, default-package strategy, non-goals, open decisions. |
| [foundation-open-questions.md](./guiding-principles/foundation-open-questions.md) | The thirteen questions (Q1–Q13) that had to be settled before Phase 0, each with options, rationale, and the **recorded resolution**. This is the *why* behind the ROADMAP's "Foundational decisions (locked)" section — read it when you need the reasoning behind a decision. |
| [planning/phase-0-plan.md](./planning/phase-0-plan.md) | Phase 0 decomposed into seven landable turns (T1–T7), each with deliverables, an acceptance gate, and dependencies. The execution plan for the first phase. |
| [phase-1-plan.md](./docs/phase-1-plan.md) | Phase 1 ("close the thin loop — own the spine") decomposed at phase granularity into six landable turns (P1.T1–T6): objective, the dogfood acceptance gate (perk ships perk), dependencies on Phase 0, and deferrals. **Authored on the perk-scaffolded substrate as the Phase-0 dogfood gate.** |
| [phase-2-plan.md](./phase-2-plan.md) | Phase 2 ("deepen the loop — objectives, CI, review") decomposed at phase granularity into twelve landable turns (P2.T1–T12): the **tool-gating primitive** keystone (T1) and perk-owned plan mode (T2), the two **context-isolation shapes** (in-process SDK session T4 / borrowed `pi-subagents` engine T6), the **read-only CI executor** (T5), the **`/address`** review loop (T7, new stage), deepened submit/land/learn (T8), and — deferred latest — **objectives as plan factories** (T9–T11, new `objective-plan` stage) with reconciliation-after-landing; objective, the full-workflow dogfood gate, dependencies, and the Phase-3 deferral boundary. **Authored on the validated Phase-1 spine — every turn rides perk's own loop.** |
| [planning/phase-2-turn-8.md](./planning/phase-2-turn-8.md) | Implementation-level plan + outcomes for **P2.T8** (deepen submission & landing + the CLI plumbing slice), landed as three green seams **T8a/T8b/T8c**. **T8a** — `perk pr-submit` composes an HTML-enhanced PR body (best-effort verbatim-plan `<details>` embed via `get_plan_body`) with a plain-backtick **PR-numbered** checkout footer via **create-then-update** (`github.update_pr_body`), fixing the latent issue-numbered-footer bug; a pure footer-scoped `validate_pr_body` runs as a post-write self-check + backs `perk pr-check`; submit stays **draft** and a new `/ready` (`perk pr-ready` + `extension/ready.ts`) is the deliberate review gate. **T8b** — the squash commit deepens to plain `title + Closes #N` (D8); `/learn` graduates to a real capture pass — a `perk:learn` issue created via the **`perk:learn`-scoped** `find_learn_issue`/`create_learn_issue` (generalized `find_plan_issue` by label/header-key) + the `perk learn-capture` worker + deepened `extension/learn.ts`; registry adds `github.learn` + fills `learn` I/O; the reconciliation-typing vocabulary (Mechanical applied; Reconcilable/objective deferred to T11). **T8c** — the `--remote` stub graduates to `launch.resolve_target` (`Target`/`RemoteTarget`) with `doors.cold_remote: true` flipped on `implement`+`address` (the Phase-3 seam: resolve + surface the `--json` descriptor, exit `remote_not_driven`; the runner is not driven). Amends contracts §8.4 + cli-vs-pi §4.5; ships `verify-p2-t8{a,b,c}.sh`. |
| [planning/phase-2-gate.md](./planning/phase-2-gate.md) | The **Phase-2 gate record** — the visible boundary that closes Phase 2: the gate verbatim, the clause→builder map (objective selects the plan → T9/T10; CI executor iterates → T5; review classified+resolved → T7; prose reconciles after landing → T11; structural read-only enforcement → T1/T2; checkpoints retire `rpiv-todo` → T2c/T12), the automatable preconditions (the cumulative `just verify` incl. `verify-p2-t12.sh`), the live dogfood run (perk drives its **full deepened** workflow on itself — objective → factory-emitted plan → CI-executor iteration → submit/ready → `/address` classify+resolve → land + node-done + reconcile → learn), the borrow→own status (only the `pi-subagents` **engine** survives behind its seam), the Phase-3 deferral boundary, and the "Phase 2 gate met" verdict. |
| [planning/phase-2-turn-12.md](./planning/phase-2-turn-12.md) | Implementation-level plan + outcomes for **P2.T12** (the **Phase-2 dogfood gate** — a **checkpoint turn**, mirrors P0.T7/P1.T6): no new spine handlers. **Retires `@juicesharp/rpiv-todo`** (dropped from `init.py`'s `BORROWED_PACKAGES` + the committed `.pi/settings.json`, mirroring the P2.T2a `pi-plan` two-step; `verify-t7.sh` Check 2 + `test_init_idempotent.py` updated in lockstep) now that perk-owned checkpoints (T2c) satisfy the conditional gate; **reconciles** `README`/`AGENTS`(via `init`)/`contracts`/`ROADMAP`/`index` to "Phase 2 complete"; **asserts** both new stages' (`address`, `objective-plan`) registry I/O + `doors` are filled and `perk registry check` passes. Ships `scripts/verify-p2-t12.sh` (six offline preconditions) + `docs/planning/phase-2-gate.md` (the visible boundary). The live-run evidence + outcomes are written **after** the operator dogfood run. |
| [planning/phase-3-turn-1.md](./planning/phase-3-turn-1.md) | Implementation-level plan + outcomes for **Phase 3 · Turn 1** (the **learned-docs consumer**, erk **hop-2**, GitHub plan #46): the missing consumer of terminal `perk:learn` issues, built as a **plan factory** (mirrors `objective-plan`, not a direct doc-writer). `perk learn-docs` (+ warm `/learn-docs`) gathers open `perk:learn` issues into an inbox and launches a read-only plan-mode session that authors a `docs/learned/` consolidation plan (steps refresh `docs/learned/*.md` + the standalone `docs/learned/index.md` catalog + the **compressed ambient index** in `.pi/APPEND_SYSTEM.md`), saved with `consumed_learn`. On land the consumed issues are closed + labelled `perk:consolidated` (fail-open, mirrors the objective reconcile). No new registry stage (borrows `plan`). |
| [planning/phase-2-turn-11.md](./planning/phase-2-turn-11.md) | Implementation-level plan + outcomes for **P2.T11** (objective **reconciliation after landing**, GitHub plan #23): closes the objective loop — when a PR linked to an objective node merges, the roadmap reconciles against what was *actually* built, along the D9 section-boundary typing. **T11a — Mechanical** (deterministic, on land): the cold land path auto-marks the objective node done. **T11b — Reconcilable**: the `reconcile_objective` tool rewrites only the marker-bounded Reconcilable prose region (the Mechanical roadmap table + Immutable notes are never touched), nudged by `/objective-reconcile`. |
| [planning/phase-2-turn-10.md](./planning/phase-2-turn-10.md) | Implementation-level plan + outcomes for **P2.T10** (the **`/objective-plan` factory** + completion-audit, GitHub plan #21, **new `objective-plan` stage**): the objective **transition** surface on top of T9's mechanics — a plan factory that selects the next actionable objective node and emits a **bounded plan** through the existing `plan → save` spine, plus the **completion-audit** contract exposed as a bounded model tool (`objective_node`). The new initial node of the deepened graph (`predecessors: []`, `successors: [plan]`). |
| [planning/phase-2-turn-9.md](./planning/phase-2-turn-9.md) | Implementation-level plan + outcomes for **P2.T9** (objective **storage + mechanics** — the plan factory's foundation, GitHub plan #19): the **objective layer** — a long-running goal that *generates* bounded plans rather than being implemented directly. T9 ships the **deterministic mechanics only**: a pure Python objective module (`perk/objective.py`), the GitHub gateway ops that persist objectives as issues, the cold-door `perk objective` group (`create`/`show`/`node`/`next`), and the dependency-graph `next` selection. No transition surface (that's T10). |
| [planning/phase-2-turn-7.md](./planning/phase-2-turn-7.md) | Implementation-level plan + outcomes for **P2.T7** (the **`/address` review loop**, GitHub plan #15, **new `address` stage**): perk's review-handling stage — **classify-then-act**. The verbose GitHub-feedback fetch + classification runs inside an **isolated spawned read-only child** (the borrowed `pi-subagents` engine wired in T6), so raw comment JSON never enters the main session; the **parent** (judgment + durable writes) applies fixes and resolves threads through a deterministic batched op (`resolve_review_threads`). First new stage since the spine; sits between `submit` and `land` (`doors.cold_remote: true`). |
| [planning/phase-2-turn-6.md](./planning/phase-2-turn-6.md) | Implementation-level plan + outcomes for **P2.T6** (the **spawned delegation engine seam**): perk's *second* context-isolation shape — a **spawned** read-only child engine stood up by **borrowing the `pi-subagents` engine** behind a thin seam rather than building a spawn primitive. **Substrate only**: wire the package, establish the perk-owned agent-definitions home (`.pi/agents/`), add a standing `perk doctor` signal, amend the cross-plane contract, and record the de-risking spike that settles open decision #6. The first consumer is T7 (`/address`). |
| [planning/phase-2-turn-5.md](./planning/phase-2-turn-5.md) | Implementation-level plan + outcomes for **P2.T5** (the **read-only CI executor**, GitHub plan #11): the perk-owned Run→Report→Fix→Verify CI executor — a deterministic, in-process check runner that runs the project's configured `[ci]` named checks and **reports** failures (double-delivery, output isolated to scratch) so the parent agent analyzes, fixes in its own turn, and re-verifies. The executor never edits, fixes, or loops — a stateless oracle backing the `run_ci` tool. Interior/TS-only; no new registry stage. |
| [planning/phase-2-turn-4.md](./planning/phase-2-turn-4.md) | Implementation-level plan + outcomes for **P2.T4** (the **in-process read-only SDK session** — context-isolation primitive #1): the perk-owned, in-process, fully-isolated read-only child spun at the SDK level via `createAgentSession`, plus the handoff contract (cap model-visible output, keep the full result in a verified scratch file + structured block, return double-delivery) that the spawned shape (T6) also honors. **Substrate only** — the consumer is the read-only CI executor (T5). No registry stage, no door change. |
| [planning/phase-2-turn-3.md](./planning/phase-2-turn-3.md) | Implementation-level plan + outcomes for **P2.T3** (**enforce formatting via a prek `ruff-format` hook**): the Phase-2 decomposition proposed a `tool_result` post-edit formatter middleware (the Pi analogue of erk's PostToolUse Ruff-on-edit); this turn **declines the middleware** and instead meets its load-bearing goal — *formatting never becomes a CI iteration* — with a one-line addition to `prek.toml`: a `ruff-format` pre-commit hook alongside the existing `ruff-check` hook. |
| [planning/phase-2-turn-2.md](./planning/phase-2-turn-2.md) | Implementation-level plan + outcomes for **P2.T2** (perk-owned plan mode; warm `/implement`; checkpoints), landed as three green seams **T2a/T2b/T2c**. **T2a** — `extension/planMode.ts` (`/plan` + `Ctrl+Alt+P` + `--plan` over T1's gate; `perk:plan-context` injection) + `extension/config.ts` (dependency-free TOML-subset reader for the `[workflow] plan_authoring` addendum); **retires `@tombell/pi-plan`** (perk owns `mode` end-to-end); `/plan-save` command auto-exits the gate on success (D1a). **T2b** — deepens warm `/implement` into an in-worktree `ctx.newSession` fresh-context handoff (`implementHandoffPrompt`, output capped, dirty-tree manually gated); `doors.warm` stays `false` (D2: no extension API changes cwd). **T2c** — `extension/checkpoints.ts` (dedicated `perk:checkpoint` entry; seed from the plan body's `## Steps`; inert on prose plans; scan-after-marker rebuild; `[DONE:n]` advance; headless-safe). Amends contracts §8.3; ships `verify-p2-t2{a,b,c}.sh`. |
| [planning/phase-2-turn-1.md](./planning/phase-2-turn-1.md) | Implementation-level plan + outcomes for **P2.T1** (the **tool-gating primitive** keystone): the generic read-only-mode enforcement (`extension/toolGating.ts`) attaching to `perk:workflow-state.mode` — `setActiveTools` allowlist + `tool_call` bash sub-allowlist + `before_agent_start` injection + `context` strip-when-off, snapshot-then-restore, synced on `session_start` AND `session_tree`, fail-closed. Ships the `enter`/`exit` surface T2/T5 consume; no `/plan` ownership, no new registry stage. Amends contracts §8.3; ships `verify-p2-t1.sh`. |
| [planning/phase-1-gate.md](./planning/phase-1-gate.md) | The **Phase-1 gate record** — the visible boundary that closes Phase 1: the gate verbatim, the spine→doors map, the live run (**perk shipped perk** — plan #1 → PR #2 merged → learned, `perk resume 1` confirms the loop closed), the automatable preconditions (the cumulative `just verify`), the **two real bugs the dogfood surfaced + fixed forward** (T4c implement priming/plan-arg, T3b plan-save robustness), the prek remote-astral deviation, the Phase-2 deferral boundary, and the "Phase 1 gate met" verdict. |
| [planning/phase-1-turn-6.md](./planning/phase-1-turn-6.md) | Implementation-level plan for **P1.T6** (the **Phase-1 dogfood gate** — perk ships perk): a **checkpoint turn** (mirrors Phase-0's T7 + `phase-0-gate.md`), no new spine handlers. **D1:** the dogfood change is **add `prek` + a ruff pre-commit hook** (a `local` `.pre-commit-config.yaml` over `uv run ruff`, plain check — single ruff-version source; `just hooks` = `uvx prek install`), authored **through the loop**. **D2:** the user runs the **live** loop (first live GitHub + live `pi`); this doc ships a copy-pasteable **runbook** — a bash-scriptable cold spine (`plan-save`/`pr-submit`/`pr-land`) + marked interactive `pi` steps, each with a report-back. **2a** free-authors the implement; **2b** tests **both doors** for `submit`/`land` (idempotency → one door mutates, the other confirms a no-op = live warm/cold **parity**), the `/implement` guard's "continue" path, `learn` (warm-only), `resume` (cold-only). Ships `scripts/verify-p1-t6.sh` (offline preconditions, mirrors `verify-t7.sh`); reconciles `README`/`AGENTS`/`contracts`/`index` to Phase-1-complete; the gate record (`phase-1-gate.md`) + §12 outcomes are written **after** the run. |
| [planning/phase-1-turn-5.md](./planning/phase-1-turn-5.md) | Detailed, implementation-level plan for **P1.T5** (PR lifecycle — `/submit`, `/land`+`/learn`, `perk resume`): **closes `implement → submit → land → learn`** + adds the one new CLI verb, as a **T5a/T5b/T5c seam**. **D1 (the architecture fork):** GitHub mutations stay **canonical in the Python gateway**; the TS warm doors **delegate via `pi.exec`** (T3's precedent confirmed as the standing pattern — the §8.4 "two gateways" hypothesis retired; cache/session tiers keep per-plane I/O). **T5a:** `perk pr-submit` worker pushes the branch + opens a **draft** PR (`Closes #N` + plain-text checkout footer, **no** full-plan re-embed — D2) + `update_plan_header` populates staged `branch`/`pr`/`lifecycle_stage=impl` (D7); warm `submit` tool/`/submit` command delegate. **T5b:** `perk pr-land` marks-ready-if-draft + squash-merges (idempotent; `gh pr ready` the lone GraphQL exception — D3) + sets `pending-learn`; `/learn` is a **thin TS-only** marker clear (no GitHub write — D4). **T5c:** `perk resume <plan>` reads the plan (`github.get_plan`), reconstructs `cache.plan-ref`, derives the current stage via a 4-row state machine (planned→implement, impl+open→submit, merged+pending-learn→learn — D5), and launches via T4a's `launch_stage`. All gh writes reuse T2a's REST conventions; fills registry `submit`/`land`/`learn` I/O (D8); authors §8.4's PR-op payloads + `get_plan` + the D1 decision + a `pending-learn` paragraph; ships `verify-p1-t5{a,b,c}.sh` (offline). Defers AI body craft, the review/`address` loop, deep learn tooling, and remote-branch worktree recreation (Phase 2). |
| [planning/phase-1-turn-4c.md](./planning/phase-1-turn-4c.md) | **Corrective turn** cut mid-T6 (the dogfood run surfaced two real `perk implement` bugs): a *direct* fix that **converges forward** on T4a (history not rewritten; contracts §8.4 amended). **Bug 1 — no session priming** (`launch_stage` launched bare `pi` → idle "does nothing"; hit `resume` too): the launcher now passes an **initial prompt** to `pi` (read the plan via `gh issue view <n> --comments`, implement on the branch, `/submit` when committed), **implement-only** (`plan` stays user-driven). **Bug 2 — no plan positional** (`perk implement 1` forwarded `1` to pi as a stray prompt; T4a's D2 was the deviation from phase-1-plan §P1.T4's `perk implement <plan>`): `implement` graduates to a **dedicated command** (`implement_cmd.py`; generic generator skips it via `stages.DEDICATED_STAGES`) accepting an optional issue number — `perk implement 42` resolves via resume's pieces (`get_plan` + `reconstruct_plan_ref`), writes the active `cache.plan-ref`, then launches; omit it to use the active ref. Promotes `resume_cmd.parse_plan_id` to public. Ships `tests/test_implement_cmd.py` + priming assertions in `test_launch.py` + `scripts/verify-p1-t4c.sh`. No registry I/O change. |
| [planning/phase-1-turn-4.md](./planning/phase-1-turn-4.md) | Detailed, implementation-level plan for **P1.T4** (`perk implement` — the cold door + session-lifecycle gates): **closes `save → implement`** as a **T4a/T4b seam**. **T4a (Python/exterior):** teaches the registry-generated launcher to **resolve + materialize a worktree from the active `cache.plan-ref`** — derive a deterministic `plan-<pr_id>` name (D1), read the active ref (no `[PLAN]` arg; arbitrary `#N` is T5c — D2), make the generic launcher plan-ref-aware (D3), **idempotent reuse** on re-run (D4), and **write handoff + plan-ref into the new worktree** (D5) so the extension's existing `session_start` reconciliation links `active_plan_ref` from the worktree cwd (no extension change). **T4b (TS/interior):** one reusable **dirty-repo lifecycle gate** (`session_before_switch`/`session_before_fork` → `{cancel:true}` via `pi.exec("git",["status","--porcelain"])`), **scoped to active workflows** (D6, fail-safe-headless), plus a **guard-only `/implement`** that *enforces* the stage's `warm:false` legality (D7). Spike S-B confirmed the gate trigger (`extensionRunner.emit({type:"session_before_fork"})` round-trips `{cancel}`). Fills registry `implement` I/O (`requires/reads/writes`, D8); amends contracts §8.4 + a lifecycle paragraph; ships `verify-p1-t4a.sh` + `verify-p1-t4b.sh`. Defers in-process `ctx.newSession`, the proceed-anyway dialog, and `perk resume` (T5c). |
| [planning/phase-1-turn-3b.md](./planning/phase-1-turn-3b.md) | **Corrective turn** cut mid-T6 (dogfood-surfaced): `/plan-save` saved a *conversational message* as the plan and a **TOML `# comment` became the title**. Root cause: `<proposed_plan>` was **perk-invented, not native** to `@tombell/pi-plan` (pure tool-gating + a "propose a plan" prompt; free-form prose, no plan schema), so the extraction always fell back to the whole last message; `derive_title` then grabbed a `#` inside a ```toml fence. Fixes: **(1)** the `plan_save` **tool** (explicit `plan` param) is canonicalized as the robust save (skill + runbook to a *exit-plan-mode → call tool* flow); **(2)** `savePlan` **fails fast while plan mode is active** — perk can't cleanly *exit* pi-plan (it owns `enabled`) but **detects** it via pi-plan's own persisted `plan-mode-state {enabled}` entry (`isPlanModeActive`, LWW; a soft coupling removed in Phase 2); **(3)** `derive_title` **skips `#` inside fenced code blocks** + prefers a real H1; **(4)** the invented `<proposed_plan>` scrape is dropped. Ships harness `planMode` planting + `planSave.test.ts`/`test_plan.py` cases + `scripts/verify-p1-t3b.sh`. No registry/contract change. |
| [planning/phase-1-turn-3.md](./planning/phase-1-turn-3.md) | Detailed, implementation-level plan for **P1.T3** (`/plan-save` — the warm terminating door + the planning skill): the **in-session warm door** that **wraps** T2's storage and **closes `plan → save`** (incremental dogfood starts here). Ships the `plan_save` **tool** (`terminate: true`, `executionMode: "sequential"`, dual-surface `content`+`details`, `promptGuidelines`) + a thin **`/plan-save` command** twin over one `savePlan()` core that **delegates the GitHub write to `perk plan-save --json`** (D2 — process-launch + the cli-vs-pi §3.2 machine-JSON surface, **not** a TS reimplementation), resolves the binary via **`PERK_BIN`** (robustness + the offline test seam), and appends `active_plan_ref` for immediate in-session linkage (strict read-back, idempotent, headless-safe). Adds the packaged **`skills/perk-plan/SKILL.md`** (declared via `package.json` `pi.skills`; carries erk's **line-numbers-DISALLOWED → durable-anchors** rule). Keeps borrowed `@tombell/pi-plan` (command-first because it hides custom tools mid-plan-mode); no plan→implement transition (that is **T4**'s cold door). Amends contracts §8.3/§8.4; fills `save.writes` to `[github.plan, cache.plan-ref, session.workflow-state]`; ships `scripts/verify-p1-t3.sh` (offline, faked via `PERK_BIN`). |
| [planning/phase-1-turn-2b.md](./planning/phase-1-turn-2b.md) | Detailed, implementation-level plan for **P1.T2b** (Plan ref — materialize `cache.plan-ref` + the session linkage): persists T2a's emitted ref in two tiers — the **`cache.plan-ref` file** (`.pi/workflow/plan-ref.json`, the cross-plane local mirror, primitives in `perk/cache.py` + `extension/cache.ts`) and the **`active_plan_ref`** session field, **reconciled from the file on `session_start`** (idempotent by `(provider, pr_id)`, strict read-back, headless-safe) and preserved across `session_tree` by the LWW rebuild. Mostly TS (the `extension/` linkage, verified through the **T1 harness**) with one small Python touch (the cold door `perk plan-save` writes the file on a real save, not on `--dry-run`) + an `init` gitignore entry. Amends contracts §8.1/§8.3/§8.4; fills registry `save.writes` to `[github.plan, cache.plan-ref]`; ships `scripts/verify-p1-t2b.sh`. Unblocks T3 (warm twin) + T4 (worktree). |
| [planning/phase-1-turn-2a.md](./planning/phase-1-turn-2a.md) | Detailed, implementation-level plan for **P1.T2a** (GitHub plan write — the Python/worker half): the **first GitHub mutation** (Phase 0 was read/verify only) — a `perk plan-save` cold/worker command that creates a plan issue with the **header/body split** (`perk/plan.py` metadata-block engine + `perk/github.py` `create_label`/`create_plan_issue`/`add_issue_comment`/`find_plan_issue`), the first real `require_github` consumer. Establishes the three reusable **write-safety conventions** (`--dry-run`, `run_id`-keyed idempotency, `GitHubError → UserFacingCliError`); writes `github.plan` only and **emits** the provider-agnostic plan-ref (all of `cache.plan-ref` is T2b; the warm `/plan-save` TS twin is T3). Amends contracts §8.4 (authors the mutation payloads + plan-header fields + `perk:plan` label); fills registry `save.writes`; ships `scripts/verify-p1-t2a.sh`. |
| [planning/phase-1-turn-1.md](./planning/phase-1-turn-1.md) | Detailed, implementation-level plan for **P1.T1** (the session test harness — de-risk first): a TS harness (`extension/testing/harness.ts`) that drives a **real bound `AgentSession`** through the SDK to re-prove the Phase-0 T3 `perk:workflow-state` lifecycle (claim/keep/fork/`session_tree`) end-to-end, **offline, no LLM**. Records the spike findings (binding — not creation — emits `session_start`; `hasUI` tracks `uiContext`; fork via a planted session `.jsonl`; keep via `reload()`); locks decisions D1–D8; ships `scripts/verify-p1-t1.sh`. Test infra only — no Python, no contract/registry change. |
| [planning/phase-0-gate.md](./planning/phase-0-gate.md) | The **Phase-0 gate record** — the visible boundary that closes Phase 0: the gate verbatim, how it maps onto the borrowed packages (metadata-confirmed), the automatable preconditions (`verify-t7.sh`), the reproduce-the-interactive-demo steps, the deferral boundary, and the "Phase 0 gate met" verdict. |
| [planning/phase-0-turn-1.md](./planning/phase-0-turn-1.md) | Detailed, implementation-level plan for **T1** (monorepo skeleton + the minimal `perk init`): grounded Pi packaging facts, target repo layout, a de-risking spike, the `init` spec, and runnable acceptance checks. |
| [planning/phase-0-turn-2.md](./planning/phase-0-turn-2.md) | Detailed, implementation-level plan for **T2** (author & lock the shared contracts): the `registry.yaml` shape + the six-stage table, the state-key vocabulary, the four `contracts.md` specs (`.pi/workflow/` layout, `PERK_RUN_ID`, `perk:workflow-state`, GitHub gateway), the both-planes self-check, and runnable acceptance checks. Folds in the seven prior-art revisions. |
| [planning/phase-0-turn-4.md](./planning/phase-0-turn-4.md) | Detailed, implementation-level plan for **T4** (CLI exterior core): the `tomllib` config loader (`perk.toml` + `perk.local.toml`), registry-generated `perk <stage>` launchers, `perk worktree create/list/remove`, and the `os.execvp` launch primitive that closes the T3 loop (shell mints `run_id` → launches `pi` → T3 extension claims it). Introduces the `PerkContext`/`require_*` DI; extends `perk init` to scaffold config + converge `.gitignore`. Python-only (reuses T3's interior). Locks the six T4 decisions. |
| [planning/phase-0-turn-3.md](./planning/phase-0-turn-3.md) | Detailed, implementation-level plan for **T3** (state-tiering helpers, both planes, no workflow logic): the `run_id`/ULID + `.pi/workflow/` cache I/O (`perk/run_id.py`, `perk/cache.py`, `extension/cache.ts`), the `perk:workflow-state` rebuild + verified-linkage claim + fork-derive (`extension/workflowState.ts`), the `perk state` dev surface, and a **real** 2-process reload + fork gate (via `pi --session-dir`/`--fork`) plus Node `node:test` units. Locks the six T3 decisions (canonical ULID, visible `perk state`, mark-consumed, fork-now, built-in TS runner). |
| [planning/phase-0-turn-7.md](./planning/phase-0-turn-7.md) | Implementation-level plan for **T7** (the Phase-0 dogfood gate — a half-turn **checkpoint**): tie the bow on Phase 0 and prove the ROADMAP gate (a later phase can be **planned in read-only plan mode with a live todo overlay on a `perk init`-scaffolded, `perk doctor`-healthy repo**) by **authoring the Phase-1 plan itself** on the scaffold. Maps the gate's abstract phrases onto the borrowed packages (`@tombell/pi-plan` = read-only plan mode, `@juicesharp/rpiv-todo` = the live todo overlay). Verification + a docs pass (`README.md`, `AGENTS.md`) + three demonstration artifacts (`scripts/verify-t7.sh` preconditions, `docs/docs/phase-1-plan.md` living proof, `docs/planning/phase-0-gate.md` the visible boundary); folds in the two T6 review fixes + finishes the `docs/planning/` reorg. **No new machinery, no new dep.** Locks decisions D1–D7. |
| [planning/phase-0-turn-6.md](./planning/phase-0-turn-6.md) | Detailed, implementation-level plan for **T6** (`perk doctor` — `init`'s diagnostic twin): a single-module health engine (`perk/doctor.py` — `run_doctor`/`DoctorReport`/pure-data `Check`) that **reuses `init`'s convergence helpers in dry-run mode** for one desired-state SSOT (the D2 `init.py` refactor to a plan/apply shape), six check groups (environment/github/package/repository/registry/state), `--fix` that re-converges drift plus an honest **empty** legacy-migration seam, self-vs-consumer dual mode via `capabilities.applicable`, and the second supervisor `--json` + stable exit-code surface (contracts §8.6; missing tool = exit 1 *report-don't-refuse*, only `not_a_repo` blocks). Python-only; no new dep. Locks decisions D1–D7. |
| [planning/phase-0-turn-5.md](./planning/phase-0-turn-5.md) | Detailed, implementation-level plan for **T5** (complete & harden `perk init`): environment + GitHub verification (`perk/env.py`, `perk/github.py` — the §8.4 verification ops, never mutating), a lightweight capability inventory (`perk/capabilities.py`), the flag set (`--force`/`--no-interactive`/`--json` — `--upgrade` collapsed into the convergent default), the post-init markdown handoff (the T7 dogfood on-ramp), and the supervisor `--json` + stable exit-code surface (`InitReport`). Introduces `require_github`. Python-only; no new dep. Locks decisions D1–D7. |

## Research inputs (grounding; mostly stable)

| Doc | What it is |
|---|---|
| [RESEARCH.md](./guiding-principles/RESEARCH.md) | The seed document: original problem analysis and the Pi-native architecture rationale the roadmap is built on. |
| [PRIOR_ART.md](./guiding-principles/PRIOR_ART.md) | erk distilled into findings for perk (12 sections: state tiers, plan storage, objectives, PR operations, CI/review, context injection, hooks, capability model, etc.). |
| [erk-subagent-usage.md](./guiding-principles/erk-subagent-usage.md) | How erk used subagents, and the governing principle perk adopts: a subagent is a **context-and-capability device, not a parallelism trick** (11 sections — route-don't-relay, double-delivery, the three never-delegate boundaries, model tiering). |

## Pattern studies (how to build it in Pi)

| Doc | What it is |
|---|---|
| [pi-best-practices.md](./guiding-principles/pi-best-practices.md) | The **authoritative** patterns, from Pi's own `examples/` and the SDK (plan-mode recipe, presets, subagent spawn, handoff, goal/loop/review controllers, `createAgentSession`/`SessionManager`). The templates perk follows. |
| [agent-stuff-best-practices.md](./guiding-principles/agent-stuff-best-practices.md) | Corroborating real-world patterns from `mitsuhiko/agent-stuff` (packaging idiom, structural command safety, `go-to-bed.ts`/`uv.ts`, `goal.ts` budget + completion-audit). Independent confirmation of the same bets. |

## Architecture / design principle

| Doc | What it is |
|---|---|
| [python-cli-guidelines.md](./guiding-principles/python-cli-guidelines.md) | House style for perk's **Click**-based Python CLI: the three-layer command pattern, context DI, option/flag conventions, two-tier validation, `UserFacingCliError`, `\b` help text, human-vs-machine output, group structure, and testing. Distilled from the erk prior-art. |
| [cli-vs-pi.md](./guiding-principles/cli-vs-pi.md) | The CLI/extension separation: **boundary = the session; authority follows the actor.** The CLI owns the exterior, the extension owns the interior; they coordinate only through durable state, process launch, and a shared static schema. Defines **stage parity** (warm/cold-local/cold-remote doors from one stage registry). |
| [design/headless-worker.md](./design/headless-worker.md) | The Phase-1 autonomy spike (Objective #137 node 1.1): the gap list for the in-process SDK headless drive pathway, and the headless-worker contract — inputs, terminal-signal, outcome shape — that node 1.2 builds against. |
| [design/migration-adoption-audit.md](./design/migration-adoption-audit.md) | The migration/adoption audit (Objective #137 node 4.2): erk's four migration surfaces audited against perk's issue-canonical Pi-native model — adopt-nothing outcome with per-surface drop rationale and record-only follow-on. |
| [design/dignified-convergence.md](./design/dignified-convergence.md) | The **dignified-Python convergence checklist + per-module friction backlog** (Objective #225 node 4.1): the ten-axis audit checklist with enforcement tiers, the candidate-ruff-rule rulings with measured violation counts, one backlog entry per non-CLI `perk/` module bucketed by sweep node, the erk-pattern adopt/drop table, and the `github.py` rebalancing ruling. Nodes 4.2–4.5 implement it. |
| [design/tui-charter.md](./design/tui-charter.md) | The **binding visual design charter** for perk's pi-TUI presence (Objective #251 node 1.1): the UI-emission inventory, surface taxonomy + placement rules, height/width budgets, glyph + severity vocabulary, and the adopted/declined richer pi surfaces (footer ownership, themed widgets, working indicator). Nodes 2.1–3.1 implement it. |

## Explainers (interactive HTML)

> **Status: not currently on disk — to regenerate.** Both were authored earlier this session
> / a prior session but are absent now and were never committed. Regenerate on request.

| Doc | What it is |
|---|---|
| `erk-explained.html` | A self-contained, interactive first-principles explainer of erk (layered diagrams, lifecycle stepper, glossary, theme toggle). |
| `perk-explained.html` | The companion explainer for perk, built around a **"follows erk ↔ departs from erk"** motif — what the Pi-native rebuild keeps and what it changes given control of Pi's execution context. |

## How the documents relate

- **ROADMAP § Foundational decisions (locked)** ⟷ **guiding-principles/foundation-open-questions Q1–Q13** —
  the ROADMAP carries the summary; the open-questions doc carries the full options/rationale
  and any qualifications. Keep them in sync (the Q5 stage-set decision, for example,
  reshaped the ROADMAP's Phase-1 spine to `plan → save → implement → submit → land → learn`).
- **guiding-principles/cli-vs-pi.md** feeds the ROADMAP's stage-registry decision (#3) and the CLI/extension
  split throughout.
- **guiding-principles/PRIOR_ART / guiding-principles/RESEARCH** are the source findings; **pi- / agent-stuff best-practices** are
  the build templates; the ROADMAP cites all four at the point each pattern is used.

## External references (not in this repo)

- erk source — `~/dev/github/dagster-io/erk` (workflow being ported).
- erk learned-docs mirror — `.prior-art/erk/docs/learned/` (the prior-art corpus).
- Pi docs — `~/dev/docs/pi/` and the npm install under `@earendil-works/pi-coding-agent/docs`.
- Pi examples — `~/dev/github/earendil-works/pi/packages/coding-agent/examples/` (authoritative pattern source).
- `mitsuhiko/agent-stuff` — `~/dev/github/mitsuhiko/agent-stuff/` (corroborating patterns).
- `pi-subagents` — `~/dev/github/nicobailon/pi-subagents` (the delegation engine perk borrows in Phase 2).
