# Remote-dogfood web hello

On 2026-07-06, the `fetch_content` web tool was exercised on the remote runner against
<https://example.com>. The fetch succeeded and the page's `<title>` text is **Example Domain**.
This is positive proof that the `pi-web-access` extension loaded (no `extension load error`) and
its tools work on the remote runner.
