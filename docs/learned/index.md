---
title: docs/learned catalog
read_when: You need to find a durable learned doc, or you are authoring/refreshing one via /learn-docs.
---

# `docs/learned/` — the durable learnings catalog

This is the **full catalog** of perk's learned docs: token caches of cross-cutting reasoning for
future AI agents, consolidated from terminal `perk:learn` issues by the `/learn-docs` plan factory
(see the `perk-learn-docs` skill). Each doc lives under `docs/learned/<category>/` with light
frontmatter (`title` + `read_when`).

A **compressed routing index** of these docs is kept ambient in `.pi/APPEND_SYSTEM.md` (appended to
every session's system prompt); this file is the on-demand full listing that ambient index points
into. Both are maintained by `/learn-docs` plans — never by `perk init`.

## Documents

| Category | Document | Read when |
|---|---|---|
| workflow | [plan-factories.md](workflow/plan-factories.md) | Building or debugging a perk plan factory (learn-docs, objective-plan, any on-demand factory launching a read-only session), or the cold-injects/warm-instructs delivery asymmetry as a reusable factory pattern |
| workflow | [human-engagement-reads.md](workflow/human-engagement-reads.md) | Working on the §8.25 human-engagement read contract — an engagement read seam (issue-keyed vs node-keyed), a flow consumer, the `perk/backends/engagement.py` renderer family / author classifier, the empty/no-op conformer family, or the cold-injects/warm-instructs delivery asymmetry |
| workflow | [in-place-adoption.md](workflow/in-place-adoption.md) | Adopting a pre-existing human-authored issue or Linear project as a perk plan/objective in place (`plan from`, `objective author --from`) — the dedicated-writer-not-borrowed-mutator rule, `read_issue`/`adopt_source_as_objective`, the handoff-rides-the-link carrier, backend-discriminated OPEN refusal, the adopt-mapping side-map, or the additive labelIds union |
| workflow | [plan-ref-lifecycle.md](workflow/plan-ref-lifecycle.md) | Debugging plan-ref linkage, adding a worktree stage, extending PlanRef schema, threading a non-default `base` branch (resolve-once-then-pin, base≠--base, the `--worktree NAME` returned-field-as-scratch clobber, the always-emitted-`to_data`-field sweep), implementing on-land secondary bookkeeping, or the cross-backend plan-ref clobber hazard (an objective-linked `plan save` inside an active worktree hijacks the gitignored plan-ref) |
| workflow | [plan-save-surfaces.md](workflow/plan-save-surfaces.md) | Working on plan-save / objective-node linkage, debugging a dropped objective_id / consumed_learn (the historical fidelity-gap incident — now closed by the two recovery carriers), adding context that must survive a model's save-surface choice, the resolvePlanSource artifact→param→transcript chain (param-path message stays byte-stable; the stale-draft hazard), the warm objective_node_claim recovery carrier, or prepending a copyable command callout (server-assigned-id timing → four per-backend write strategies: post-create read+PATCH / fold-at-compose / post-create content update / fold-into-unified-desc + idempotency guard) |
| workflow | [plan-review-flow.md](workflow/plan-review-flow.md) | Working on plan_review / a review backend (plannotator, first-party, tombell), the approvalSave seam (gate-exit ownership), the asymmetric review-surface tiering (artifact→param only), wiring a review surface's APPROVED/DENIED arms (the approved-first routing split), flavoring the shared first-party core for a second subject, changing factory/authoring guidance prose (the three-tier prose mirror), the `PLAN_AUTHORING_CONTEXT` consult-learnings nudge seam + its three-surface lockstep, the reviewOutcomeResult/SKIP_TEXT/guard-ordering footguns, or testing dialog flows via the extracted core + scripted UI fake |
| workflow | [objective-lifecycle.md](workflow/objective-lifecycle.md) | Working on objective node status transitions, objective-plan selection (pending-first over live claims, `resumable_claims` surfacing, the accepted backlink race — a `planning`-stuck node with `pr: null` after merge is its known signature), the authoring/save loop (the review-first objective-draft JSON artifact, render-at-the-door, the draft-module-leaf rule), the deterministic `perk objective run` supervisor loop, the "design-only node" reframing pattern (deliverable is a design doc; mandatory post-merge reconcile; the reconcile-ready *audit* node + the audit-a-green-codebase methodology), or a node stuck in planning |
| workflow | [warm-door-commands.md](workflow/warm-door-commands.md) | Building or fixing a warm perk slash-command (/plan-save, /objective-save, /address, /learn-docs), debugging a command that dead-ends or false-succeeds, wiring how a warm TS door renders a cold Python door's structured sub-result, or the reactive sub-result-gated + capped drive (`/submit`'s driveConflictResolution) |
| workflow | [skill-bindings.md](workflow/skill-bindings.md) | Working on skill-binding config, the cold/warm delivery doors, the resolver, the linked-worktree skill mirror (delivery depends on the cold door mirroring `.agents/skills/`), doctor validation of bindings + the injection-presence mirror, skills `references:` frontmatter + subdirectory routing (zero init/doctor wiring — the per-skill whole-dir worktree symlink carries `references/` for free, no wheel entry), a description-discovered (non-stage-bound) skill + its self-contained references second-mirror (the perk-expert pattern), or debugging double-delivered / missing binding context |
| workflow | [shared-contracts.md](workflow/shared-contracts.md) | Adding parsed shared/ data files (the TS reader is now the vendored `miniYaml`, not the `yaml` npm package), registry stages, changing a stage's registry `writes` (the two-pytest-pins gotcha), prose contracts maintenance (§-numbering grep), cross-plane byte-parity prompt fragments (SSOT helper + lockstep substrings), wording-pinning content-parallel prompts in both suites, registry vocabulary deferral (keys land with their first declaring stage), post-merge objective roadmap reconciliation, or tracing shared contract ripples |
| workflow | [extension-clone-lifecycle.md](workflow/extension-clone-lifecycle.md) | A consumer repo loads none of perk's tools or the extension fails at launch (Cannot find module / months-old code), you are touching extension-clone materialization (the launch warm-clone, init/doctor freshness reconcile), the ownership recipe (init-converges / doctor-repairs / launch-warms), the vendored miniYaml reader, or you need pi's `git:`-package loading internals |
| workflow | [init-doctor.md](workflow/init-doctor.md) | Adding a managed piece (doctor checks), choosing committed-tracked delivery vs a cold-door worktree symlink mirror (the prune-the-owned-subdir rule), adding a transient file to gitignore, the extension-clone freshness reconcile + retired `extension-deps` check (commit-freshness ≠ dependency installation — see extension-clone-lifecycle.md), doctor migration, templates reconvergence, Click bottom-of-file imports and register_with_aliases arity, doctor-disk vs selfcheck-prompt, the GROUP_ORDER render trap, fail-level baseline shifts, fix_errors as first-class report data, network repairs via the verify-gated gesture (never a ManagedConvergence), adding a NEW gated probe beside an existing gated check (the monkeypatch-census rule + the `report.healthy`-is-wrong non-fatal-assertion gotcha + the two-phase independent probe), the §8.5/§8.6 machine-surface co-owners, seam-signature ripple, report-only pre-flights, or the optional `EnvCheck` tier (a non-fatal tool that warns not blocks — the `optional` flag + `required_tools_ok` filter, no `GROUP_ORDER` change) |
| workflow | [init-external-cli.md](workflow/init-external-cli.md) | Making perk init shell out to an external CLI, choosing a failure posture (best-effort vs load-bearing two-tier loud failure — the skills posture flip), declaring a committed manifest fragment, pinning a self-repo vs consumer ref (incl. `__version__`'s post-collapse role: version-string + AGENTS-stamp only, never a ref pin), promoting an external skill into the managed manifest (the three-SSOT split PERK_SKILLS/REQUIRED_EXTERNAL_SKILLS/MANAGED_SKILL_NAMES verification SSOT, dagster ref:master, the conflict-not-missing-skill + "Converged before failure" regen path), scoping a cross-repo plan to the perk slice, or the `PERK_SKILLS` SSOT cascade + the self-converge `missing-skill` expectation (the documented first-appearance path, non-fatal to the artifact write) |
| workflow | [config-tables.md](workflow/config-tables.md) | Adding a [table] to perk.toml, choosing where a knob is consumed (TS interior gate vs init→settings.json convergence), the committed-only knob recipe (three precedents, the overlay-ignored test), the local-only secret-fallback reader (`perk.local.toml`, fail-soft on TOMLDecodeError, NOT in the merged Config, read from the **main checkout** via `main_worktree_root`), the `[worktree] setup` overlay-aware wholesale-replace key, change-scoped CI gating (the `[[ci]]` basename glob convention, skip-result shape, run-all-only discipline), or a config value silently vanishing (string-only / bool-is-int) |
| workflow | [cli-command-groups.md](workflow/cli-command-groups.md) | Adding/folding a `perk` CLI command group (the §8.1 group-dir template), wrapping an upstream CLI as a pass-through-first noun-group (the `perk skills` group + the sanctioned-subprocess guard + the test-all-rollback-arms rule), resolving a stage-launcher/group name collision (hybrid default-dispatch), the enacted taxonomy arc (the dormant-substrate discipline + `MergedCommand`/per-object-marker substrate, two-edits-to-retire-a-flat-launcher, flat-alias-routes-before-STAGE_LAUNCHERS, launcher-only vs merged — `pr ready` is W, pure-relocation ≠ merge, the parallel-node union-conflict file set, the cold-door-argv grep-all-tests, the worker-object-invocation test rule, the SSOT-corrects-parent-propagates-to-three-surfaces pattern), the sectioned root `--help` taxonomy (`SectionedAliasGroup` is-an-`AliasGroup`-not-`SectionedGroup`, generated vs dedicated launcher help, two-paragraph short/long split), per-group `fail()` byte-compat across folds (incl. the mixed EXIT_FOR_TYPE naming reality), a structural CLI refactor's parity smoke + test patterns, or the `objective doctor` worker exit codes (report-vs-abort: detected drift is still a clean 0) |
| workflow | [cold-door-client.md](workflow/cold-door-client.md) | Adding a warm door that shells to a `--json` cold door (rollout complete — the substrate is mandatory), writing a cold-door envelope decode (strict vs advisory vs fully-lenient vs derive-don't-decode; the bad_output reachability acceptance test), consuming a fail-arm payload, the fixture-sweep rule when strictening a decode (plus the merge-race fixture sweep after any cross-plane shape change), cold/warm version-skew hardening (the substring-preserving reword), the parent-posts warm-tool recipe (`post_pr_review` — strict whole-batch decode + soft Result + appendWorkflowState), a parity-only field on a strict cross-plane decoder (must be lenient), the legacy-label byte-compat lever, or door-test assertion churn after a migration |
| workflow | [github-gateway.md](workflow/github-gateway.md) | Touching `perk/github/`, consolidating repeated subprocess/parse idioms (the parse-helper family + boundary rules), debugging a phantom-`None` GitHub lookup (the not-found fold), adding a REST/GraphQL call (the gh-GraphQL transport facts — no `{owner}/{repo}` templating, cursor pagination, the GraphQL not-found shape that broke the shared `_is_not_found` fold), the non-default-base autoclose strand (`Closes #N` fires only on a default-branch merge — the `base_ref` surfacing + explicit non-default-base close), or designing a mutation-posting policy (the verdict-driven /pr-review split — now reshaped single→parallel parent-posts with headless-drivability unlocked, asymmetric failure ladders, structurally-unpostable fields) |
| workflow | [issue-backend.md](workflow/issue-backend.md) | Touching perk/backends/issue_backend.py or perk/backends/issues.py, an issue-tier consumer, adding a backend, the backend_id stamp discipline, opaque backend-owned ids (the validator relaxation — the backend is the authority on junk — and the sweep-all-id-consumers rule), per-backend land closure (branch on backend_id, never id shape), growing a read contract across two conformance-checked protocols at once (the 7-site census), the github-native-rows→adapter mapping + the import-direction docstring trap, `read_issue` as the third issue-read shape, the doctor issues check (collapsed-error arm-mapping), required-kwarg-first caller census, the late-bound GitHub adapter, two-shape fixtures, or fighting the boundary/import-direction tests |
| workflow | [objective-store.md](workflow/objective-store.md) | Touching perk/backends/objective_store.py or perk/backends/objective_stores.py, the dormant-contract → atomic-removal CI-green recipe (removal inseparable from rewire), the facade refactor splitting a tier off a substrate-heavy backend, the translate-CM, the resolver single-sourced off `[issues]`, the `backend_id` import-cycle literal, the `close_issue`-vs-`close_objective` tier split (delegation transparency + the guard asymmetry), the node↔plan unification protocol (`None`-means-doesn't-unify, the self-referential land-time backlink, node-issue-title ≠ plan-title), the Protocol-method-count growth / three-implementers conformance rule, the `add_objective_node` re-render-vs-materialize split, the objective-keyed engagement reads + the node-keyed sibling, the adoption Protocol growth (`read_objective_source`/`adopt_source_as_objective`), the no-op-return (`save_node_plan`/`post_status_update`/drift) family, or the manifest + drift-detection/repair design (the structural-identity manifest, the pure offline engine split, the add-node-vs-reconcile authority precedence, the deferred-edge sweep) |
| workflow | [linear-backend.md](workflow/linear-backend.md) | Touching `perk/backends/linear.py` / `perk/backends/linear_backend.py`, Linear GraphQL queries (auth header, RATELIMITED-as-400, errors-first parse), dual-encoding metadata markers (transcoder byte-identity, the presence-check bug class), Linear readiness in init/doctor, the env-first/config-fallback `client_from_env` seam + the corrected worktree key bridge (the gitignored `perk.local.toml` read from the **main checkout** via `main_worktree_root`, not env-seed-only — #730; the widening-broke-21-fakes `*a,**k` rule), the Node-3.1 GraphQL type-literacy consolidation (zero `cast(` in the package, substrate-home `_opt_*`/`_require_*` vs the `_helpers.py` payload mapping, the single `TypedDict` pilot — #731), backend-aware prompt rendering, agent-session emission (internally-gated emitters, fail-open side-channel testing), the stateful FakeLinearWorkspace lifecycle fake (named operations, read-seeding, UUIDs-only-on-mutations), the live-smoke results (Modes 1 & 2 + the Projects spike ran green), the paired `INPUT_ERROR`+"entity not found" not-found discriminator (`_is_entity_not_found`), the `[issues] team` KEY-not-name gotcha, or the now-BUILT Projects substrate / `LinearProjectObjectiveStore` (re-homing onto `LinearClient`, the substrate-home principle, the `_FakeLinear`/`FakeLinearWorkspace` substring-ordering footguns), the Phase-4 project-objective ops (the `add_objective_node` flow + UNREACHABLE id-collision branch, the phase→milestone seam routed empty-`known` for byte-equivalence, fail-open Project Updates, the manifest-drift design, `check_project_readiness`), or the third backend-aware seam (`objective_read_instruction` + the cold-vs-warm backend-source asymmetry), the `uuid_for` collapse now **delivered** (module-level mutation wrapper over sibling op classes), the Mode-4 Project ops now **live-proven**, or `attach_issue_to_milestone` + the fresh-manifest inline-code splice mechanics, the idiomatic-backend work (attribution at the create bottleneck, workspace-scoped labels + the four→five ripple, fail-open attachments, the prose-first reorder), the byte-stable sibling selection + `_is_entity_not_found`, or the `_FakeLinear` insertion-order substring footgun |
| workflow | [session-data.md](workflow/session-data.md) | Working on run_id minting/claiming (warm ULID mint, two-resolver doctrine), `extension/substrate/sessionData.ts` / `perk/state/cache.py` data-dir accessors, session-artifact provenance pointers (digest convention, LWW map appends, refusal tiers, two-digests-two-roles — integrity vs cache invalidation), adding a session-data consumer (the full producer→consumer recipe, incl. the read-only carve-out 2-instance template and its JSON-artifact variant), deciding whether to store a derived flag, or `perk state prune` / the `cache-gc` doctor check (the destructive-op triad) |
| workflow | [source-scan-guards.md](workflow/source-scan-guards.md) | Adding/extending a source-scan guard test (call-site or string-literal confinement — surfacesGuard, the session-data path guards), a guard fires and you're deciding whether to allowlist, or bringing a new `ctx.ui.*` method under governance (the leading-dot call-site pattern skips the seam's own structural declaration) |
| workflow | [doc-reconciliation.md](workflow/doc-reconciliation.md) | Reconciling a guidelines/design doc against grown reality, citing landed PRs from objective roadmaps (the `pr` field ≠ merge PR trap), deciding whether to delete never-adopted forward guidance, or reconciling objective roadmap prose (the hard-count drift-magnet, the prior-node-paragraph gap, the scope-attribution drift, the Anchors-region drift magnet, the landing-narrative PR-number convention, reconciling a node landed with its original planned description, the landing-log single-entry-append recipe — extract-region + difflib-prove-only-additions) |
| workflow | [borrowed-packages.md](workflow/borrowed-packages.md) | Adding/removing a borrowed Pi package (the `BORROWED_PACKAGES` lockstep-surfaces recipe), vetting a candidate for singleton UI slots (the setFooter clobber, `--no-ignore` greps), the thrice-affirmed retirement recipe, allowlisting a borrowed package's tools in read-only mode, or deciding between a provider seam and a plain borrow |
| workflow | [cold-door-launch.md](workflow/cold-door-launch.md) | Touching launch_stage argv/--approve trust injection, injecting child env vars at the launch seam (the merge-order setdefault layering, incl. the Linear-key env-seed before chdir), running a repo-configured `[worktree] setup` hook before exec (`run_worktree_setup` single canonical path; remote `position_worktree` skips it), the worktree `.agents/skills/` mirror (`materialize_skills` at positioning), wrapping a last-wins CLI, composing/testing a Python surface that nests a machine_output command, CliRunner stdout/stderr asserts on Click ≥8.2 (`.output` is a combined stream — compare `.stdout`/`.stderr`), or refactoring launch/run behind byte-exact pins (cache.py import-leaf, dataclasses.replace transitions) |
| workflow | [worktree-lifecycle.md](workflow/worktree-lifecycle.md) | Writing a worktree-batch CLI, matching git worktree paths (the .resolve() trap, incl. CliRunner payload assertions on macOS), split --force semantics, the `[worktree] setup` hook + the `created`-flag real-run-vs-dry-run asymmetry (re-derive `stage.worktree=="create" and not path.exists()`), the `main_worktree_root` primitive (`--git-common-dir`+`.parent`, both return forms, the macOS `.resolve()`-both-sides gotcha), or a worktree test that is unexpectedly dirty |
| workflow | [mergeability-and-conflict-resolution.md](workflow/mergeability-and-conflict-resolution.md) | Touching the `/submit` merge-tree conflict probe (`perk/substrate/git.py`, exit-code-is-the-verdict, `MergeProbe.mergeable`), the carry-the-verdict-never-re-derive principle, the warm reactive bounded `driveConflictResolution`, the worker `mergeable !== false` bar, the first write-capable context-inheriting `conflict-resolver` subagent, or a PR-mergeability / force-push stale-`headRefOid` gotcha |
| workflow | [provider-seam.md](workflow/provider-seam.md) | Working on a provider seam (now five: plan/todo/askuser/footer/web) — classifying a proposed seam **artifact vs interface** (decides whether you write an adapter at all), the vacate-only `askuser`/`footer`/`web` interface seams, the three vacating mechanisms + the **install-site/runtime vacating keyed off `ctx.cwd`** (footer, no `process.chdir` test tier) and **"nothing to vacate at all"** (web) postures, the **first non-null-`package` default** (web — the `.startswith`/`in packages` string-membership test trap), the collision-kind decision rule (registration-time vacating vs runtime deferral), the **2→N widening census** (incl. the `GOOD`-fixture-must-gain-each-seam-default rule), **widening the footer catalog** with a new declarative provider (`pi-status-footer` / `pi-default` — one catalog row + one TS id constant, zero convergence-code change; `pi-default` the first null-package *non-default*) and the `pi-status` **no-extension-status counterexample** to "both foreign footers bridge automatically", the borrow-ban-vs-selected-footer-provider reconciliation, the selection substrate, deferring perk's own authoring surface under a foreign selection, the cross-plane resolver, wiring a foreign plan/todo adapter, gating adapter behavior on a foreign package's persisted state (the state-twin read), injected prompts under foreign tool restrictions (the tools-hidden branch), or an augment-posture provider (the plannotator bridge + persisted-mode gate read) |
| workflow | [remote-runner.md](workflow/remote-runner.md) | Working on remote execution (runner, work_worker, GHA workflows), cancel/retry supervisor controls, GHA rerun reuse, universal zero-spend smoke short-circuit, fail-soft orchestrator overlays vs local records truth, or subprocess monkeypatching traps |
| pi | [context-system.md](pi/context-system.md) | Surfacing information to a plan session, building a factory, debugging why a bash command is blocked, or extending the read-only bash allowlist (incl. the subcommand-shaped gh entries and the command-keyed, language-agnostic `ast-grep` entry — perk cannot own the `grep` builtin), or the **five-surface lockstep** for allowlisting a read-only bash gate command (SAFE_PATTERNS + paired test lists + contracts.md + user-docs two notes + this doc) |
| pi | [extension-api.md](pi/extension-api.md) | Live system-prompt inputs in extensions, registerTool execute results details requirement, custom tool read-only gating traps, the `session_compact` event (first-class SDK event; type-only harness union) and the stale-ctx compaction-race silent swallow, tsconfig strict-mode index access in tests, command vs event handlers, injected-message persistence, `ctx.ui.editor` facts (no AbortSignal; title-borne key hints), testing `pi.events`-bridge logic / flag-shortcut non-registration from the harness, asserting a `pi.sendUserMessage` injection offline (the spy is mandatory for seed-turn `invokeCommand` tests), the `headfulUIContext` select/input/editor gap, registration-time cwd config reads (host-repo-sensitive harness tests), the `PERK_RUN_ID` harness leak, or vendored-extension test/infra (single `node --test <file>`, the offline registration smoke, the extracted-core + seeded-random pattern) |
| pi | [extension-seams.md](pi/extension-seams.md) | Collapsing a repeated context-dependent extension idiom into one tested seam (the `report()`/`branchOf`/`EntrySink` minimal-structural-interface recipe — incl. narrowing plan-named SDK types to structural slices and typing an extracted core's injected dependency as a one-method slice), the type-only-import cycle break, the strict workflow-state append + read-back tiering (incl. object-valued custom `equals` / identity-subset comparators), the seam-owned-prefix trap, the report()-rollout gotchas (name shadowing, failFor-only failures, banner-fragile notify asserts), or the P1/P2/P3 site triage for what a single-message seam can absorb |
| pi | [tui-surfaces.md](pi/tui-surfaces.md) | Touching extension/surfaces/surfaces.ts or any perk-rendered TUI surface (footer, widgets, the composed perk status slot), the RPC dual-publish law, setFooter adoption facts (incl. the last-wins footer-clobber hazard), windower/budget patterns, harness recipes for factory widgets/commands, width-sweep invariant testing, or vendoring a pi-tui-touching extension (the dual-copy CLASS type clash, the `setWorkingMessage` governed-but-permitted seam, the `/btw` charter bounded exception) |
| pi | [tool-param-decode.md](pi/tool-param-decode.md) | Decoding registered-tool params (the tri-state `toolParams.ts` seam), adding a tool handler, choosing strict vs lenient decode semantics at a boundary, making a required param optional behind a fallback chain (the absent→`undefined` flip), backend-agnostic id params (`idParam`/`idArrayParam`), or testing decode-before-side-effect ordering |
| pi | [context-injection.md](pi/context-injection.md) | Injecting context into a session (planMode/objectiveAuthor/bindings) and stripping it later, deduplicating a once-only injection (the stringify-includes branch scan), serving two stages from one adapter (two flavors, one customType), or disambiguating two stages that share a read-only mode |
| pi | [structured-output.md](pi/structured-output.md) | Getting typed/structured output from a model in an extension, gating a model call offline in tests (PERK_NO_LLM is the only gate — auth resolution succeeds keyless), or writing offline tests for provider-calling code (top-level vs nested faux registration by caller) |
| pi | [headless-session-drive.md](pi/headless-session-drive.md) | Constructing or driving a headless (non-TUI) Pi session via the SDK — the runtime-factory path, bindExtensions, session.subscribe event facts, a single-prompt drive + budget watchdog, the structured run-event stream, offline model-availability determinism, or driving the real runtime with a faux model (the nested-pi-ai per-instance registry trap) |
| pi | [subagents.md](pi/subagents.md) | Spawning subagents for fresh-context work, the newest `conflict-resolver` agent (now four perk agents; first write-capable + context-inheriting), the widening-lockstep census, agent-def delivery to consumer repos (frontmatter-derived runtime name, installed-packages-never-scanned, the committed managed convergence, no worktree mirror), **editing/reconverging a perk agent's prose rubric** (the `agents/*.md` SSOT → `.pi/agents/perk/` reconverge ritual, the stale-path gotcha, the no-test-asserts-prose-body fact), **the judgment-agent prompt anti-pattern** (default verdicts + decide-conclusion-first bias → enumerate-then-derive + an "earned, not defaulted" balance statement, binary posting bar unchanged), flat agent/seam-keyed config, TS TOML backslash continuation restriction, cross-plane model/prompt literal parity, exported guidance unit-testing, child-posts-own-mutation vs parent-mutates (the report-only reviewer drops `write`, angle per-call — read-only fan-out, not the posting agent), or the isolated-`createAgentSession` read-only-gate bypass (thread tool-gating in + fold it into the cache key) |
| toolchain | [ruff.md](toolchain/ruff.md) | Ruff check vs format split, silent-failure commit-reformat traps (incl. run_ci green ≠ committable — the hook can reformat after CI passed), RUF100 unused noqa rules, RUF043 regex metacharacters in pytest.raises match patterns, UP047 (PEP-695 inline generic vs `TypeVar(bound=...)` on a bound-type return), template string E501 multiline limits, SIM105 exception suppressions, RUF022 unsorted `__all__` (insert new re-exports in isort-alphabetical position in both the import list and `__all__`), or preview-rule enablement dragging cross-bucket fixes forward |
| toolchain | [biome.md](toolchain/biome.md) | Hitting a Biome lint or tsc error in the extension (incl. TS parameter-property stripping under `node --test`, the `organizeImports` assist-only-under-`check`, the `let x = undefined` trap, forEach expression-body assertions, `noControlCharactersInRegex` — the single-pass alternation fix), editing prose inside a template literal before a `--write` run (the backtick-mangling trap), auditing a `--write` pass after an import-path sweep, a CI lint iteration on TS formatting (new files: `biome check --write` first), or vendored upstream code tripping `noUncheckedIndexedAccess` (the `?? ""` / `?? messages[0] ?? "fallback"` / guard-before-discriminant patterns) |
| toolchain | [ts-module-moves.md](toolchain/ts-module-moves.md) | Moving extension TS modules into a subdirectory (the extension-layout tranches — the two-commit mv+sweep recipe, incl. multi-tranche moves), auditing a path-rewrite sweep (the two-mode pre-move import-resolution rule), or a justfile/Node test glob dropping nested tests |
| toolchain | [python-package-splits.md](toolchain/python-package-splits.md) | Splitting a large `perk/<mod>.py` into a `perk/<mod>/` package (preserving import paths) — the script-generated slice/rewrite/census recipe, the objective-#714 split-arc refinements (seed-generous import-resolution loop, monkeypatch-name-resolution contract + public-name blind spot, module-object-vs-name-binding patches, constant-travels-with-helper, E402 leaf data module, `(stem,func)`-keyed guard ripple, `__file__`→package-dir-glob source-scan-guard fix, the AST byte-identity recipe, long-method call-sequence preservation), or the package-`__init__` submodule-import fallback / sorted-`__all__` F401 / hatchling auto-include facts |
| toolchain | [worktree-node-modules.md](toolchain/worktree-node-modules.md) | CI failing in files your diff never touched, a fresh worktree failing `tsc`/`node --test` before `npm ci`, a pinned Pi/SDK version bump in a worktree seeming to do nothing, a `shared/` change not reflected when smoked via the stale global `perk` (and the worktree self-converge trap), a test run exiting -1/143 with no FAILED line (a kill, not a failure), proving a failure pre-dates your diff (the stash-diagnostic), or a delete/edit rebase conflict on lines your branch moved |
| toolchain | [ty.md](toolchain/ty.md) | Hitting a ty invalid-argument-type / no-matching-overload error narrowing untyped or object-form values parsed from JSON/settings (incl. the narrowing-helper family for deep GraphQL payloads and the heterogeneous list/dict literal → explicit-local-annotation gotcha), reading a known key off an `object`-narrowed dict without a `cast` (the `.items()`-iteration idiom), per-element `isinstance` not refining the surrounding container (typed-list accumulation), the lenient `_opt_*` twin of `_require_*` + the disposition-matching rule, suppressing a ty diagnostic (`# ty: ignore[rule]`, not mypy syntax), or ty rejecting a string literal where an enum is annotated |
| toolchain | [test-parallelism.md](toolchain/test-parallelism.md) | Making `just test` / `just ci` faster — pytest-xdist-by-default (`addopts -n auto --dist loadgroup`, the `-n0` serial escape hatch, the build-once shared-`xdist_group` idiom), or splitting a harness-heavy `node:test` file (cross-file-only parallelism, intra-file concurrency UNSAFE, the noUnusedLocals/tsc trim loop, the count-preservation proof) |
