---
title: docs/learned catalog
read_when: You need to find a durable learned doc, or you are authoring/refreshing one via /learn-docs.
---

# `docs/learned/` — the durable learnings catalog

This is the **full catalog** of perk's learned docs: token caches of cross-cutting reasoning for
future AI agents, consolidated from terminal `perk:learn` issues by the `/learn-docs` plan factory
(see the `perk-learn-docs` skill). Each doc lives under `docs/learned/<category>/` with light
frontmatter (`title` + `read_when`).

A **compressed routing index** of these docs is kept ambient in `.pi/APPEND_SYSTEM.md` (appended to
every session's system prompt); this file is the on-demand full listing that ambient index points
into. Both are maintained by `/learn-docs` plans — never by `perk init`.

## Documents

| Category | Document | Read when |
|---|---|---|
| workflow | [plan-factories.md](workflow/plan-factories.md) | Building or debugging a perk plan factory (learn-docs, objective-plan, any on-demand factory launching a read-only session) |
| workflow | [plan-ref-lifecycle.md](workflow/plan-ref-lifecycle.md) | Debugging plan-ref linkage, adding a worktree stage, extending PlanRef schema, or implementing on-land secondary bookkeeping |
| workflow | [plan-save-surfaces.md](workflow/plan-save-surfaces.md) | Working on plan-save / objective-node linkage, debugging a dropped objective_id / consumed_learn (the historical fidelity-gap incident — now closed by the two recovery carriers), adding context that must survive a model's save-surface choice, the resolvePlanSource artifact→param→transcript chain (param-path message stays byte-stable; the stale-draft hazard), or the warm objective_node_claim recovery carrier |
| workflow | [plan-review-flow.md](workflow/plan-review-flow.md) | Working on plan_review / a review backend (plannotator, first-party, tombell), the approvalSave seam (gate-exit ownership), the asymmetric review-surface tiering (artifact→param only), wiring a review surface's APPROVED/DENIED arms (the approved-first routing split), flavoring the shared first-party core for a second subject, changing factory/authoring guidance prose (the three-tier prose mirror), the reviewOutcomeResult/SKIP_TEXT/guard-ordering footguns, or testing dialog flows via the extracted core + scripted UI fake |
| workflow | [objective-lifecycle.md](workflow/objective-lifecycle.md) | Working on objective node status transitions, objective-plan selection (pending-first over live claims, `resumable_claims` surfacing, the accepted backlink race — a `planning`-stuck node with `pr: null` after merge is its known signature), the authoring/save loop (the review-first objective-draft JSON artifact, render-at-the-door, the draft-module-leaf rule), the deterministic `perk objective run` supervisor loop, or a node stuck in planning |
| workflow | [warm-door-commands.md](workflow/warm-door-commands.md) | Building or fixing a warm perk slash-command (/plan-save, /objective-save, /address, /learn-docs), debugging a command that dead-ends or false-succeeds, or wiring how a warm TS door renders a cold Python door's structured sub-result |
| workflow | [skill-bindings.md](workflow/skill-bindings.md) | Working on skill-binding config, the cold/warm delivery doors, the resolver, the linked-worktree skill mirror (delivery depends on the cold door mirroring `.agents/skills/`), doctor validation of bindings + the injection-presence mirror, skills `references:` frontmatter + subdirectory routing (zero init/doctor wiring), or debugging double-delivered / missing binding context |
| workflow | [shared-contracts.md](workflow/shared-contracts.md) | Adding parsed shared/ data files, registry stages, changing a stage's registry `writes` (the two-pytest-pins gotcha), prose contracts maintenance (§-numbering grep), cross-plane byte-parity prompt fragments (SSOT helper + lockstep substrings), wording-pinning content-parallel prompts in both suites, registry vocabulary deferral (keys land with their first declaring stage), post-merge objective roadmap reconciliation, or tracing shared contract ripples |
| workflow | [init-doctor.md](workflow/init-doctor.md) | Adding a managed piece (doctor checks), choosing committed-tracked delivery vs a cold-door worktree symlink mirror (the prune-the-owned-subdir rule), adding a transient file to gitignore, doctor migration, templates reconvergence, Click bottom-of-file imports and register_with_aliases arity, doctor-disk vs selfcheck-prompt, the GROUP_ORDER render trap, fail-level baseline shifts, fix_errors as first-class report data, network repairs via the verify-gated gesture (never a ManagedConvergence), the §8.5/§8.6 machine-surface co-owners, seam-signature ripple, or report-only pre-flights |
| workflow | [init-external-cli.md](workflow/init-external-cli.md) | Making perk init shell out to an external CLI, choosing a failure posture (best-effort vs load-bearing two-tier loud failure — the skills posture flip), declaring a committed manifest fragment, pinning a self-repo vs consumer ref, or scoping a cross-repo plan to the perk slice |
| workflow | [config-tables.md](workflow/config-tables.md) | Adding a [table] to perk.toml, choosing where a knob is consumed (TS interior gate vs init→settings.json convergence), the committed-only knob recipe (three precedents, the overlay-ignored test), change-scoped CI gating (the `[[ci]]` basename glob convention, skip-result shape, run-all-only discipline), or a config value silently vanishing (string-only / bool-is-int) |
| workflow | [cli-command-groups.md](workflow/cli-command-groups.md) | Adding/folding a `perk` CLI command group (the §8.1 group-dir template), resolving a stage-launcher/group name collision (hybrid default-dispatch), the enacted taxonomy arc (the dormant-substrate discipline + `MergedCommand`/per-object-marker substrate, two-edits-to-retire-a-flat-launcher, flat-alias-routes-before-STAGE_LAUNCHERS, launcher-only vs merged — `pr ready` is W, pure-relocation ≠ merge, the parallel-node union-conflict file set, the cold-door-argv grep-all-tests, the worker-object-invocation test rule, the SSOT-corrects-parent-propagates-to-three-surfaces pattern), the sectioned root `--help` taxonomy (`SectionedAliasGroup` is-an-`AliasGroup`-not-`SectionedGroup`, generated vs dedicated launcher help, two-paragraph short/long split), per-group `fail()` byte-compat across folds (incl. the mixed EXIT_FOR_TYPE naming reality), or a structural CLI refactor's parity smoke + test patterns |
| workflow | [cold-door-client.md](workflow/cold-door-client.md) | Adding a warm door that shells to a `--json` cold door (rollout complete — the substrate is mandatory), writing a cold-door envelope decode (strict vs advisory vs fully-lenient vs derive-don't-decode; the bad_output reachability acceptance test), consuming a fail-arm payload, the fixture-sweep rule when strictening a decode (plus the merge-race fixture sweep after any cross-plane shape change), cold/warm version-skew hardening (the substring-preserving reword), the legacy-label byte-compat lever, or door-test assertion churn after a migration |
| workflow | [github-gateway.md](workflow/github-gateway.md) | Touching `perk/github/`, consolidating repeated subprocess/parse idioms (the parse-helper family + boundary rules), debugging a phantom-`None` GitHub lookup (the not-found fold), adding a REST/GraphQL call, or designing a mutation-posting policy (the verdict-driven /pr-review split, asymmetric failure ladders, structurally-unpostable fields) |
| workflow | [issue-backend.md](workflow/issue-backend.md) | Touching perk/backends/issue_backend.py or perk/backends/issues.py, an issue-tier consumer, adding a backend, the backend_id stamp discipline, opaque backend-owned ids (the validator relaxation — the backend is the authority on junk — and the sweep-all-id-consumers rule), per-backend land closure (branch on backend_id, never id shape), the doctor issues check (collapsed-error arm-mapping), required-kwarg-first caller census, the late-bound GitHub adapter, two-shape fixtures, or fighting the boundary/import-direction tests |
| workflow | [linear-backend.md](workflow/linear-backend.md) | Touching `perk/backends/linear.py` / `perk/backends/linear_backend.py`, Linear GraphQL queries (auth header, RATELIMITED-as-400, errors-first parse), dual-encoding metadata markers (transcoder byte-identity, the presence-check bug class), Linear readiness in init/doctor, backend-aware prompt rendering, agent-session emission (internally-gated emitters, fail-open side-channel testing), the stateful FakeLinearWorkspace lifecycle fake (named operations, read-seeding, UUIDs-only-on-mutations), or the live-smoke deferral register (still unrun) |
| workflow | [session-data.md](workflow/session-data.md) | Working on run_id minting/claiming (warm ULID mint, two-resolver doctrine), `extension/substrate/sessionData.ts` / `perk/state/cache.py` data-dir accessors, session-artifact provenance pointers (digest convention, LWW map appends, refusal tiers, two-digests-two-roles — integrity vs cache invalidation), adding a session-data consumer (the full producer→consumer recipe, incl. the read-only carve-out 2-instance template and its JSON-artifact variant), deciding whether to store a derived flag, or `perk state prune` / the `cache-gc` doctor check (the destructive-op triad) |
| workflow | [source-scan-guards.md](workflow/source-scan-guards.md) | Adding/extending a source-scan guard test (call-site or string-literal confinement — surfacesGuard, the session-data path guards), or a guard fires and you're deciding whether to allowlist |
| workflow | [doc-reconciliation.md](workflow/doc-reconciliation.md) | Reconciling a guidelines/design doc against grown reality, citing landed PRs from objective roadmaps (the `pr` field ≠ merge PR trap), or deciding whether to delete never-adopted forward guidance |
| workflow | [borrowed-packages.md](workflow/borrowed-packages.md) | Adding/removing a borrowed Pi package (the `BORROWED_PACKAGES` lockstep-surfaces recipe), vetting a candidate for singleton UI slots (the setFooter clobber, `--no-ignore` greps), the thrice-affirmed retirement recipe, allowlisting a borrowed package's tools in read-only mode, or deciding between a provider seam and a plain borrow |
| workflow | [cold-door-launch.md](workflow/cold-door-launch.md) | Touching launch_stage argv/--approve trust injection, injecting child env vars at the launch seam (the merge-order setdefault layering), the worktree `.agents/skills/` mirror (`materialize_skills` at positioning), wrapping a last-wins CLI, composing/testing a Python surface that nests a machine_output command, CliRunner stdout/stderr asserts on Click ≥8.2 (`.output` is a combined stream — compare `.stdout`/`.stderr`), or refactoring launch/run behind byte-exact pins (cache.py import-leaf, dataclasses.replace transitions) |
| workflow | [worktree-lifecycle.md](workflow/worktree-lifecycle.md) | Writing a worktree-batch CLI, matching git worktree paths (the .resolve() trap, incl. CliRunner payload assertions on macOS), split --force semantics, or a worktree test that is unexpectedly dirty |
| workflow | [provider-seam.md](workflow/provider-seam.md) | Working on a provider seam (now three: plan/todo/askuser) — classifying a proposed seam **artifact vs interface** (decides whether you write an adapter at all), the vacate-only `askuser` interface seam, the collision-kind decision rule (registration-time vacating vs runtime deferral), the 2→N widening census, the selection substrate, deferring perk's own authoring surface under a foreign selection, the cross-plane resolver, wiring a foreign plan/todo adapter, gating adapter behavior on a foreign package's persisted state (the state-twin read), injected prompts under foreign tool restrictions (the tools-hidden branch), or an augment-posture provider (the plannotator bridge + persisted-mode gate read) |
| workflow | [remote-runner.md](workflow/remote-runner.md) | Working on remote execution (runner, work_worker, GHA workflows), cancel/retry supervisor controls, GHA rerun reuse, universal zero-spend smoke short-circuit, fail-soft orchestrator overlays vs local records truth, or subprocess monkeypatching traps |
| pi | [context-system.md](pi/context-system.md) | Surfacing information to a plan session, building a factory, debugging why a bash command is blocked, or extending the read-only bash allowlist (incl. the subcommand-shaped gh entries) |
| pi | [extension-api.md](pi/extension-api.md) | Live system-prompt inputs in extensions, registerTool execute results details requirement, custom tool read-only gating traps, the `session_compact` event (first-class SDK event; type-only harness union) and the stale-ctx compaction-race silent swallow, tsconfig strict-mode index access in tests, command vs event handlers, injected-message persistence, `ctx.ui.editor` facts (no AbortSignal; title-borne key hints), testing `pi.events`-bridge logic / flag-shortcut non-registration from the harness, asserting a `pi.sendUserMessage` injection offline (the spy is mandatory for seed-turn `invokeCommand` tests), the `headfulUIContext` select/input/editor gap, registration-time cwd config reads (host-repo-sensitive harness tests), or the `PERK_RUN_ID` harness leak |
| pi | [extension-seams.md](pi/extension-seams.md) | Collapsing a repeated context-dependent extension idiom into one tested seam (the `report()`/`branchOf`/`EntrySink` minimal-structural-interface recipe — incl. narrowing plan-named SDK types to structural slices and typing an extracted core's injected dependency as a one-method slice), the type-only-import cycle break, the strict workflow-state append + read-back tiering (incl. object-valued custom `equals` / identity-subset comparators), the seam-owned-prefix trap, the report()-rollout gotchas (name shadowing, failFor-only failures, banner-fragile notify asserts), or the P1/P2/P3 site triage for what a single-message seam can absorb |
| pi | [tui-surfaces.md](pi/tui-surfaces.md) | Touching extension/surfaces/surfaces.ts or any perk-rendered TUI surface (footer, widgets, the composed perk status slot), the RPC dual-publish law, setFooter adoption facts (incl. the last-wins footer-clobber hazard), windower/budget patterns, harness recipes for factory widgets/commands, or width-sweep invariant testing |
| pi | [tool-param-decode.md](pi/tool-param-decode.md) | Decoding registered-tool params (the tri-state `toolParams.ts` seam), adding a tool handler, choosing strict vs lenient decode semantics at a boundary, making a required param optional behind a fallback chain (the absent→`undefined` flip), backend-agnostic id params (`idParam`/`idArrayParam`), or testing decode-before-side-effect ordering |
| pi | [context-injection.md](pi/context-injection.md) | Injecting context into a session (planMode/objectiveAuthor/bindings) and stripping it later, deduplicating a once-only injection (the stringify-includes branch scan), serving two stages from one adapter (two flavors, one customType), or disambiguating two stages that share a read-only mode |
| pi | [structured-output.md](pi/structured-output.md) | Getting typed/structured output from a model in an extension, gating a model call offline in tests (PERK_NO_LLM is the only gate — auth resolution succeeds keyless), or writing offline tests for provider-calling code (top-level vs nested faux registration by caller) |
| pi | [headless-session-drive.md](pi/headless-session-drive.md) | Constructing or driving a headless (non-TUI) Pi session via the SDK — the runtime-factory path, bindExtensions, session.subscribe event facts, a single-prompt drive + budget watchdog, the structured run-event stream, offline model-availability determinism, or driving the real runtime with a faux model (the nested-pi-ai per-instance registry trap) |
| pi | [subagents.md](pi/subagents.md) | Spawning subagents for fresh-context work, agent-def delivery to consumer repos (frontmatter-derived runtime name, installed-packages-never-scanned, the committed managed convergence, no worktree mirror), flat agent/seam-keyed config, TS TOML backslash continuation restriction, cross-plane model/prompt literal parity, exported guidance unit-testing, or child-posts-own-mutation vs parent-mutates |
| toolchain | [ruff.md](toolchain/ruff.md) | Ruff check vs format split, silent-failure commit-reformat traps (incl. run_ci green ≠ committable — the hook can reformat after CI passed), RUF100 unused noqa rules, RUF043 regex metacharacters in pytest.raises match patterns, UP047 (PEP-695 inline generic vs `TypeVar(bound=...)` on a bound-type return), template string E501 multiline limits, SIM105 exception suppressions, or preview-rule enablement dragging cross-bucket fixes forward |
| toolchain | [biome.md](toolchain/biome.md) | Hitting a Biome lint or tsc error in the extension (incl. TS parameter-property stripping under `node --test`, the `organizeImports` assist-only-under-`check`, the `let x = undefined` trap, forEach expression-body assertions, `noControlCharactersInRegex` — the single-pass alternation fix), editing prose inside a template literal before a `--write` run (the backtick-mangling trap), auditing a `--write` pass after an import-path sweep, or a CI lint iteration on TS formatting (new files: `biome check --write` first) |
| toolchain | [ts-module-moves.md](toolchain/ts-module-moves.md) | Moving extension TS modules into a subdirectory (the extension-layout tranches — the two-commit mv+sweep recipe, incl. multi-tranche moves), auditing a path-rewrite sweep (the two-mode pre-move import-resolution rule), or a justfile/Node test glob dropping nested tests |
| toolchain | [python-package-splits.md](toolchain/python-package-splits.md) | Splitting a large `perk/<mod>.py` into a `perk/<mod>/` package (preserving import paths) — the script-generated slice/rewrite/census recipe, or the package-`__init__` submodule-import fallback / sorted-`__all__` F401 / hatchling auto-include facts |
| toolchain | [worktree-node-modules.md](toolchain/worktree-node-modules.md) | CI failing in files your diff never touched, a fresh worktree failing `tsc`/`node --test` before `npm ci`, a pinned Pi/SDK version bump in a worktree seeming to do nothing, a `shared/` change not reflected when smoked via the stale global `perk` (and the worktree self-converge trap), a test run exiting -1/143 with no FAILED line (a kill, not a failure), proving a failure pre-dates your diff (the stash-diagnostic), or a delete/edit rebase conflict on lines your branch moved |
| toolchain | [ty.md](toolchain/ty.md) | Hitting a ty invalid-argument-type / no-matching-overload error narrowing untyped or object-form values parsed from JSON/settings (incl. the narrowing-helper family for deep GraphQL payloads), suppressing a ty diagnostic (`# ty: ignore[rule]`, not mypy syntax), or ty rejecting a string literal where an enum is annotated |
