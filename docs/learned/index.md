# Learned Index

This is the **compressed routing index** into `docs/learned/` — one line per doc, so an agent knows
*when* to read the full doc, without needing to read all of them up front. The full catalog sits
below, after the routing index.

## Compressed Routing Index (one cue per doc; read on demand)

- **workflow/** — plan factories (plane-specific link carriers, inbox-over-gh now a discipline, cold-injects/warm-instructs reusable pattern), human-engagement reads contract (§8.25 — decoupled-vocabulary pure leaf `engagement.py` imports neither tier; two keyings two seams — issue/objective-keyed vs node-keyed, never reuse across keying — if-subject-IS-an-issue reuse issue-keyed for free (#702); growing-the-contract = ty-static conformance ripple across ALL sites — census dormant store + fakes; empty/no-op conformer family + shared frozen `EMPTY_AGENT_SESSION`/`EMPTY_NODE_ENGAGEMENT`; author classifier heuristic over perk's OWN marker grammar — no committed app-actor id so sentinel-regex / `perk_bot_ids` empty forward seam / always-constructed-`Actor`-defeats-`unknown` → neutral mapper returns None; Linear sibling `_comments_with_authors` + `_is_entity_not_found`→empty / auth raises; GitHub both reads via gh-GraphQL not porcelain; renderer family untrusted-DATA bounded blocks — skip sentinel comments / edits labeled-never-filtered / ≤30 items ~1500-char / byte-stable extraction discipline; cold-injects-warm-instructs + seed-byte-unchanged-on-empty + per-consumer injection-placement; worker-side composition over Protocol growth), in-place adoption writers (3rd/4th in-place writer — stamp additively never mint a second; surface shape decided per-node asymmetrically — `plan from` VERB vs `objective author --from` FLAG; don't-borrow-a-mutator-that-touches-the-preserved-field — compose from primitives — GitHub one body-PATCH + comment upsert never title / Linear `save_node_plan`+additive labelIds union; `read_issue` third issue-read shape; additive labels GitHub-natively-additive vs Linear-REPLACES-read-union; adopt-mapping SEPARATE side-map via `parse_adopt_mapping` never on `ObjectiveNode` — TS `ROADMAP_PARAM_SCHEMA` gatekeeps / Python parse lenient; verbatim-preservation-differs-by-tier — plan in-place / objective archives original into immutable `Adopted-from` note; handoff-rides-the-link `_adopt_from_handoff` no-TS-tool-change + `--adopt-from` mutually-exclusive-with objective/node ids; `adopted_from` provenance self-referential; backend-discriminated OPEN refusal — github-only reaches issue-tier `.state` only when `backend_id=="github"`; Protocol-growth no-op family `read_objective_source`/`adopt_source_as_objective`→None + dry-run→None; Linear project-backed writer — `update_project_content` not create / sibling `project_issues_for_adoption` / two-pass node-then-relation ordering; "doctor note" can mean prose-only; offline-covered only; seed-from-file mode SIBLING — `plan from <file>`/`--from <file>` has no backend identity to stamp so omits `adopt_from` + skips `require_github` (gate auth at point-of-network-use), detection-before-parsing at door top, backend-free `seed_file.py` leaf, `isolated_filesystem()`-already-chdirs test gotcha), plan-ref lifecycle (cross-backend clobber hazard — an objective-linked `plan save` inside an active worktree hijacks the gitignored plan-ref → numeric-id `/submit` failure; reconstruct via the cache writer; non-default `base` resolve-once-then-pin — base(stored)≠--base(flag), three readers create_pr/resolve_base/submit-probe, `--worktree NAME` returned-field-as-scratch clobber, always-emitted-`to_data`-field sweep hits exact-dicts + ty conformance fakes), plan-save surfaces (fidelity gap & handoff_extra carrier, `resolvePlanSource` artifact→param→transcript chain — param-path message byte-stable & ignored-differing-param flag & the stale-draft hazard, the warm `objective_node_claim` recovery carrier — mirror new factory-threaded params on both planes, copyable command callout — server-assigned-id timing → 4 per-backend write shapes (post-create read+PATCH / fold-at-compose / post-create content update / fold-into-unified desc + idempotency guard), prepend-above-metadata durable by construction, portable Markdown no-transcode byte-equivalent, insert-call-into-create-seq breaks error-path fakes too), plan review flow (backend-neutral `plan_review` dispatch — plannotator bridge vs first-party `ctx.ui.editor` default, asymmetric review tiering — artifact→param ONLY never the transcript, `approvalSave` owns the gate exit — review backends compose never own, reviewOutcomeResult renders DENIED unless approved-first routing & SKIP_TEXT/guard-ordering footguns, approved-first split mirrored to objectives & optional-param flavoring of the shared first-party core & the three-tier prose mirror, extracted-core + scripted-UI-fake testing, `PLAN_AUTHORING_CONTEXT` consult-learnings nudge — `planContextContent(cwd)` appends `[workflow] plan_authoring` addendum / ONE seam reaches BOTH plan factories (bridge seeds separate, no byte-parity) / three-surface lockstep constant+`skills/perk-plan/SKILL.md`+additive `planMode.test.ts` assert / soft-nudge-over-gate), warm-door commands (read-only gating trap, drive-the-session, the warm gate-enter recipe — enter distributed/exit centralized, rendering every cold-door outcome, terminating-door drives next pass — `/land` auto-drives `/objective-reconcile`), objective lifecycle (resumable-lease nodes & authoring loop — JSON draft artifact & render-at-the-door & draft-module-leaf & the deterministic `perk objective run` supervisor — never-lands/never-plans, PR-existence implement-done signal, wait-then-refetch-whole-world, report-only budget; pending-first selection over live claims & `resumable_claims` coordination surface & accepted backlink race — `planning`+`pr:null` after merge is its known signature & JSON-mode≠JSON-payload & parallel-node rebase friction & the design-only-node pattern — node reframed build-X→author-design-doc, deliverable is a docs/planning design doc (no markdown linter → skips all hooks), status stays done, mandatory post-merge reconcile of BOTH prose & node description; reconcile-ready design-only AUDIT node #717 — write the findings→roadmap section as direct input to `/objective-reconcile` (land = refinement not re-plan), audit-a-green-codebase methodology = chase what ruff/ty CAN'T see (module depth/cohesion, type-literacy, declare-close-to-use, LBYL/EAFP, edge correctness), don't re-catalog enforced rules, honest P-severity (honest no-P1 over inflated)), config tables (own-section vs wholesale-consumed-map placement, string-only TS parser vs bool-is-int Python guard, committed-only vs overlaid read — the three-precedent committed-only recipe & the overlay-ignored test, interior-gate vs init→settings.json convergence, local-only secret-fallback reader (`perk.local.toml`, fail-soft on TOMLDecodeError, NOT in the merged Config — a third reader shape; env wins; reads `main_worktree_root(repo_root) or repo_root` = MAIN checkout #730 so the secret is found inside a linked worktree w/o file-copy or env-seed, `or repo_root` keeps non-worktree/tmp_path callers byte-identical) & `[worktree] setup` overlay-aware wholesale-replace key, change-scoped CI gating — `[[ci]]` basename glob convention (slash-free → basename at any depth, `/`-bearing → full path) & skip-result shape (extra `glob?` for prose only) & run-all-only discipline (compute changedFiles once, explicit `only` always runs, `changed===null` fail-open never skips) & the tool-path harness limit (prod piExec only → gate at runCiChecks), mirror the providers selection shape), cold-door pi-launch seam (build argv once before dry-run, --approve run-scoped trust on ephemeral worktrees, last-wins inject-before-passthrough, local stage execvpe never returns, capture nested machine_output in a --json surface, Click 8.4.1 CliRunner mix_stderr dropped → parse last line & Click ≥8.2 `.output` is a combined stream — compare `.stdout`/`.stderr` for byte-identity asserts, launch/run refactor posture — cache.py import-leaf & promote-in-place, dataclasses.replace transitions, byte-exact pins as the proof, merge-order env setdefault layering (Linear-key `LINEAR_API_KEY` seed from main-checkout `perk.local.toml` before os.chdir → worktree consumers inherit it), `run_worktree_setup` single canonical `[worktree] setup` exec-path (bash -lc, abort-on-failure; remote `position_worktree` skips it), worktree `.agents/skills/` mirror — `materialize_skills` per-skill single-hop symlinks after materialize_plan_body, loud-but-non-fatal & idempotent (repoint stale, never clobber a real entry), local-only since position_worktree is repo_root), worktree filesystem lifecycle (.resolve()-both-sides candidate match, uncertainty⇒skip, split --force, .pi/-dirty test gotcha, `main_worktree_root(cwd)` primitive #730 — MAIN checkout root from inside a linked worktree via `git rev-parse --git-common-dir`+`.parent` (relative form resolve-then-parent / absolute form parent-is-main-root), contrast `repo_root`/`--show-toplevel`=worktree, fail-open `None` outside a repo, macOS `.resolve()`-both-sides gotcha (`--git-common-dir` returns realpath), safe layering config→git (git.py stdlib-only), `[worktree] setup` hook + `created`-flag real-run-vs-dry-run asymmetry — preview re-derives `stage.worktree=="create" and not path.exists()` not `resolved.created`, user_output→stderr/machine_output→stdout, `**kwargs`-to-keyword-only ty trap), mergeability & conflict-resolution (`git merge-tree --write-tree` exit-code-IS-the-verdict offline probe & parse-paths-from-info-block-only, carry-the-verdict-explicitly never-re-derive-from-`len(conflicts)==0` — `MergeProbe.mergeable` from the exit code, fail-open everywhere (probe failure never changes submit exit; conflicts→submit still exit 0, reported-not-failed; `--dry-run` offline), warm reactive `driveConflictResolution` gated on `ok && mergeable===false && conflicts.length>0` & bounded by `conflict_resolution_attempts` cap reset-on-clean & type-guard-not-bool-helper, advisory-decode leniency for new base/mergeable/conflicts fields, first write-capable + context-inheriting `conflict-resolver` subagent fetches own context via `perk pr review-context --json`, worker `mergeable !== false` implement bar, GitHub force-push stale-`headRefOid`→FF-empty-commit unstick & unique-file-per-branch), write-capable cold doors (borrow the `save` stage to WRITE the main checkout — `mode:read-write,worktree:none`→repo_root, write-capable sibling of the read-only `plan`-stage borrow; otherwise inert — `binding_trigger="command:skills-<verb>"` suppresses save's bindings, authoring-context injection gated on read-only so no save behavior; NO structural sandbox — soft-scope in seed only; repo-skills lifecycle verbs scaffold/delete (FS) vs create/refine (cold doors), refine refuses on missing SKILL.md-file-not-dir; main-checkout `repo_skills_root` = `main_worktree_root or repo_root`; non-fatal reconverge — FS write FIRST then `converge_repo_skills_manifest`, errors ride `--json`/stderr but exit stays 0; reconverge ONLY the fragment never `skills update --sync`, delete unlinks single dangling symlink in try/except; dogfood-gate stitched-precondition test — stub only `github.repo_identity` run real verb assert BOTH frontmatter-valid SKILL.md AND converged manifest fragment, refine-skips-sync structural spy — `converge`/`run_skills` seams stay `== []` while launch fires once; manual dogfood gotcha — default `gh` token lacks `delete_repo`), skill bindings (two-plane delivery & doctor validation/injection mirror & `references:` subdirectory routing with zero init/doctor wiring (the per-skill whole-dir worktree symlink carries `references/` free, no wheel entry — reconfirmed by a bundled non-perk skill) & linked-worktree delivery depends on the cold-door `.agents/skills/` mirror — dangling-binding warning is the symptom & description-discovered ≠ stage-bound (perk-expert — no `[[bindings]]` row, invoked on frontmatter description match, description IS its primary docs) & self-contained references because delivered skills land where docs/user-docs absent — `*Canonical source*` footer + the references second-mirror lockstep (human-review discipline not CI gate) & `DELIVERABLE_COMMAND_TARGETS` MANUALLY-curated frozenset grown to objective-reconcile/learn-docs/pr-review/objective-replan — one-line membership/drift fix + two assertion sites (`tests/test_bindings.py` + `bindings.test.ts` EXPECTED count) & forward-declared bindings land ahead of their door — a `command:<id>` binding + `DELIVERABLE_COMMAND_TARGETS` entry can land before the firing door (inert with no call site until a door wires the matching `binding_trigger`)), shared `shared/` contracts recipe (incl. `contracts.md` §-numbering not contiguous — grep `## §8.` before assigning; cross-plane SSOT prompt-fragment helpers + lockstep parity substrings; registry vocabulary keys land with their first declaring stage; registry-writes changes trip two pytest pins; both-planes wording pinning; TS reader is the vendored `miniYaml` not the `yaml` npm package — dropped #639/#641 for clone self-containment), provider seam (five seams now: plan/todo/askuser/footer/web; artifact-vs-interface classification decides whether you write an adapter at all — askuser/footer/web are vacate-only interface seams (`adapter:null`, no shim; askuser's tool NAME `ask_user_question` is the whole contract, shared-tool-name free allowlisting in READ_ONLY_TOOLS but SDK_READ_ONLY_TOOLS stays strict), three vacating mechanisms + a 4th posture: registration-time vacating (plan, askuser) / install-site/runtime vacating keyed off `ctx.cwd` (footer — 3rd mechanism, install gated in index.ts `session_start`, no `process.chdir` test tier, config reader in `footerProvider.ts`) / runtime deferral (todo) / "nothing to vacate at all" (web — perk registers no web tools); footer is the 2nd interface seam (footer-ownership≠status-publishing decoupling — composed `perk` setStatus publishes independently); web is the first non-null-`package` default (perk owns no native web impl) — fresh-init fixtures get object-form `{source:npm:…}` → `.startswith`/`in packages` string-membership breaks (`isinstance(p,str)` guard / identity helper), no substrate change needed; borrow-ban footer-clobber reconciled-not-contradicted vs a selected footer provider; `package_filter` omitted for single-root extensions; collision-kind decision rule — command/tool-name collision ⇒ registration-time-vacating (plan, askuser) vs no-collision ⇒ runtime deferral (todo), 2→N widening census (SEAMS/PROVIDER_SEAMS, ResolvedProviders+resolveSeam union, providers.ts id constants, config readers, init provider loop, doctor ok-summary; GOOD-fixture must gain a default per new seam else negative validate() tests break; validate/by_id/default_for need no change), owned-surface deferral vs always-registered substrate, registration-time vacating vs runtime deferral two-node split, injection-only adapter shim, both artifact seams have real foreign adapters, produced-contract tier sets bridge weight, re-derive a sibling's forward-note don't mirror, `package_filter` omission, cross-plane resolver mirror, `cache.plan-ref.provider`== issue-backend `github` not seam id, augment-posture provider — three-tier registerPlanMode & persisted-mode gate read & present-for-review save split, tombell state-twin read & tools-hidden prompt branch, footer catalog purely-declarative widening — `pi-status-footer` / `pi-default` (1 row + 1 id const, zero convergence change), `pi-default` first null-pkg NON-default (orthogonal to default), catalogued foreign pkg ⇒ managed-identity two-way removal, `pi-status` renders NO extension statuses — first counterexample to 'both foreign footers bridge automatically'), CLI command groups (§8.1 group-dir template, hybrid default-dispatch stage/group coexistence, enacted taxonomy arc nodes 1.1–3.3 all landed — dormant substrate + structural parity-smoke fingerprint (literal EXPECTED_SURFACE, never raw --help), `MergedCommand` argv-`--json`-pre-dispatch (no option union) + per-object marker state (root-group flat-alias, command-object kind), `SectionedAliasGroup`-is-an-`AliasGroup`-not-`SectionedGroup`, two-edits-to-retire-a-generated-launcher (DEDICATED_STAGES + drop from STAGE_LAUNCHERS), flat-alias-routes-before-STAGE_LAUNCHERS, defensive-merged-command registry try/except → bare worker, launcher-only-≠-merged (`pr ready` is W, merged set = submit/land/plan-save), pure-relocation-≠-merge fold (objective group), thread-optional-kwarg-through-launch-chain (seed-prompt flag inert on --remote), update-warm-doors-argv often a verified no-op, cold-door-argv-grep-ALL-tests (planReview asserted argv[0]), worker-object-direct-invocation under --json with explicit obj=PerkContext, parallel-node union-conflict file set (stages.py DEDICATED_STAGES + cli.py root block; fixtures auto-merge but re-verify EXPECTED_SURFACE), cli.md-guard-deleted-for-structural-guards arc (fix prose that advertised it), SSOT-corrects-parent-propagates-to-3-surfaces (SSOT + reconcilable prose + node descriptions), `objective doctor` worker exit codes (detect/fix→0 even an ERROR-severity report-only drift, aborted-write→1, not-a-repo→2; `fix.failed.error` optional field; add to EXPECTED_SURFACE alphabetically); perk skills pass-through-first noun-group — every verb forwards to substrate (inherited stdio, verbatim exit code) EXCEPT upstream-lacked `remove` (edit manifest + `skills sync` capture_output), manifest `sources` keys = managed-source authority, sanctioned-subprocess guard `_SANCTIONED_SUBPROCESS_WRAPPERS` keyed (file_stem,func_name) + explicit check=/timeout=, N-symmetric-rollback-arms = test ALL arms, group self-containment own `skills_fail`/`skills_emit` mirror-the-`--json`-shape-not-share-across-groups; PlanGroup is 3rd inline hybrid copy, HybridDispatchGroup base deferred, registry `command:` field informational), CliRunner clobbers `sys.stdin` mid-invoke (swap the module's whole `sys` for a fake to test an `isatty()` branch), `EXPECTED_SURFACE` parity-smoke fingerprint is alphabetically SORTED (ignore a plan's positional instruction), SectionedGroup help taxonomy & generated-vs-dedicated launcher long-help split & Click two-paragraph short/long help, byte-identical-JSON fold discipline & re-grep-all-N-helpers, parity smoke & rendered-row help-slice tests, fold gotchas — quoted two-token bash case keys, git mv blame), cold-door client (`runColdDoor` envelope client — rollout COMPLETE, substrate mandatory for new doors; strict-iff-appended-to-workflow-state vs advisory-dropped vs fully-lenient decode criterion, fail-arm payload re-narrowing, fixture-sweep rule when strictening a decode & the merge-race fixture sweep — grep ALL fixtures repo-wide after any cross-plane shape change, derive-don't-decode strongest tier — render-only-and-redundant fields derived from the strict field & the bad_output-reachability acceptance test, cross-plane skew structural for the dev flow — the substring-preserving skew-naming reword, exported narrowing helpers, stdin-flag-at-argv-end, legacy-label byte-compat lever, non-string envelope fields fall back generic, parent-posts warm-tool recipe (`post_pr_review` — strict whole-batch decode + soft Result + best-effort appendWorkflowState, reuse the existing `--batch` cold door unchanged, new WorkflowState field needs no rebuild change), a parity-only field on a strict cross-plane decoder MUST be lenient — `base` via nullableStringField), github gateway (`perk/github/` parse-helper family, consolidation boundary rules — byte-identical errors via per-site what/source & leave single-site wrappers alone, helper-prefix+caller-tail argv pattern, substring not-found fold residual — phantom-`None` lookups, mutation-posting policies — verdict-driven /pr-review split (now reshaped single→parallel parent-posts via `post_pr_review`, incidentally unlocks headless-drivability) & 👍-reaction minimal artifact & asymmetric failure ladders & structurally-unpostable fields, gh-GraphQL transport facts — no `{owner}/{repo}` templating / cursor-pagination-omit-on-first-page (`-f cursor=` empty ≠ null) / GraphQL not-found shape (`not_found`/`could not resolve to`) broke shared `_is_not_found` → broaden + live-refixture, non-default-base autoclose strand — `Closes #N` fires ONLY on default-branch merge → surface PR `base_ref` (object-shaped `base:{ref:.base.ref}` `--jq`) + capture-before-reassign + close explicitly only on confirmed non-default base + fail-open layering; gateway purification by hoisting a backend-specific read to the consumer — delete-the-read + value-as-param + resolve-via-resolver (drops `isdigit()` gate) → pure gateway AND a free missing-backend fallback, layering-violation-means-read-belongs-in-consumer, `backend_id="github"` literal breaks resolver↔adapter cycle, `repo_identity` third repo-view read shape (`gh repo view --json name,url,defaultBranchRef`→`RepoIdentity` one-shot, reuses lenient `_exec` helpers but strict-on-incomplete, GitHub-only by construction, isort-sorted re-export, sole consumer = repo-authored-skills source derivation)), issue-backend seam (protocol module shape & annotated-binding conformance, late-bound GitHub adapter over the monkeypatched substrate, 21-function consumer boundary + substring import-direction guards, two-shape fixture rule, numeric-id grep tag, not-found message contract, `backend_id` stamp discipline & required-kwarg caller census & doctor arm-mapping over a collapsed error & opaque backend-owned header ids, opaque-id validator relaxation — the backend is the authority on junk & sweep ALL id consumers (isdigit/`\d+`/number-typed params), per-backend land closure — branch on `backend_id` never id shape), linear backend (plain-`Authorization` auth & RATELIMITED-as-HTTP-400 errors-first parse & `.codes`-not-substrings, dual-encoding metadata markers — transcoder byte-identity & form preservation & the both-encodings presence-check bug class, two composition disciplines, client-side comment sort & lookup-first labels & `_PERK_LABELS` tuple, one readiness probe two consumers, env-first/config-fallback `client_from_env(repo_root=)` + CORRECTED worktree key bridge #730 (was env-seed-only & masked a bug — cold-door funnels `resolve_issue_backend(worktree)` → `<worktree>/.pi/perk.local.toml` never exists; fix = single reader `load_local_linear_api_key` now reads `main_worktree_root(repo_root) or repo_root` = MAIN checkout, found inside a worktree independent of the env-seed, repairs both cold-door fallback AND env-seed; env-seed still wins when env set since env-first; widening-broke-21-fakes → loosen to `*a,**k`), Node-3.1 GraphQL type-literacy consolidation #731 (zero `cast(` in the package — only the 4 inside `_opt_*`/`_require_*` defs in linear.py remain; substrate-home — generic `_opt_*`/`_require_*` in linear.py vs domain payload mapping (1 pilot TypedDict `LinearIssueNode` + its narrowing helper) in pkg leaf `_helpers.py` importing the generics; `description` stays `_opt_str`→`str|None`; disposition-rule detail at ty.md), backend-aware prompts branch on the stamped `cache.plan-ref.provider` not config, scripted GraphQL fake & httpx MockTransport recipes, agent-session emission — internally-gated emitters keep hook sites one-line & force-gate-open/break-substrate fail-open testing, stateful `FakeLinearWorkspace` — named GraphQL operations & read-seeding & UUIDs-only-on-mutations, live smoke RAN (Modes 1/2 + Projects spike green, docs-only PRs, no defect): ProseMirror round-trip CLEAN & re-save patches-in-place, paired `INPUT_ERROR`+"entity not found" discriminator `_is_entity_not_found` (generic-code caution — confirm code is specific & re-shape offline twins & prove the narrowing via negative tests), bare-identifier mutation acceptance (`_uuid_for` collapse DELIVERED #562/#620 — module-level `_request_issue_mutation` over SIBLING op classes (not a method — they share a client, not a composition), not-found mapping relocated into the wrapper, capture-at-create UUID for the UUID-only `issueRelationCreate`), attachments-not-comments + installed≠connected (control-PR isolation test, App uncheckable from a normal `gh` token), team-KEY-not-name `[issues] team` gotcha (non-fatal at land), idiomatic backend — attribution rides single create bottleneck (`assigneeId` in `_create_issue_raw` / `leadId`+`startDate` in `create_project` / `viewer_id()` cached), fake-init gotcha (no super().__init__ → seed cache or AttributeError; scripted pre-seeds / stateful leaves None), workspace-scoped labels (drop `teamId`) + four→five label ripple (grep `four perk`), idempotent `attachmentCreate` fail-open into `update_plan_header`, prose-first reorder safe except the one manifest-backfill WRITE flips before-START→after-END + deferred collapsible-toggle never-guess-unverified-rendered-form, byte-stable sibling `_comments_with_authors`, Mode-4 project-backed lifecycle PROVEN LIVE #621 (find-by-run-id idempotency, Project Update on create/land/reconcile, close→completed, node workflow mirror BOTH directions, ProseMirror clean, node↔plan unification makes NO new perk:plan issue; setup: backend selection reads COMMITTED `.pi/perk.toml` ONLY — overlay ignored, `uv run perk` not global; `get_objective` omits depends_on, derives phase from node id, silently absorbs drift), Projects substrate now BUILT (`LinearProjectObjectiveStore`): substrate-home principle (only `LinearClient` encapsulates GraphQL; op classes register only the client — op-class composition was a planning-time-confirmed defect #582, corrected #586), re-homing via both-fakes-SUBCLASS-`LinearClient` (no super().__init__, inherit machinery through overridden request), project-backed `create_objective` (overview = inline-code header + reconcilable prose, NO roadmap table — roadmap derived LIVE from node-issues, no stored YAML), content-at-create + documentCreate fallback, direction-by-field relations via relations/inverseRelations, milestone-by-name-not-position, `_FakeLinear`/`FakeLinearWorkspace` insertion-ORDER substring footgun (more-specific needle first), parsed-selection fixture-sweep rule, `issueRelationCreate` not-found divergence INVALID_INPUT/"Argument Validation Error"; Phase-4 project-objective ops — `add_objective_node` materializes a node-ISSUE (live-roadmap→compute-`<phase>.<n>`→phase-name enrich→`ensure_phase_milestone(known=None)`→create-issue→per-`depends_on` relation), id-collision `None` branch UNREACHABLE (test the gateway error-map, not the impossible branch), FakeLinear add-node needs 2-elem `project(id` list + `teams(filter` entry; phase→milestone seam routed empty-`known` for byte-equivalence (negative `assert not projectMilestones(`, name-is-dedup-key); fail-open `projectUpdateCreate` from pure count composers (no extra reads, try/except non-fatal stderr); manifest-drift design DELIVERED #626 — persisted visible inline-code manifest beside `objective-header` (status/pr excluded), `attach_issue_to_milestone` (bare-id, no uuid resolution) + `project_issues_with_milestones` sibling query, fresh-manifest splice renders inline-code manually (replace_metadata_block append-when-absent emits HTML — bad for Linear), `FakeLinearWorkspace` cascade-delete relations on issue delete; `check_project_readiness` separate fn → 2 doctor checks + nullable `LinearReport.project`; third backend-aware seam `objective_read_instruction`/`objectiveReadInstruction` supplemental-clause (github="" byte-unchanged) — cold-vs-warm source asymmetry (cold has store.backend_id+url, warm resolves `resolveIssueBackendId(ctx.cwd)`+runColdDoor fetch only when linear & fail-open)), measurement-node firing (`client_from_env`+`request`, team UUID via `teams(filter:{key})`, /tmp throwaway) + `--json` cold-worker driving (implement has no worker → copy plan-ref into worktree) + probe-clobbers-the-sentinel + urllib-SSL-fails-use-curl, still-deferred: RATELIMITED retry/backoff, `LINEAR_API_KEY` GHA secret, agent-session deferrals, remote-vs-local session pointer; a new mutation needs the fake to ROUTE it AND have the workflow states (supersede surfaced a `"projectId"` `issueUpdate(` arm + a `projectUpdateCreate(` arm + a `canceled`-type `_STATES` entry)), objective-store tier (split off `IssueBackend` — dormant-contract → atomic-removal CI-green rule (removal inseparable from rewire), Linear facade-refactor + the late-bound-delegation equivalence lock, translate-CM, resolver single-sourced off `[issues]`, `backend_id` import-cycle literal, `close_issue` issue-tier vs `close_objective` on the store — delegation transparency + the guard-asymmetry (issue-tier guard owns the close, add `objective_stores.py` to its allowed-set), and the node↔plan unification protocol — `None`=doesn't-unify because unifying-store-raises-on-not-found, dry-run returns `None`, derive the land-time backlink self-referentially never a clobbered field, node-issue title ≠ plan title; Phase-4 protocol growth 6→9→10→12 methods ⇒ THREE implementers (GitHub/dormant-issue-backed/live-project-backed, ty-enforced) — `add_objective_node` re-render-vs-materialize split (project store has no roadmap block → node IS a Linear issue, `comment_updated` always False) & the no-op-return family (`save_node_plan`→None/`post_status_update`→False/drift no-ops let call sites invoke unconditionally) & manifest-unifies-both-backends; objective-keyed engagement reads reuse issue-tier honest reads on GitHub (cross-import private issues.py mappers OK — same tier) / Linear projects no edit-history → `read_description_edits` honest `()` / Project Updates deliberately not read / node-keyed `read_node_engagement` honest only on project store else `EMPTY_NODE_ENGAGEMENT` + deferral-comment-names-its-consumer rule; adoption Protocol growth `read_objective_source`/`adopt_source_as_objective` extend →None no-op family; #626 drift design — structural-identity manifest (3-state parse: absent/malformed/valid; no-op on GitHub/issue-backed), pure OFFLINE `objective_drift` engine (store builds the observed snapshot, the one network step), authority precedence (add-node→MANIFEST authoritative for an existing phase; reconcile→OVERVIEW authoritative incl. revert-to-`Phase N` default), deferred-edge sweep (create all missing nodes first then ONE post-loop sweep restores every manifest edge in BOTH directions), whole-repo `ty check` catches the conformance fake under tests/; objective replan supersede ≠ upsert — close-old/create-new (since `create_objective` is find-then-return idempotent), fresh `run_id` + bidirectional `supersedes`/`superseded_by` lineage, create-new-first/close-old-last/fail-open, `supersede_objective` joins no-op family → census `_FakeObjectiveStore`, fail-open close uses lower-level `_projects`/`_issue_ops` primitives not public methods (error-type boundary), `--supersedes`/`--adopt-from` mutually exclusive, `adopt_issue` reused MOVE-not-STAMP), session data & run identity (warm ULID mint in `session_start`'s none arm — grammar-not-subprocess cross-plane proof, two-resolver doctrine — never unify, provenance digest convention & LWW whole-map appends & two-grade refusal tiers & never-trust-stored-paths, the read-only carve-out 2-instance template & JSON-artifact variant, two-digests-two-roles & recompute-derived-flags, GC destructive-op triad & registry-derived terminal set & conservative eligibility ladder & dangling-pointers-by-design), source-scan guard tests (node:test-as-grep recipe — leading-dot patterns (a new-`ctx.ui.*`-method guard reuses it verbatim: `\.setWorkingMessage\(` allowlisted to surfaces — the seam's own structural interface decl has no leading dot so it's not flagged) & vacuous-scan self-check & production-only, pattern-matches-the-seam self-check, path-construction-scoped literal bans), doc-reconciliation (roadmap `pr` field ≠ merge PR, execute-doc-examples accuracy gate, keep-and-annotate over delete, boundary-criterion check before rewording a principle, objective-roadmap reconcile craft — hard-counts-in-prose are drift magnets re-derive every reconcile, check the PRIOR node's landed-narrative paragraph too (gaps accumulate), scope-attribution drift when earlier nodes absorbed the work → rewrite node description + extend narrative tied to the real PR diff scope, landing-log recipe — reconciling a just-landed node = append ONE 'Node X.Y landed (PR #n)' entry after the prior (extract-region + difflib-prove-only-additions, keep-and-annotate never rewrite older entries), Anchors-(verified)-region is a drift magnet — a node delivering what an anchor said didn't-exist guarantees stale → check every reconcile keep-and-annotate, landing-narrative PR-number convention — GitHub-objective body header+roadmap-only (no Reconcilable prose) → narrative lives in node description citing plan-issue#, reconcile-a-node-landed-with-its-ORIGINAL-planned-description — append LANDED narrative + rewrite phase-complete "X remains" claim (drift magnet)), borrowed packages (lockstep surfaces recipe — BORROWED_PACKAGES/settings/capability-summary/contracts/tests, setFooter-clobber vetting & `--no-ignore` greps under .pi/npm, thrice-affirmed retirement recipe, foreign tool names inert when absent, repo-non-mutation read-only bar, borrow vs provider-seam criterion), remote-runner (declarative-correct/execution-untested CI seam, Runner contract & two run-ids, establish-before-consume dispatch, fresh-runner git identity & PAT auth, three-candidate worker-entry ladder (`consumer-git` RETIRED — npm install superseded it; `source` = env/self/consumer-npm), loud-deferral→REALIZED pinned `@perk/pi` install (`_WORKER_DEPS_CONSUMER` now real `npm install @perk/pi@{__version__} --prefix .pi/npm`, still execution-untested), resolver-candidate-vs-migration-helper independent lifecycles (drop the probe but `consumer_git_clone_root`+`GIT_PACKAGE` stay for the `_remove_orphaned_git_clone` cleanup migration), grep-ALL-contracts third-§8.14-site discipline, open follow-up — defaultCreateRuntime in-memory settings never load disk packages so a real remote launch may register zero extension tools), init/doctor division & managed-convergence SSOT & committed-tracked-managed-subdir-needs-no-worktree-symlink-mirror (contrast skills materialize_skills; perk owns the WHOLE subdir — prune inside never outside, same change-list apply=True/False) & doctor-disk-vs-selfcheck-prompt & the `GROUP_ORDER` human-render trap (every new Check.group must join the render order) & the optional `EnvCheck` tier (warn-not-fail non-fatal tool — `optional` flag stamped on BOTH present/absent results, `required_tools_ok` filters them off the exit-2 path, NO GROUP_ORDER change since warn leaves healthy/exit untouched, 3-way init mark) & fix_errors first-class report data & fail-level checks shift verify=True baselines & §8.5/§8.6 machine-surface co-owners & seam-signature ripple & the AGENTS managed-block byte-match proof & adding-a-NEW-gated-probe-beside-an-existing-gated-check (census ALL sibling-readiness monkeypatches incl. `--fix`-under-verify, `report.healthy` is the WRONG non-fatal assertion → assert no fail-status in the group, two-phase independent probe no-early-return-between + read-access-as-honest-scope-proxy + nullable-field-must-not-flip-`ok`), init shelling out to external CLIs (skills delivery now load-bearing — two-tier loud-failure posture flip, post-sync presence verification, config-vs-substrate check split, `__version__` now version-string + AGENTS-stamp only never a ref pin — three consumer-ref sites collapsed `v{__version__}`→`main`, import dropped from `workflow_artifacts.py`; `PERK_SKILLS` SSOT — one alphabetical tuple edit cascades to manifest fragment + post-sync verify + doctor check (holds for non-`perk-` names), pre-merge self-converge `missing-skill` is EXPECTED/non-fatal (skills CLI resolves ref:main, skill not on main yet — init still converges the artifacts you commit), `references/` subdir travels via the per-skill worktree symlink + no wheel entry; three-SSOT skills split PERK_SKILLS/REQUIRED_EXTERNAL_SKILLS/MANAGED_SKILL_NAMES (verification iterates MANAGED_SKILL_NAMES not PERK_SKILLS), dagster ref:master, regen-via-`_desired_skills_manifest`-equality not full init, conflict-not-missing-skill + "Converged before failure" is committable; repo-authored-skills 2nd fragment = verify-gated gesture NOT a ManagedConvergence (rendering a valid fragment does a `repo_identity` GitHub read; convergences run unconditionally offline) + substrate-build patterns — gate-early-return on BOTH items AND errors, self-exclusion scanning sibling fragments, network-skip ordering by testability, byte-identical shared-constant extraction verified by DIRECT byte-equality, seam-signature ripple absorbed by `**kw` stubs, residual alias-grammar sanitization intended-but-unbuilt), extension-clone lifecycle (RETIRED — git-clone extension lifecycle REMOVED, perk now ships `npm:@perk/pi` (live story in distribution); doc trimmed to the pi-`git:`-loading substrate (host-alias jiti + no-self-heal install / unlocked-clone race / frozen-freshness — still accurate for ANY `git:` package) + the retire-an-orphaned-lifecycle recipe — relocate-survivor-to-dep-home (`consumer_git_clone_root`→settings.py beside `GIT_PACKAGE`, facade scrub RUF022 isort-alphabetical in BOTH import-list+`__all__`) + removing-a-doctor-check-is-a-three-site-edit (checks.py def / doctor `__init__` import+`__all__`+append / fixes.py elif; keep sibling extension-install) + dead-primitives-go-last-after-sole-consumption-proof (`git.clone/reset_hard/head_sha/ls_remote_sha` gone w/ tests, shared `git.fetch` stays) + `_MIGRATIONS` filesystem-rmtree forward-migration seam (`_remove_orphaned_git_clone`, forward-only idempotent ([],[]), failed rmtree→report.fix_errors LOUD, runs unconditionally after fix loop so `run_doctor(fix=True,verify=False)` exercises it); kept-general F821 test-insertion split-assert trap (anchor between-functions insert on prior test's COMPLETE boundary not first terminator)), distribution (version SSOT = static pyproject `version` bumped via `uv version`, `__version__` DERIVED via `importlib.metadata`+`tomllib` fallback (install-time staleness, `uv sync` refreshes), three-way `test_version_lockstep` package.json==pyproject==`__version__`; KEEP hatchling not `uv_build` — no wheel force-include + fixed sysconfig `data` slots can't nest `perk/_shared`, but `uv build`/`publish`/`version` backend-agnostic; dual-plane `release.yml` — widened triggers + per-job gating, AUDIT every job for tag-assumptions when widening (`validate-release-versions` gained `startsWith(github.ref,'refs/tags/')`), PyPI build-once/download-publish (`build-pypi` uploads `pypi-dist`, publish jobs download never rebuild) via OIDC trusted publishing (no tokens, `id-token:write`, Environments+required-reviewers gate, trusted-publisher config out-of-band=runbook), npm asymmetry `--provenance` re-packs from source AT publish (no build→publish handoff, `build-npm` always-on rehearsal), capstone tag-gated `github-release` `needs:` both publish jobs; `--provenance` needs `repository.url` matching OIDC repo case-sensitive else HTTP 422 (adding repository/homepage/bugs doesn't change `npm pack` files; `@perk/pi` needs `--access public`, Node22 npm≥10); three-way install-pin policy — machine/repro surfaces pin `__version__` (`workflow_artifacts.py` `perk==`, `settings.py` `@perk/pi@`; can require RE-introducing `from perk import __version__`), human docs unpinned always-latest, self-repo exempt `..`/`--from .`; version-parity enforcement (the `PERK_CLI_VERSION` informational launch env var injected at the local seam only, the soft `session_start` version-drift signal, the no-third-doctor-check decision since settings-wiring + extension-install already cover parity, `test_npm_pin_lockstep`); git→npm wiring flip — discriminator PROTOCOL→IDENTITY (perk's own pkg now an npm: entry so protocol-split would wrongly reconcile borrowed npm → identity-vs-own-name; generalizable: categories-collapse-into-one-protocol ⇒ discriminator moves to identity-within-protocol), migration flipped DIRECTION (strips legacy `git:` perk entry by ref-agnostic identity, `GIT_PACKAGE` retained role=strip-identity), transient-orphan forward-pointer convention; init/doctor/launch own `@perk/pi` npm install mirroring git-clone lifecycle but FOUR mirror-breaks — non-env-verified npm gateway must wrap `OSError` (don't inherit required-tool leniency), best-effort `json.loads[key]` needs TypeError not just KeyError (non-dict package.json), absent→fail check makes fresh verify=True unhealthy ⇒ census ALL `report.healthy` verify=True asserts (--fix self-heals, plain-verify doesn't), conftest stub must land `present` not `unverifiable`; version-parity enforcement — `PERK_CLI_VERSION` 2nd *informational* launch env var (injected at LOCAL launch seam only, remote/dry-run early-return before it; read to COMPARE never drive state, contrast run-control `PERK_RUN_ID`), NO third doctor check (settings-wiring + extension-install already cover parity from another angle — ask-before-adding-a-check), soft `session_start` drift signal vs the live-loaded ext static checks can't see (no once-guard = one plain `if`, headless `report()` warning→`doctor --fix`), launch-env recorder-monkeypatch test (`os.execvpe`→capture env dict, assert `PERK_CLI_VERSION==perk.__version__`), `test_npm_pin_lockstep` both `@perk/pi` pins track file SSOT `_pyproject_version()` not just each other; reusables lock-in-install-ROOT, `.pi/npm/` already gitignored, `(stem,func)` subprocess-guard entry, `git checkout package-lock.json` before staging pi-ai bin-path churn), prompt-templates (which bundling tier a new top-level resource dir joins — `shared/` tier (wheel force-include + sdist only-include + npm `files`) for runtime-TS-read resources vs `agents/` tier (Python-only, materialized by init) — deciding question: does TS read at runtime or Python materialize?; `prompts/` joins shared-tier, four-test resolver proof (Python-editable/wheel/npm-tarball/TS-dev-tree), durable-README-probe over throwaway-placeholder (load by explicit name never scan); engine: jinja2 (Python) + vendored zero-dep `miniJinja` (TS `extension/substrate/miniJinja.ts`, 2nd vendored-engine precedent after miniYaml, drop `dependencies` key ENTIRELY); byte-parity render config DATA-SHAPE — jinja2 `autoescape=False,trim_blocks=True,lstrip_blocks=False,keep_trailing_newline=True,StrictUndefined` / miniJinja trimBlocks-on/lstripBlocks-off/keep-trailing-newline/throwOnUndefined (baked in, no config object), `trim_blocks`/`trimBlocks` now GLOBAL True (env flag trims since `{%- -%}` can't trim-newline-only), `keep_trailing_newline=True` LOAD-BEARING, `{% include root-relative %}` both engines + blank-line gotcha, jinja2 IS reference engine (golden bytes=jinja2, miniJinja matches byte-for-byte); frozen mini-jinja subset = renderer's input contract (4 categories: bare `{{ ident }}` / `{% include %}` / `{% if/elif/else/endif %}` over idents+strings+`==`+`and`/`or`/`not` / plain `{% %}` no `{%- -%}`); author-time grammar guard MUST match the stricter runtime (regex char-set gate + recursive-descent shape validator complementary, fourth lockstep surface, reject escaped strings + out-of-containment includes + malformed condition SHAPES, `{# #}`/`in`/`is` reject gotchas); CRLF byte-parity hazard (normalize `\r\n?`→`\n` in any TS module byte-matching a Python file read); string-only contract lazy-TS-vs-eager-Python enforced on BOTH planes; two-tier render-parity REPLACES the prose-copy golden bridge (golden's only role was the cross-process parity bridge) — Tier A purpose-built contract-snapshot goldens generated BY jinja2, each isolating ONE contract feature, multi-arm / Tier B `live.yaml` cross-engine equality via `extension/testing/renderLive.ts` (NO goldens, tarball-excluded but tsc/Biome-checked, prints in manifest order, Python skips when node absent, coverage guard = every real template in manifest); bare-import source-scan guard (strip block+line comments FIRST, scan dynamic `import()`/`require()`, exclude `*.test.ts`+`testing/`, pair Python `test_no_runtime_dependencies`); `cases.yaml`+`live.yaml` both miniYaml subset VARS STRINGS-ONLY (sidesteps True/true + number-format divergence), seed live.yaml verbatim from old real-template vars (drop `golden:`); prompt-move pattern — single-file-vs-subdir deciding factor is in-code BODY branching not var differences (branching=template SELECTION in code), no-trailing-newline arm templates for mid-prompt fragments (template AND golden, `printf` not `echo`, verify `tail -c1|xxd`), branching-in-code pass UNION of vars to every arm (StrictUndefined fires only on REFERENCED missing var), `.md`-suffixed root-relative render call (objective's no-extension illustration is loose framing), unify-vs-split decision rule KEYED ON WHY bodies differ — superficial house-style (header/indent/qualifier/closing)→UNIFY one flat template (converge away) vs cold-injects/warm-instructs load-bearing asymmetry→SPLIT arm files; unify CHANGES output so plan-time decision; plane-only-qualifier (e.g. 'from this read-only session') is a superficial tell — verify its premise (warm /learn-docs is NOT read-only, injects via sendUserMessage) before deciding load-bearing; conditional templates — `{%- -%}` CANNOT trim-newline-only (also eats next-line indent) → env `trim_blocks`/`trimBlocks` ON (strips only newline-after-block-tag, preserves indent, lstrip stays off, miniJinja byte-matches jinja2), bounded-blast-radius grep `{%` across prompts/ before flipping + keep golden byte-stable by editing TEMPLATE not regenerating; raw-var string-coercion `model or ""`/`node ?? ""` so `{% if %}` false arm behaves (block-tag-own-line vs inline-mid-line both byte-stable); selection-stays-in-code-only-string-ASSEMBLY-moves-to-template; single-arm subdirectory (conditional/early-return arm = NO file/NO golden, keep subdir shape even w/ one arm); demote-in-place substring constant (check other local consumers before deleting, remove only cross-plane lockstep framing — don't churn); byte-stability /tmp-capture de-risker (capture pre-change literal-helper output, assert render()==bytes in throwaway scratch NOT committed — catches em-dash/backtick/`{% %}` transcription); grammar-freeze stale-claim ripple (render/grammar change flips earlier LANDED claims → record supersession in CAUSING node never rewrite history + amend §8.31, frozen subset adds `{% if/elif/else/endif %}`+string `==`+`or`/`not`), `cases.yaml` don't-escape-backticks (only `\"`/`\\`/`\n`/`\t` escapable, miniYaml leaves backslash-backtick literal + PyYAML rejects → double-quoted literal backticks with only inner `"` escaped), golden-parity REPLACES substring + keep thin per-plane selection test, warm `pi.sendUserMessage`→assert `notifies`/`report(...)` NOT `seeded` (seeded only captures replaced `newSession` ctx) + ref-aware converged door (resolve plan-ref like learn door, null-guard warning like /implement); recurring mechanics `npm ci` fresh worktree before TS checks when node added runtime dep, `edit`-fails-across-em-dash→python `str.replace` heredoc, `run_ci` green≠committed-format-green run Biome formatter, end-of-line inline-conditional eaten by `trim_blocks` → author `{% endif %}\n\n` so the structural newline survives, script-generated byte-fidelity (string-replace an all-clauses capture, longest-first var-replacement ordering — URL before bare id), `[[ci]]`-skips-`node:test` on a `prompts/`-only diff → run `promptGrammar.test.ts`/`prompts.test.ts` node:test manually (TS scans `prompts/` at runtime), keep `contracts.md §8.31`), dot-directory migration (.pi/→.perk/ canonical contract ownership-vs-discovery — `.perk/` perk-owned vs `.pi/`/`.agents/` discovery namespaces perk only materializes into; centralized `paths.py`/`.ts` seam allowlisting both paths+cache, `config_dir` single config-family redirection point; guard-is-a-backstop-not-proof + consumer-census-oracle; three grep forms slash-literal/join-token/escaped-regex; failure-injection-targets-root-by-name; `_MIGRATIONS` forward-only triad + `filecmp.dircmp` shallow-diff-needs-`cmpfiles(shallow=False)` + `common_funny` type-collision gotchas; manifest-fragment source-location-independent; dogfood gitignored-cache-root move forces self-repo gitignore reconverge + worktree-binding migration to land; AGENTS managed-block substring-asserts; worktree init skills-sync-fails-AGENTS-converges-first; audit-before-edit grep; arc status 1.1/3.1/4.1/5.1 landed, 2.1 closed-unmerged, 6.1 pending), templates reconvergence, Click bottom-of-file imports & register_with_aliases arity, report-only pre-flights, supervisor cancel/retry control seam, rerun reuse, GHA universal zero-spend smoke short-circuit, fail-soft overlays vs local records truth, subprocess monkeypatching traps, contracts §-numbering grep, post-merge objective roadmap reconciliation, Pydantic boundary models (strict/lenient base decision — `StrictBoundaryModel` perk-authored-shapes / plain frozen `BaseModel` forgiving `Config` / `LenientApiModel` external-APIs; "No Pydantic Settings"≠no-pydantic; model validates ASSEMBLED result not the `_parse_*` layer (lenient parse stays byte-identical, model is a frozen structural backstop); structural/content split PRESERVED by DEFAULTING every field never requiring (missing key = content Issue not structural raise) + keep value-untyped fields plain `str`/`dict[str,Any]` so `validate()` enum/vocab checks stay live; `model_validate(dict)` vs explicit-kwargs is the `extra="forbid"` lever (per-element validates / top-level kwargs tolerate); "model IS the tolerant parser" — relocate the `_str` sentinel-collapse into `@model_validator(mode="before")` returning ONLY declared keys (stray key stripped before extra="forbid", no robustness regression), `strict=False` CANNOT replace `_str` (no `str←int/bool/None` conversion row, raises in BOTH modes); derived fields → read-only `@property` (forces keyword-only test constructors); vocabulary/uniqueness stays in `validate()` not model constraints; context-gated set-level `@model_validator(mode="after")` raises ONLY on `info.context` opt-in (load lenient / `validate()` re-validates `model_dump()` + converts `exc.errors()` → Issues — re-validated field MUST be `list` not strict `tuple` since dump emits a list); named `Annotated` before-validators not lambdas (absent→default skips coercer, present-ill-typed→coerce); byte-stable stored YAML depends on field-DECLARATION order (`model_dump(mode="json")` emits declaration order → declare in old `to_data()` emission order NOT dataclass order else every body churns + order-guard test; pydantic v2 has NO required-after-default rule); `translate_validation_errors(DomainError, source=path)` wraps UNCONTROLLED boundaries only — code-controlled constructors (`PlanHeader`/`PlanRef`/`Config`) let `ValidationError` surface loud (a perk bug); frozen mutation raises `ValidationError` not `FrozenInstanceError`; testing — exercise validation through `model_validate({...})` NEVER typed kwargs (ty checks `tests/` → flags deliberately-invalid kwargs invalid-argument-type/unknown-argument/missing-argument; `model_validate` takes `Any` AND is the real boundary shape), unavoidable typed-kwarg negative needs `# ty: ignore[rule]` (mypy `# type: ignore` doesn't suppress ty), lax coercion picks the negative target (`str`→`Path` accepted so target a non-coercible field), pydantic rejects positional args (grep+switch to kwargs), bind `.default_for("x")` to a local before `.id` (ty narrowing across separate calls, run_ci-only), `mode="before"` dict needs `d: dict[Any,Any]` annotation (else `dict[Never,Never]`), ruff isort case-sensitive + E501 reflow run `ruff format` before commit; whole-tree `typecheck-py` gate (not change-scoped) → a latent typed-kwarg bug in `tests/` blocks unrelated PRs, clear pre-existing ty debt separately + git-stash diagnostic; Python-internal tightening with byte-identical behavior touches NEITHER `shared/contracts.md` NOR `docs/user-docs/` — TS twins read the same `shared/` files independently); read when working on perk internals, factories, planning/objective/binding/provider subsystems, doctor checks, contracts, or gitignore mechanics → `docs/learned/workflow/`
- **pi/** — Pi extension API (getSystemPromptOptions/ctx.mode/injected-message persistence, registerTool execute results details requirement, planning tools read-only gating traps, tsconfig strict-mode index access in tests, offline `pi.sendUserMessage` injection spy via instance-property override — mandatory for seed-turn invokeCommand tests, headfulUIContext has no select/input/editor, `ctx.ui.editor` facts — no AbortSignal & title-borne key hints & dialog arms test via extracted core, registration-time `process.cwd()` config reads make harness tests host-repo-sensitive — chdir into the scaffold, the `PERK_RUN_ID` harness leak, `session_compact` first-class SDK event — type-only harness `emitLifecycle` union & stale-ctx-compaction-race silent swallow (isStaleCtxError diverges from log-not-throw) & export-wins-for-named-unit-test & String(null-proto) TypeError caution, vendored-extension test/infra — single `node --test <file>` (Node 26 native TS, NO `extension/testing/register.ts` hook), offline registration smoke via `loadPerkSession({cwd:scaffoldRepo()})`, extracted-core + tagging-theme-fake + seeded-`Math.random`), tool-param decode (`toolParams.ts` tri-state boundary seam — absent vs mistyped, strict-fail bad_input vs native-shape refusals, decode-before-side-effect ordering proofs, exported pure decode for UI tools, never reuse cold-door lenient helpers, fallback-chain optionality — absent flips to `undefined` & the tri-state survives, `idParam`/`idArrayParam` opaque-id params), context injection (conditional inject-and-strip, stage-field disambiguation, stringify-includes once-only dedup, two-flavors-one-customType), context system (no transclusion, ambient index split, read-only bash allowlist — gh subcommand-shaped never verb-inferred, command-keyed language-agnostic `ast-grep` entry gates the command not `--lang`; perk can't own the `grep` builtin → managed AGENTS bullet + ambient skill + allowlist, no custom tool; allowlisting a read-only bash gate command = five-surface lockstep (SAFE_PATTERNS + paired test lists + contracts.md + user-docs two notes + learned doc); `agent-browser` is the 2nd command-keyed precedent (write-leniency accepted, no READ_ONLY_TOOLS change)), structured output (pi-ai tool-calling not JSON mode, faux-provider offline tests — top-level vs nested registration by caller, `PERK_NO_LLM` the only offline gate — auth resolution succeeds keyless), headless session construction & driving (runtime-factory builds the loader internally, explicit `bindExtensions` `mode:"json"`, `session.subscribe` raw-AgentEvent facts, single-prompt drive + budget watchdog & premature-idle gap, structured run-event stream — additive `RunEvent` union, two fail-soft tiers, route-don't-relay, offline model-availability determinism), subagents (agentOverrides reach builtin-only & per-call inline model override, child-posts-own-mutation vs read-only-child-parent-mutates (/pr-review reshaped to report-only parent-posts — the reviewer drops `write`, angle passed per-call, read-only fan-out not the posting agent), context fresh/fork, review 422→comment fallback, agent-def consumer delivery realized — frontmatter-derived runtime name (NOT file path; subdir placement free), installed-npm-packages-never-scanned (carrier is wheel + `perk init` not npm), PERK_AGENTS committed managed convergence into `.pi/agents/perk/` (prunes inside, no worktree mirror since tracked) — now four perk agents, `conflict-resolver` newest & first write-capable + context-inheriting (driven by `/submit`), widening-lockstep census, editing a perk agent's prose rubric (`agents/*.md` SSOT → re-run `perk init` → commit both byte-identical; the `.pi/agents/perk/`-not-`.pi/agents/` stale-path gotcha; skills-sync-fails-but-agent-converges-first worktree non-fatal; gitignored `.pi/perk.local.toml` so stage explicitly; no test asserts prose body verbatim — guards check presence+consistency) & the judgment-agent prompt anti-pattern (default-verdict + decide-conclusion-first + uncountered anti-noise framing biases → enumerate-findings-first + derive-verdict + "earned-not-defaulted" balance statement, binary posting bar unchanged, pure prompt-engineering change), fixed-key `[subagents]`, flat agent/seam-keyed config, TS TOML backslash continuation restriction, cross-plane model/prompt parity, exported guidance offline unit-testing, isolated `createAgentSession` bypasses perk's main-session read-only `tool_call` hook → thread tool-gating in (read-only⇒`['read']` only since foreign bash is unsandboxable) + fold gate state into the side-session cache key so a flip recreates it), extension consolidation seams (minimal structural interface — ReportTarget/BranchSource/EntrySink & narrowing plan-named SDK types to structural slices & typing an extracted core's injected dependency as a one-method slice, type-only-import cycle break when a vocabulary type moves owners, pure-module-plus-effectful-seam reconciliation, `appendWorkflowState` strict read-back & verified-linkage tiering & reference-identity object compare & object-valued custom `equals` identity-subset comparators & aspirational-comment migration signal, opt-in flag over API fork, report() owns-the-prefix de-prefixing trap, P1/P2/P3 site triage, report-rollout gotchas — local-`report` shadowing & failFor-only failure surfacing & severity-filter notify asserts), TUI surfaces (surfaces module = surfaces.ts+report.ts only sanctioned rich-UI sites, composed single `perk` status slot & headless full no-op, RPC dual-publish law — factory widgets/setFooter dropped in RPC, setFooter 0.78.1 facts — method-syntax bivariance & render-driven reactivity & once-only install & the single last-wins footer slot (load-order clobber hazard), windower/content-line budgets, truncateToWidth `...`-not-`…` & visibleWidth asserts, factory-widget/invokeCommand harness recipes, width-sweep invariant testing; vendoring a pi-tui-touching extension #628 — dual-copy CLASS type clash (top-level pi-tui vs the nested pi-coding-agent copy → value classes with private fields are DISTINCT nominal types; functions/interfaces are structural so fine; fix = structural-slice params + import KeybindingsManager from pi-coding-agent, NO dep bump/deep-path import), `setWorkingMessage` governed-but-permitted seam (headless-no-op over a minimal structural target, whimsical flavors the default indicator), `/btw` charter bounded-exception (ctx.ui.custom declined by D6 → named SOLE bounded exception, human-invoked/hasUI-gated/never-machine-reachable, lockstep across charter/contracts/AGENTS)), `pi.events` unreachable from harness — pure-over-a-fake-bus split & env-var over module-constant injection & getFlags/getShortcuts non-registration asserts, faux-model real-runtime drive (nested pi-ai per-instance registry — resolve through pi-coding-agent, inMemory settings don't layer disk → extensionFactories, seed faux-provider auth, eventSink vs file-sink exclusivity) → `docs/learned/pi/`
- **toolchain/** — ruff check vs format (silent-commit trap, `RUF100` fires on a `# noqa` for a non-enabled rule, `RUF043` raw/escaped regex for `pytest.raises(match=...)` metacharacters, `UP047` PEP-695 inline generic vs `TypeVar(bound=...)` when a generic fn returns the bound type (Callable-return escapes it), template string E501 multiline limits, SIM105 exception suppressions, preview-rule enablement drags cross-bucket fixes forward — ruff bumps too, run_ci green ≠ committable — pre-commit can reformat after CI passed, `RUF002` en-dash in a docstring — use a hyphen (em-dash fine), `RUF022` unsorted `__all__` fails CI — insert new re-exports in isort-alphabetical position in both the import list and `__all__`), Biome/tsc gotchas, Biome param-property type-stripping under `node --test` / `organizeImports` assist-only-under-`check` / `let x = undefined` trap / `Omit<Union,K>` collapses a discriminated union — use a distributive Omit / forEach expression-body assertion needs a block body / vendored upstream code + `noUncheckedIndexedAccess` (every `arr[i]`/`str.split("\n")[0]` is `T|undefined` — `?? ""` string-index, `?? messages[0] ?? 'fallback'` random pick, guard-before-discriminant-narrowing an array-walk) / new files: `biome check --write` before CI / template-literal backtick mangling under `--write` — review the diff, two-commit TS module mv+sweep recipe (blame survives the sweep, non-import-line audits, quoted justfile `**` glob, two-mode pre-move import resolution — resolve moved files against their OLD dir or a naive resolve-against-current silently no-ops, tsc --noEmit complete oracle, Biome reorders import-brace members), python package splits (script-generated slice-by-line-range off the git mv'd original, call-site-only paren-anchored binding rewrites cross-module-before-`_exec`, name-census + per-consumer negative-grep completeness proofs, partially-init package submodule-import fallback & sorted-`__all__` silences F401 & hatchling auto-includes the subpackage; objective-#714 split-arc refinements — seed-generous import loop (over-seed imports, ruff F401 trims/F821 flags, ty confirms → upfront census non-load-bearing), grep class-NAME across ALL function bodies not just `__init__`s, monkeypatch contract keyed on NAME-RESOLUTION (relocated fn reads siblings in OWN namespace; PUBLIC names a blind spot; some break loud others fragile-green), module-OBJECT attr patches survive but NAME-binding patches don't → facade re-imports os/subprocess/git/github in `__all__` (`import x as x`, PLC0414 not enabled), constant-used-only-by-moved-helper travels WITH helper (define-in-`__init__` = cycle; "facade keeps X"=re-export not defined-in-`__init__`), E402 forces a leaf data module (doctor/data.py mirrors linear_backend/_helpers), `(stem,func)`-keyed guards ripple → only full `just ci` catches, `__file__`-source-scan-guard silently narrows to `__init__.py` under a pkg → `.parent.glob("*.py")` + re-verify by plant/revert, AST byte-identity recipe (`ast.get_source_segment` vs `inspect.getsource`; caveat strips decorator line + drops trailing same-line comment), submodule-naming avoids protocol-sibling clash, long-method extraction preserves call-SEQUENCE — pass resolved network values DOWN, pure string recompute fine; folds & cross-package relocations — folding a flat module into a pkg as a NON-re-exported submodule: every `pkg.<sym>` becomes aliased submodule ref + grep INSUFFICIENT (misses type annotations `def f(x: pkg.Type)` import-time AttributeError + module-docstring paths) → `python -c "import <consumer>"` first oracle, git-mv directory-first then files; monkeypatch targets diverge by patch SHAPE across a fold (consumer-attr survives if alias preserved / absolute-module-path breaks / late-bound pkg-attr moves to new owning module); cross-package relocation sweep FOUR-really-FIVE forms (attr-access/single-line-setattr/MULTI-line-setattr regex-must-span-line-breaks=90-failure-miss/string-path-repoint-via-late-binding/two-level `pkg.sub.NAME`) + ty HAS A HOLE on `setattr(mod,"NAME")` string-form (ty catches import repoints+stale attrs but NOT string setattr → never trust green ty, run full pytest), classify moved-vs-stays-put + census counts up front; macOS BSD `sed \b` is SILENTLY a no-op (reports success does nothing) → python `re.sub(r"(?<![.\w])X\.",...)`/`perl`, negative-lookbehind spares qualified names; E402 insert-after-FIRST-`from perk`-import not last (mid-file `# noqa: E402` block trap); source-scan guard goes VACUOUS when its target moves (matches nothing=false-green) → re-anchor to NEW location + ALWAYS self-check plant/FAIL/revert; sibling-submodule imports avoid package-root cycle (import siblings via submodule paths, leave `__init__` byte-unchanged, `import X as old` keeps call sites byte-identical, whole-repo `ty check` completeness oracle); package-organization layering north-star — consult-established-layering-before-reorg, the rule outlives its now-removed planning doc), Biome `noControlCharactersInRegex` — single-pass alternation replace (two-star alt first) dodges the sentinel, worktree node_modules stale-SDK trap & package-lock churn cleanup & stale-global-`perk`/self-converge smoke trap (use worktree `.venv/bin/perk` for `shared/` changes; init smokes in a scratch dir; fresh worktree = empty node_modules → `npm ci` before tsc/node --test; a -1/143 test exit with no FAILED line is a SIGTERM kill — rerun before debugging; prove pre-existing breakage with the git-stash diagnostic before assuming you caused it & the delete/edit rebase-conflict keep-deletion-verify-moved-copy recipe), ty narrowing of untyped/JSON dict values (cast vs truthiness guard & the `_require_*` narrowing-helper family for deep GraphQL payloads & the heterogeneous list/dict literal → explicit-local-annotation gotcha — `nodes: list[dict[str, object]] = [...]`, not mypy `# type: ignore`; `.items()`-iteration idiom reads a known key off an `object`-narrowed dict cast-free (`next((v for k,v in entry.items() if k=="src"),None)` where `.get`/`[]`/`Mapping.get` all fail Expected-Never) — cast-free alternative to the cast fix; per-element `isinstance` in a loop does NOT refine the container element type (`tuple(raw)` stays `tuple[object,…]`) → typed-list accumulation (`acc:list[str]=[];append`); these are RESTRUCTURES not signature-only; `_opt_*` lenient twin of `_require_*` (cast-if-isinstance-else-None, never raises) + disposition-matching rule — never route a tolerant/skip/default site through a RAISING helper (silently flips fail-loud), `_opt_str(x) or ""` byte-eq to `x if isinstance else ""`; `Any`→`object` isinstance-tightening is body-unchanged-safe ONLY for scalar/list/set returns — dict-returning helpers hit `dict` INVARIANCE (`Top[dict[Unknown,Unknown]]` not assignable to `dict[str,Any]`, neither local-annotation nor non-ternary if/return rescues) need guarded `cast(dict[str,Any],x) if isinstance(x,dict) else {}`; intra-objective inconsistency — siblings chose cast-free `.items()`/typed-accum vs minimal guarded cast, BOTH sanctioned match local site; distrust a plan's "`Any`→`object` body-unchanged no-new-cast" claim — `object`-via-isinstance(dict) `.get(str)` fails Expected-Never + `(cur or {}).get` makes `~AlwaysFalsy|dict` no-`.get` → route every hop through cast-confined `_opt_dict`/`_dicts`/`_opt_str`/`_opt_int` in SHARED LEAF (casts only there, call sites cast-free), budget for these; incidental hardening `_opt_dict` walker more None-safe (→`{}` on non-dict not AttributeError), `_opt_int` REJECTS bool (`isinstance(True,int)`) preventing bool→numeric-id; inline dict literal to `dict[str,object]` param fails ty annotate-the-local; two divergent same-named enums → RENAME to domain-accurate names not unify (unify gives one an unused member), string VALUES unchanged so `__str__`/serialization byte-identical, no `__all__`/facade re-export surface, mechanics perl/python-not-BSD-`sed \b` + longer-idents-ripple-E501/I001 xref python-package-splits/ruff) & ty suppression syntax (`# ty: ignore[rule]`, mypy-style `# type: ignore` doesn't suppress) & ty enum strictness in tests (pass the member, not the literal — CI-only catch), test-suite parallelism (pytest `addopts -n auto --dist loadgroup` by default — `-n0` serial escape hatch, build-once = shared `xdist_group` pins consumers to one worker, skip-into-the-fixture; node `--test` parallelizes across files only — intra-file `concurrency:true` UNSAFE (process-global env mutation) so split harness-heavy files at section boundaries, noUnusedLocals/tsc trim loop, count-preservation `grep -cE '\btest\('` proof, `--test-concurrency=nproc*2`) → `docs/learned/toolchain/`

## Catalog

The routing index above is what lands in `.pi/APPEND_SYSTEM.md` (ambient for every agent). The
catalog below is a richer, human-readable table so you can browse the whole collection. Two columns:
*Doc* links to the doc, *When to read* is a single-line cue.

| Category | Doc | When to read |
|----------|-----|-------------|
| workflow | [plan-factories.md](workflow/plan-factories.md) | Touching a cold-door plan factory or its artifact vocab, plane-specific link-carrying, or the inbox-over-GitHub discipline |
| workflow | [human-engagement.md](workflow/human-engagement.md) | Working on the engagement renderer (§8.25 — the `engagement.py` pure leaf, two keyings/two seams issue-vs-objective-keyed + node-keyed, conformance ripple census dormant store+fakes, ty-static `EMPTY_*` constants, Actor heuristics over perk's marker grammar, Linear `_comments_with_authors`+`_is_entity_not_found`→empty / GitHub gh-GraphQL not porcelain, renderer untrusted-DATA blocks ≤30 items/~1500-char, cold-injects-warm-instructs + byte-unchanged-on-empty + per-consumer placement, worker-side Protocol growth composition), untrusted-data bounded rendering, the cold-injects/warm-instructs pattern + seed-byte-unchanged-on-empty + per-consumer injection-placement, or worker-side composition over Protocol growth |
| workflow | [in-place-adoption.md](workflow/in-place-adoption.md) | Working on in-place adoption writers for plan-from-issue / objective-author-from-issue (the 3rd/4th in-place writers, or the per-node asymmetric surface shape `plan from` VERB vs `objective author --from` FLAG, the don't-borrow-a-mutator-that-touches-the-preserved-field principle, compose from primitives, `read_issue` third shape, additive labels, the adopt-mapping side-map discipline, verbatim-preservation-differs-by-tier, handoff-rides-the-link `_adopt_from_handoff`, backend-discriminated OPEN refusal, Protocol-growth no-op family, Linear project-backed writer `update_project_content` not create / sibling `project_issues_for_adoption` / two-pass node-then-relation ordering, "doctor note" can mean prose-only, or offline-covered-only rule), OR the **seed-from-file mode** sibling (`plan from <file>` / `--from <file>` — a file has no backend identity to stamp so it omits `adopt_from` + skips `require_github` (gate auth at point-of-network-use), detection-before-parsing at the door top, the backend-free `seed_file.py` leaf, the `isolated_filesystem()`-already-chdirs test gotcha) |
| workflow | [plan-ref-lifecycle.md](workflow/plan-ref-lifecycle.md) | Touching plan-ref filesystem ops, the cross-backend objective-linked plan-save clobber hazard, reconstructing a plan-ref via the cache writer, non-default `base` resolve-once-then-pin mechanics (three readers, the `--worktree NAME` returned-field-as-scratch clobber), or the always-emitted `to_data` field sweep (hits exact-dicts + ty conformance fakes) |
| workflow | [plan-save-surfaces.md](workflow/plan-save-surfaces.md) | Touching the three plan-save surfaces (issue opener, /plan-save, plan_save), the fidelity gap & handoff_extra carrier, `resolvePlanSource` artifact→param→transcript chain, the warm `objective_node_claim` recovery carrier, the copyable command callout, or the 4 per-backend write shapes (post-create read+PATCH / fold-at-compose / post-create content update / fold-into-unified desc + idempotency guard) |
| workflow | [plan-review.md](workflow/plan-review.md) | Touching the plan_review tool, the backend-neutral dispatch (plannotator bridge vs first-party editor default), asymmetric review tiering (artifact→param only never transcript), `approvalSave` owns the gate exit, reviewOutcomeResult approved-first routing & SKIP_TEXT/guard-ordering footguns, the approved-first split mirrored to objectives & optional-param flavoring, extracted-core + scripted-UI-fake testing, or the `PLAN_AUTHORING_CONTEXT` consult-learnings nudge (`planContextContent` / one seam reaches both factories / three-surface lockstep / soft-nudge-over-gate) |
| workflow | [warm-doors.md](workflow/warm-doors.md) | Working on warm-door commands (the read-only gating trap, drive-the-session, the warm gate-enter recipe — enter distributed/exit centralized, rendering every cold-door outcome, or terminating-door drives next pass — `/land` auto-drives `/objective-reconcile`) |
| workflow | [objective-lifecycle.md](workflow/objective-lifecycle.md) | Touching the objective resumable-lease node authoring loop, the JSON draft artifact, render-at-the-door, the deterministic `perk objective run` supervisor (never-lands/never-plans, PR-existence implement-done signal, wait-then-refetch-whole-world, report-only budget), the pending-first selection, the `resumable_claims` coordination surface, the `planning`+`pr:null` accepted backlink race signature, JSON-mode≠JSON-payload, parallel-node rebase friction, the design-only-node pattern (node reframed build-X→author-design-doc; deliverable is a docs/planning design doc, no markdown linter skips all hooks, status stays done), the mandatory post-merge reconcile of BOTH prose & node description, the reconcile-ready design-only AUDIT node #717 (write the findings→roadmap section as direct input to `/objective-reconcile`, land = refinement not re-plan, audit-a-green-codebase methodology = chase what ruff/ty CAN'T see, don't re-catalog enforced rules, honest P-severity — honest no-P1 over inflated), or parallels with perk's plan lifecycle |
| workflow | [config-tables.md](workflow/config-tables.md) | Touching `.pi/perk.toml`, own-section vs wholesale-consumed-map placement, string-only TS parser vs bool-is-int Python guard, committed-only vs overlaid read (the three-precedent committed-only recipe & the overlay-ignored test), interior-gate vs init→settings.json convergence, the local-only secret-fallback reader (`perk.local.toml`, fail-soft on TOMLDecodeError, NOT in the merged Config — a third reader shape; env wins; reads `main_worktree_root(repo_root) or repo_root` = MAIN checkout so the secret is found inside a linked worktree w/o file-copy or env-seed, `or repo_root` keeps non-worktree/tmp_path callers byte-identical), the `[worktree] setup` overlay-aware wholesale-replace key, change-scoped CI gating (`[[ci]]` basename glob convention / skip-result shape / run-all-only discipline / tool-path harness limit), or mirroring the providers selection shape |
| workflow | [cold-door-launch.md](workflow/cold-door-launch.md) | Touching launch_stage argv/--approve trust injection, injecting child env vars at the launch seam (the merge-order setdefault layering, incl. the Linear-key env-seed before chdir), running a repo-configured `[worktree] setup` hook before exec (`run_worktree_setup` single canonical path; remote `position_worktree` skips it), the worktree `.agents/skills/` mirror (`materialize_skills` at positioning), wrapping a last-wins CLI, composing/testing a Python surface that nests a machine_output command, CliRunner stdout/stderr asserts on Click ≥8.2 (`.output` is a combined stream — compare `.stdout`/`.stderr`), or refactoring launch/run behind byte-exact pins (cache.py import-leaf, dataclasses.replace transitions) |
| workflow | [write-capable-cold-doors.md](workflow/write-capable-cold-doors.md) | Building/debugging a write-capable cold door (`perk skills create`/`refine`) that borrows the `save` stage to write the main checkout, the repo-authored-skills lifecycle verbs (`scaffold`/`delete`/`create`/`refine`) + their shared posture (non-fatal network reconverge, main-checkout `repo_skills_root` resolution, single-target symlink cleanup), or the dogfood-gate test pattern (stubbed-external-CLI → only the offline stitched precondition is automatable; the refine-skips-sync structural spy) |
| workflow | [worktree-lifecycle.md](workflow/worktree-lifecycle.md) | Writing a worktree-batch CLI, matching git worktree paths (the .resolve() trap, incl. CliRunner payload assertions on macOS), split --force semantics, the `[worktree] setup` hook + the `created`-flag real-run-vs-dry-run asymmetry (re-derive `stage.worktree=="create" and not path.exists()`), the `main_worktree_root` primitive (`--git-common-dir`+`.parent`, both return forms, the macOS `.resolve()`-both-sides gotcha), or a worktree test that is unexpectedly dirty |
| workflow | [mergeability-and-conflict-resolution.md](workflow/mergeability-and-conflict-resolution.md) | Touching the `/submit` merge-tree conflict probe (`perk/substrate/git.py`, exit-code-is-the-verdict, `MergeProbe.mergeable`), the carry-the-verdict-never-re-derive principle, the warm reactive bounded `driveConflictResolution`, the worker `mergeable !== false` bar, the first write-capable context-inheriting `conflict-resolver` subagent, or a PR-mergeability / force-push stale-`headRefOid` gotcha |
| workflow | [skill-bindings.md](workflow/skill-bindings.md) | Touching skill bindings (`[[bindings]]`), two-plane delivery, doctor validation/injection mirror, the `references:` subdirectory routing with zero init/doctor wiring (the per-skill whole-dir worktree symlink carries `references/` free, no wheel entry — reconfirmed by a bundled non-perk skill), linked-worktree delivery depending on the cold-door `.agents/skills/` mirror (dangling-binding warning is the symptom), description-discovered ≠ stage-bound (perk-expert has no `[[bindings]]` row, invoked on frontmatter description match, description IS its primary docs), self-contained skill references (because delivered skills land where docs/user-docs absent — `*Canonical source*` footer + the references second-mirror lockstep, human-review discipline not CI gate), the `DELIVERABLE_COMMAND_TARGETS` MANUALLY-curated frozenset (now grown to objective-reconcile/learn-docs/pr-review/objective-replan — a one-line membership/drift fix + its two assertion sites `tests/test_bindings.py` + `bindings.test.ts` EXPECTED count), or forward-declared bindings (a `command:<id>` binding + its `DELIVERABLE_COMMAND_TARGETS` entry can land ahead of the door that fires it — inert until a door wires the matching `binding_trigger`) |
| workflow | [shared-contracts.md](workflow/shared-contracts.md) | Touching cross-plane contracts in `shared/` — `contracts.md` §-numbering (not contiguous, grep `## §8.` before assigning), cross-plane SSOT prompt-fragment helpers + lockstep parity substrings, registry vocabulary keys land with their first declaring stage, registry-writes changes trip two pytest pins, both-planes wording pinning, or TS YAML reader choice (vendored `miniYaml` not the `yaml` npm package — dropped for clone self-containment) |
| workflow | [provider-seam.md](workflow/provider-seam.md) | Working on a provider seam (now five: plan/todo/askuser/footer/web) — classifying a proposed seam **artifact vs interface** (decides whether you write an adapter at all), the vacate-only `askuser`/`footer`/`web` interface seams, the three vacating mechanisms + the **install-site/runtime vacating keyed off `ctx.cwd`** (footer, no `process.chdir` test tier) and **"nothing to vacate at all"** (web) postures, the **first non-null-`package` default** (web — the `.startswith`/`in packages` string-membership test trap), the collision-kind decision rule (registration-time vacating vs runtime deferral), the **2→N widening census** (incl. the `GOOD`-fixture-must-gain-each-seam-default rule), **widening the footer catalog** with a new declarative provider (`pi-status-footer` / `pi-default` — one catalog row + one TS id constant, zero convergence-code change; `pi-default` the first null-package *non-default*) and the `pi-status` **no-extension-status counterexample** to "both foreign footers bridge automatically", the borrow-ban-vs-selected-footer-provider reconciliation, the selection substrate, deferring perk's own authoring surface under a foreign selection, the cross-plane resolver, wiring a foreign plan/todo adapter, gating adapter behavior on a foreign package's persisted state (the state-twin read), injected prompts under foreign tool restrictions (the tools-hidden branch), or an augment-posture provider (the plannotator bridge + persisted-mode gate read) |
| workflow | [cli-groups.md](workflow/cli-groups.md) | Writing a Click command group, the §8.1 group-dir template, hybrid default-dispatch stage/group coexistence, the enacted taxonomy arc nodes 1.1–3.3 all landed (dormant substrate + structural parity-smoke fingerprint), `MergedCommand` argv-`--json`-pre-dispatch + per-object marker state, `SectionedAliasGroup`-is-an-`AliasGroup`-not-`SectionedGroup`, two-edits-to-retire-a-generated-launcher (DEDICATED_STAGES + drop from STAGE_LAUNCHERS), flat-alias-routes-before-STAGE_LAUNCHERS, defensive-merged-command registry try/except → bare worker, launcher-only-≠-merged (`pr ready` is W, merged set = submit/land/plan-save), pure-relocation-≠-merge fold (objective group), thread-optional-kwarg-through-launch-chain (seed-prompt flag inert on --remote), update-warm-doors-argv often a verified no-op, cold-door-argv-grep-ALL-tests (planReview asserted argv[0]), worker-object-direct-invocation under --json with explicit obj=PerkContext, parallel-node union-conflict file set (stages.py DEDICATED_STAGES + cli.py root block; fixtures auto-merge but re-verify EXPECTED_SURFACE), cli.md-guard-deleted-for-structural-guards arc (fix prose that advertised it), SSOT-corrects-parent-propagates-to-3-surfaces (SSOT + reconcilable prose + node descriptions), `objective doctor` worker exit codes (detect/fix→0 even an ERROR-severity report-only drift, aborted-write→1, not-a-repo→2; `fix.failed.error` optional field; add to EXPECTED_SURFACE alphabetically), perk skills pass-through-first noun-group (every verb forwards to substrate, manifest `sources` keys = managed-source authority, sanctioned-subprocess guard `_SANCTIONED_SUBPROCESS_WRAPPERS` keyed (file_stem,func_name), N-symmetric-rollback-arms = test ALL arms, group self-containment — own `skills_fail`/`skills_emit` mirror the `--json` shape don't share across groups; PlanGroup is 3rd inline hybrid copy, HybridDispatchGroup base deferred, registry `command:` field informational), CliRunner clobbers `sys.stdin` mid-invoke (swap the module's whole `sys` for a fake to test an `isatty()` branch), `EXPECTED_SURFACE` parity-smoke fingerprint is alphabetically SORTED (ignore a plan's positional instruction), SectionedGroup help taxonomy, generated-vs-dedicated launcher long-help split, Click two-paragraph short/long help, byte-identical-JSON fold discipline, re-grep-all-N-helpers, parity smoke + rendered-row help-slice tests, fold gotchas (quoted two-token bash case keys, git mv blame) |
| workflow | [cold-door-client.md](workflow/cold-door-client.md) | Touching the `runColdDoor` envelope client (rollout complete, substrate mandatory for new doors), strict-iff-appended-to-workflow-state vs advisory-dropped vs fully-lenient decode criterion, fail-arm payload re-narrowing, the fixture-sweep rule when strictening a decode, the merge-race fixture sweep (grep ALL fixtures repo-wide), the derive-don't-decode strongest tier (render-only-and-redundant fields derived, the bad_output-reachability acceptance test), cross-plane skew structural for the dev flow (substring-preserving skew-naming reword), exported narrowing helpers, stdin-flag-at-argv-end, legacy-label byte-compat lever, non-string envelope fields fall back generic, the parent-posts warm-tool recipe (`post_pr_review` — strict whole-batch decode + soft Result + best-effort appendWorkflowState), or a parity-only field on a strict cross-plane decoder MUST be lenient (`base` via nullableStringField) |
| workflow | [github-gateway.md](workflow/github-gateway.md) | Touching `perk/github/`, the parse-helper family, consolidation boundary rules (byte-identical errors via per-site what/source, leave single-site wrappers alone), the helper-prefix+caller-tail argv pattern, substring not-found fold residual (phantom-`None` lookups), mutation-posting policies (verdict-driven /pr-review split now reshaped single→parallel parent-posts via `post_pr_review`, 👍-reaction minimal artifact, asymmetric failure ladders, structurally-unpostable fields), gh-GraphQL transport facts (no `{owner}/{repo}` templating, cursor-pagination-omit-on-first-page, GraphQL not-found shape broke shared `_is_not_found` → broaden + live-refixture), non-default-base autoclose strand (`Closes #N` fires ONLY on default-branch merge → surface PR `base_ref`, capture-before-reassign, close explicitly only on confirmed non-default base, fail-open layering), gateway purification by hoisting a backend-specific read to the consumer (delete-the-read + value-as-param + resolve-via-resolver → pure gateway AND a free missing-backend fallback), `backend_id="github"` literal breaks resolver↔adapter cycle, or the `repo_identity` third repo-view read shape (`gh repo view --json name,url,defaultBranchRef` → `RepoIdentity` in one shot, reuses lenient `_exec` helpers but strict-on-incomplete, GitHub-only by construction, isort-sorted re-export; sole consumer is the repo-authored-skills source derivation) |
| workflow | [issue-backend-seam.md](workflow/issue-backend-seam.md) | Touching the issue backend protocol, protocol module shape, annotated-binding conformance, late-bound GitHub adapter over the monkeypatched substrate, 21-function consumer boundary + substring import-direction guards, two-shape fixture rule, numeric-id grep tag, not-found message contract, `backend_id` stamp discipline & required-kwarg caller census & doctor arm-mapping over a collapsed error, opaque backend-owned header ids, opaque-id validator relaxation (the backend is the authority on junk; sweep ALL id consumers: isdigit/`\d+`/number-typed params), per-backend land closure (branch on `backend_id` never id shape), or URL-peeling in the shared id parser (`parse_plan_id` single chokepoint → all ~15 commands uniformly, scheme-gate-first additive so a bare id is byte-unchanged, key-on-path-shape not host, `/pull/N` deliberately rejected, the peeled id stays opaque past the parser) |
| workflow | [linear-backend.md](workflow/linear-backend.md) | Working on the Linear issue/objective backend, plain-`Authorization` auth, RATELIMITED-as-HTTP-400 errors-first parse, `.codes`-not-substrings, dual-encoding metadata markers (transcoder byte-identity, form preservation, the both-encodings presence-check bug class), two composition disciplines, client-side comment sort, lookup-first labels, `_PERK_LABELS` tuple, one readiness probe two consumers, env-first/config-fallback `client_from_env(repo_root=)`, the CORRECTED worktree key bridge (was env-seed-only & masked a bug — cold-door funnels `resolve_issue_backend(worktree)` → `<worktree>/.pi/perk.local.toml` never exists; fix = single reader `load_local_linear_api_key` now reads `main_worktree_root(repo_root) or repo_root` = MAIN checkout, found inside a worktree independent of the env-seed), Node-3.1 GraphQL type-literacy consolidation (zero `cast(` in the package — only the 4 inside `_opt_*`/`_require_*` defs in linear.py remain; substrate-home — generic `_opt_*`/`_require_*` in linear.py vs domain payload mapping in pkg leaf `_helpers.py`), backend-aware prompts branch on the stamped `cache.plan-ref.provider` not config, scripted GraphQL fake & httpx MockTransport recipes, agent-session emission (internally-gated emitters, force-gate-open/break-substrate fail-open testing), stateful `FakeLinearWorkspace` (named GraphQL operations, read-seeding, UUIDs-only-on-mutations), live smoke RAN (Modes 1/2 + Projects spike green, docs-only PRs, no defect): ProseMirror round-trip CLEAN, paired `INPUT_ERROR`+"entity not found" discriminator `_is_entity_not_found`, bare-identifier mutation acceptance (`_uuid_for` collapse DELIVERED — module-level `_request_issue_mutation` over SIBLING op classes, not-found mapping relocated into the wrapper, capture-at-create UUID for the UUID-only `issueRelationCreate`), attachments-not-comments, installed≠connected (control-PR isolation test, App uncheckable from a normal `gh` token), team-KEY-not-name `[issues] team` gotcha (non-fatal at land), idiomatic backend (attribution rides single create bottleneck), fake-init gotcha (no super().__init__ → seed cache or AttributeError), workspace-scoped labels (drop `teamId`), four→five label ripple (grep `four perk`), idempotent `attachmentCreate` fail-open into `update_plan_header`, prose-first reorder safe except the one manifest-backfill WRITE flips before-START→after-END, deferred collapsible-toggle never-guess-unverified-rendered-form, byte-stable sibling `_comments_with_authors`, Mode-4 project-backed lifecycle PROVEN LIVE (find-by-run-id idempotency, Project Update on create/land/reconcile, close→completed, node workflow mirror BOTH directions, ProseMirror clean, node↔plan unification makes NO new perk:plan issue; setup: backend selection reads COMMITTED `.pi/perk.toml` ONLY — overlay ignored, `uv run perk` not global; `get_objective` omits depends_on, derives phase from node id, silently absorbs drift), Projects substrate now BUILT (`LinearProjectObjectiveStore`): substrate-home principle (only `LinearClient` encapsulates GraphQL; op classes register only the client), re-homing via both-fakes-SUBCLASS-`LinearClient`, project-backed `create_objective` (overview = inline-code header + reconcilable prose, NO roadmap table — roadmap derived LIVE from node-issues, no stored YAML), content-at-create + documentCreate fallback, direction-by-field relations via relations/inverseRelations, milestone-by-name-not-position, `_FakeLinear`/`FakeLinearWorkspace` insertion-ORDER substring footgun (more-specific needle first), parsed-selection fixture-sweep rule, `issueRelationCreate` not-found divergence INVALID_INPUT/"Argument Validation Error"; Phase-4 project-objective ops (`add_objective_node` materializes a node-ISSUE, id-collision `None` branch UNREACHABLE, FakeLinear add-node needs 2-elem `project(id` list + `teams(filter` entry, phase→milestone seam routed empty-`known` for byte-equivalence, fail-open `projectUpdateCreate` from pure count composers), manifest-drift design DELIVERED (persisted visible inline-code manifest beside `objective-header`, `attach_issue_to_milestone` + `project_issues_with_milestones` sibling query, fresh-manifest splice renders inline-code manually, `FakeLinearWorkspace` cascade-delete relations on issue delete; `check_project_readiness` separate fn → 2 doctor checks + nullable `LinearReport.project`; third backend-aware seam `objective_read_instruction`/`objectiveReadInstruction` supplemental-clause), measurement-node firing (`client_from_env`+`request`, team UUID via `teams(filter:{key})`, /tmp throwaway), `--json` cold-worker driving (implement has no worker → copy plan-ref into worktree), probe-clobbers-the-sentinel, urllib-SSL-fails-use-curl, still-deferred: RATELIMITED retry/backoff, `LINEAR_API_KEY` GHA secret, agent-session deferrals, remote-vs-local session pointer; a new mutation needs the fake to ROUTE it AND have the workflow states (supersede surfaced a `"projectId"` `issueUpdate(` arm + a `projectUpdateCreate(` arm + a `canceled`-type `_STATES` entry) |
| workflow | [objective-store.md](workflow/objective-store.md) | Touching the objective-store tier, split off `IssueBackend` (dormant-contract → atomic-removal CI-green rule), Linear facade-refactor + the late-bound-delegation equivalence lock, translate-CM, resolver single-sourced off `[issues]`, `backend_id` import-cycle literal, `close_issue` issue-tier vs `close_objective` on the store (delegation transparency + the guard-asymmetry), the node↔plan unification protocol (`None`=doesn't-unify because unifying-store-raises-on-not-found, dry-run returns `None`, derive the land-time backlink self-referentially never a clobbered field, node-issue title ≠ plan title); Phase-4 protocol growth 6→9→10→12 methods ⇒ THREE implementers (GitHub/dormant-issue-backed/live-project-backed, ty-enforced) (`add_objective_node` re-render-vs-materialize split, the no-op-return family, manifest-unifies-both-backends); objective-keyed engagement reads reuse issue-tier honest reads on GitHub / Linear projects no edit-history → `read_description_edits` honest `()` / Project Updates deliberately not read / node-keyed `read_node_engagement` honest only on project store else `EMPTY_NODE_ENGAGEMENT` + deferral-comment-names-its-consumer rule; adoption Protocol growth `read_objective_source`/`adopt_source_as_objective` extend →None no-op family; the drift design (structural-identity manifest, pure OFFLINE `objective_drift` engine, authority precedence, deferred-edge sweep, whole-repo `ty check` catches the conformance fake under tests/); objective replan supersede ≠ upsert (close-old/create-new since `create_objective` is find-then-return idempotent, fresh `run_id` + bidirectional `supersedes`/`superseded_by` lineage, create-new-first/close-old-last/fail-open, `supersede_objective` joins the no-op family → census `_FakeObjectiveStore`, fail-open close uses lower-level `_projects`/`_issue_ops` primitives not public methods (error-type boundary), `--supersedes`/`--adopt-from` mutually exclusive, `adopt_issue` reused as MOVE not STAMP) |
| workflow | [session-data.md](workflow/session-data.md) | Touching the session-data schema or its semantics, warm ULID mint in `session_start`'s none arm (grammar-not-subprocess cross-plane proof), two-resolver doctrine (never unify), provenance digest convention, LWW whole-map appends, two-grade refusal tiers, never-trust-stored-paths, the read-only carve-out 2-instance template, JSON-artifact variant, two-digests-two-roles, recompute-derived-flags, GC destructive-op triad, registry-derived terminal set, conservative eligibility ladder, or dangling-pointers-by-design |
| workflow | [source-scan-guards.md](workflow/source-scan-guards.md) | Writing a node:test source-scan guard, the node:test-as-grep recipe (leading-dot patterns — a new-`ctx.ui.*`-method guard reuses it verbatim: `\.setWorkingMessage\(` allowlisted to surfaces — the seam's own structural interface decl has no leading dot so it's not flagged, vacuous-scan self-check, production-only), pattern-matches-the-seam self-check, path-construction-scoped literal bans, or guarding a path family across a phased migration (widen `".pi"`→`(".pi"|".perk")` so the family stays guarded at BOTH homes, flip the non-vacuous self-check to the new reality, backstop-not-completeness + the consumer-census-oracle reaffirmation) |
| workflow | [dot-directory-migration.md](workflow/dot-directory-migration.md) | Relocating a perk-owned dot-directory path root, the canonical contract (ownership-vs-discovery — `.perk/` perk-owned, `.pi/`/`.agents/` discovery namespaces perk only materializes into), the centralized path seam (`paths.py`/`paths.ts`, guards allowlist BOTH paths+cache, `config_dir` the single config-family redirection point), the three grep forms (slash-literal / pathlib-join-token / escaped-regex), failure-injection-targets-root-by-name, the `_MIGRATIONS` forward-only filesystem-migration triad + the `filecmp.dircmp` shallow-diff + `common_funny` byte-identity gotchas, the source-location-independent manifest fragment, or the dogfood self-repo gitignore-reconverge + worktree-binding migration needed to LAND a gitignored cache-root move (arc 1.1/3.1/4.1/5.1 landed, 2.1 closed-unmerged, 6.1 pending) |
| workflow | [doc-reconciliation.md](workflow/doc-reconciliation.md) | Reconciling `docs/planning/` or `docs/learned/` after landing a phase/node/turn, the roadmap `pr` field ≠ merge PR distinction, execute-doc-examples accuracy gate, keep-and-annotate over delete, boundary-criterion check before rewording a principle, objective-roadmap reconcile craft (hard-counts-in-prose are drift magnets re-derive every reconcile, check the PRIOR node's landed-narrative paragraph too, scope-attribution drift when earlier nodes absorbed the work → rewrite node description + extend narrative tied to the real PR diff scope), landing-log recipe (reconciling a just-landed node = append ONE 'Node X.Y landed (PR #n)' entry after the prior, extract-region + difflib-prove-only-additions, keep-and-annotate never rewrite older entries), Anchors-(verified)-region is a drift magnet (a node delivering what an anchor said didn't-exist guarantees stale → check every reconcile keep-and-annotate), or landing-narrative PR-number convention (GitHub-objective body header+roadmap-only (no Reconcilable prose) → narrative lives in node description citing plan-issue#, reconcile-a-node-landed-with-its-ORIGINAL-planned-description — append LANDED narrative + rewrite phase-complete "X remains" claim) |
| workflow | [borrowed-packages.md](workflow/borrowed-packages.md) | Borrowing a foreign Pi extension, the lockstep surfaces recipe (BORROWED_PACKAGES/settings/capability-summary/contracts/tests), setFooter-clobber vetting, `--no-ignore` greps under .pi/npm, thrice-affirmed retirement recipe, foreign tool names inert when absent, repo-non-mutation read-only bar, or borrow vs provider-seam criterion |
| workflow | [remote-runner.md](workflow/remote-runner.md) | Working on remote execution (runner, work_worker, GHA workflows), the three-candidate worker-entry ladder (`consumer-git` retired), the realized consumer worker-deps `@perk/pi` install (formerly a loud `exit 1` deferral, still execution-untested), the resolver-candidate-vs-migration-helper independent-lifecycle rule, cancel/retry supervisor controls, GHA rerun reuse, universal zero-spend smoke short-circuit, fail-soft orchestrator overlays vs local records truth, or subprocess monkeypatching traps |
| workflow | [init-and-doctor.md](workflow/init-and-doctor.md) | Touching `perk init` or `perk doctor`, the init/doctor division, managed-convergence SSOT, committed-tracked-managed-subdir-needs-no-worktree-symlink-mirror (contrast skills materialize_skills; perk owns the WHOLE subdir — prune inside never outside, same change-list apply=True/False), doctor-disk-vs-selfcheck-prompt, the `GROUP_ORDER` human-render trap (every new Check.group must join the render order), the optional `EnvCheck` tier (warn-not-fail non-fatal tool — `optional` flag stamped on BOTH present/absent results, `required_tools_ok` filters them off the exit-2 path, NO GROUP_ORDER change since warn leaves healthy/exit untouched, 3-way init mark), fix_errors first-class report data, fail-level checks shift verify=True baselines, §8.5/§8.6 machine-surface co-owners, seam-signature ripple, the AGENTS managed-block byte-match proof, or adding-a-NEW-gated-probe-beside-an-existing-gated-check (census ALL sibling-readiness monkeypatches incl. `--fix`-under-verify, `report.healthy` is the WRONG non-fatal assertion → assert no fail-status in the group, two-phase independent probe no-early-return-between + read-access-as-honest-scope-proxy + nullable-field-must-not-flip-`ok`) |
| workflow | [init-external-cli.md](workflow/init-external-cli.md) | Touching perk's skills init shelling out to external CLIs (skills delivery now load-bearing — two-tier loud-failure posture flip, post-sync presence verification, config-vs-substrate check split, `__version__` now version-string + AGENTS-stamp only never a ref pin — three consumer-ref sites collapsed `v{__version__}`→`main`, import dropped from `workflow_artifacts.py`; `PERK_SKILLS` SSOT — one alphabetical tuple edit cascades to manifest fragment + post-sync verify + doctor check (holds for non-`perk-` names), pre-merge self-converge `missing-skill` is EXPECTED/non-fatal (skills CLI resolves ref:main, skill not on main yet — init still converges the artifacts you commit), `references/` subdir travels via the per-skill worktree symlink + no wheel entry; three-SSOT skills split PERK_SKILLS/REQUIRED_EXTERNAL_SKILLS/MANAGED_SKILL_NAMES (verification iterates MANAGED_SKILL_NAMES not PERK_SKILLS), dagster ref:master, regen-via-`_desired_skills_manifest`-equality not full init, conflict-not-missing-skill + "Converged before failure" is committable); OR the **repo-authored-skills** second fragment (a verify-gated gesture NOT a ManagedConvergence — rendering a valid fragment does a `repo_identity` GitHub read, and convergences run unconditionally in offline tests) + its substrate-build patterns (gate an early-return on BOTH items AND errors, self-exclusion when scanning sibling fragments, network-skip ordering driven by testability, byte-identical shared-constant extraction verified by direct byte-equality, the seam-signature ripple absorbed by `**kw` stubs, residual: alias-grammar sanitization intended-but-unbuilt) |
| workflow | [extension-clone-lifecycle.md](workflow/extension-clone-lifecycle.md) | RETIRED — git-clone extension lifecycle REMOVED, perk now ships `npm:@perk/pi` (live story in distribution); doc trimmed to the pi-`git:`-loading substrate (host-alias jiti + no-self-heal install / unlocked-clone race / frozen-freshness — still accurate for ANY `git:` package) + the retire-an-orphaned-lifecycle recipe (relocate-survivor-to-dep-home (`consumer_git_clone_root`→settings.py beside `GIT_PACKAGE`, facade scrub RUF022 isort-alphabetical in BOTH import-list+`__all__`), removing-a-doctor-check-is-a-three-site-edit (checks.py def / doctor `__init__` import+`__all__`+append / fixes.py elif; keep sibling extension-install), dead-primitives-go-last-after-sole-consumption-proof (`git.clone/reset_hard/head_sha/ls_remote_sha` gone w/ tests, shared `git.fetch` stays), `_MIGRATIONS` filesystem-rmtree forward-migration seam (`_remove_orphaned_git_clone`, forward-only idempotent ([],[]), failed rmtree→report.fix_errors LOUD, runs unconditionally after fix loop so `run_doctor(fix=True,verify=False)` exercises it); kept-general F821 test-insertion split-assert trap (anchor between-functions insert on prior test's COMPLETE boundary not first terminator)) |
| workflow | [distribution.md](workflow/distribution.md) | Working on perk's release workflow, the version SSOT (static pyproject `version` + derived `__version__`, the three-way lockstep), the build-backend decision (keep hatchling, not `uv_build`), the dual-plane `release.yml` (widened-triggers-gate-every-job, build-once/download-publish, OIDC trusted publishing, the npm re-pack-at-publish asymmetry, `--provenance` needs `repository.url`), the three-way install-pin policy (machine-pins / docs-latest / self-exempt), version-parity enforcement (the `PERK_CLI_VERSION` informational launch env var injected at the local seam only, the soft `session_start` version-drift signal, the no-third-doctor-check decision since settings-wiring + extension-install already cover parity, `test_npm_pin_lockstep`), the git→npm extension-wiring flip (protocol→identity discriminator, direction-flipped migration), or init/doctor/launch owning the `@perk/pi` npm install (non-env-verified-CLI wraps OSError, best-effort `json.loads[key]` needs TypeError, the absent→fail `report.healthy` census, the conftest-stub-lands-present rule) |
| workflow | [prompt-templates.md](workflow/prompt-templates.md) | Bundling a new top-level resource dir (the shared-tier-vs-agents-tier decision + the four-test resolver proof), working the cross-plane jinja2 + vendored zero-dep `miniJinja` render seam (the 2nd vendored-engine precedent after `miniYaml`, dropping the `dependencies` key entirely, the byte-parity config DATA-SHAPE with `trim_blocks`/`trimBlocks` now GLOBAL True + `keep_trailing_newline` load-bearing + jinja2 as reference engine), the frozen mini-jinja subset (four categories) + its author-time grammar guard which MUST match the stricter runtime (the cross-cutting insight — regex char-set gate + recursive-descent shape validator are complementary, fourth lockstep surface, `{# #}`-comment + `in`/`is` reject gotchas), the CRLF byte-parity hazard (normalize `\r\n?`→`\n` in any TS module byte-matching a Python file read) + the string-only contract (lazy TS vs eager Python, enforce on BOTH planes), the two-tier render-parity tests replacing the prose-copy golden bridge (Tier A purpose-built contract-snapshot goldens generated by jinja2 / Tier B `live.yaml` cross-engine equality via `renderLive.ts` with NO goldens + coverage guard), the bare-import source-scan guard (strip comments first, scan dynamic forms, exclude `*.test.ts`+`testing/`, pair with `test_no_runtime_dependencies`), or the prompt-move pattern (the unify-vs-split decision rule keyed on WHY bodies differ — superficial house-style→unify vs cold-injects/warm-instructs→split, conditional templates and the `trim_blocks`-not-`{%- -%}` whitespace gotcha (incl. the end-of-line inline-conditional case eaten by `trim_blocks` → `{% endif %}\n\n` fix) + bounded-blast-radius grep, raw-var string coercion `model or ""`/`node ?? ""`, selection-stays-in-code-assembly-moves, the single-arm subdirectory (no file/no golden for an early-return arm), demote-in-place substring constants, the byte-stability `/tmp`-capture de-risker + script-generated byte-fidelity (longest-first var-replacement ordering), the grammar-freeze stale-claim ripple, single-file-vs-subdir deciding-factor-is-body-branching, no-trailing-newline `printf` fragments, union-vars, golden-replaces-substring + a thin per-plane selection test, warm `sendUserMessage`→assert-notifies-not-seeded + the ref-aware converged door, the `[[ci]]`-skips-`node:test` on a `prompts/`-only diff manual-run mechanic) |
| workflow | [pydantic-boundary-models.md](workflow/pydantic-boundary-models.md) | Converting a frozen-dataclass parse boundary (registry/bindings/providers/config/plan-metadata/cache) to a Pydantic v2 boundary model — the strict-vs-lenient base decision (`StrictBoundaryModel` for perk-authored shapes / plain frozen `BaseModel` for the forgiving `Config` / `LenientApiModel` for external APIs), the model-validates-the-assembled-result-not-the-`_parse_*`-layer rule, structural/content split preserved by DEFAULTING fields + keeping value-untyped fields untyped, the "model IS the tolerant parser" `mode="before"` relocation (and why `strict=False` can't replace the `_str` sentinel-collapse), derived-fields-become-`@property`, the context-gated set-level `model_validator` (lenient at load, re-validate-the-dump in `validate()` → needs `list` not strict `tuple`), byte-stable stored YAML via field-DECLARATION order (declare in old `to_data()` emission order + order-guard test), error-translation only at uncontrolled boundaries (`translate_validation_errors`) not code-controlled constructors, frozen raises `ValidationError`, and the ty/pydantic/ruff test gotchas (test via `model_validate(dict)` not typed kwargs / `# ty: ignore` not `# type: ignore` / lax-coercion picks the negative target / positional-arg rejection / whole-tree ty gate clears pre-existing `tests/` debt) |
| pi | [context-system.md](pi/context-system.md) | Surfacing information to a plan session, building a factory, debugging why a bash command is blocked, or extending the read-only bash allowlist (incl. the subcommand-shaped gh entries and the command-keyed, language-agnostic `ast-grep` entry — perk cannot own the `grep` builtin), or the **five-surface lockstep** for allowlisting a read-only bash gate command (SAFE_PATTERNS + paired test lists + contracts.md + user-docs two notes + this doc) |
| pi | [extension-api.md](pi/extension-api.md) | Live system-prompt inputs in extensions, registerTool execute results details requirement, custom tool read-only gating traps, the `session_compact` event (first-class SDK event; type-only harness union) and the stale-ctx compaction-race silent swallow, tsconfig strict-mode index access in tests, command vs event handlers, injected-message persistence, `ctx.ui.editor` facts (no AbortSignal; title-borne key hints), testing `pi.events`-bridge logic / flag-shortcut non-registration from the harness, asserting a `pi.sendUserMessage` injection offline (the spy is mandatory for seed-turn `invokeCommand` tests), the `headfulUIContext` select/input/editor gap, registration-time cwd config reads (host-repo-sensitive harness tests), the `PERK_RUN_ID` harness leak, or vendored-extension test/infra (single `node --test <file>`, the offline registration smoke, the extracted-core + seeded-random pattern) |
| pi | [extension-seams.md](pi/extension-seams.md) | Collapsing a repeated context-dependent extension idiom into one tested seam (the `report()`/`branchOf`/`EntrySink` minimal-structural-interface recipe — incl. narrowing plan-named SDK types to structural slices and typing an extracted core's injected dependency as a one-method slice), the type-only-import cycle break, the strict workflow-state append + read-back tiering (incl. object-valued custom `equals` / identity-subset comparators), the seam-owned-prefix trap, the report()-rollout gotchas (name shadowing, failFor-only failures, banner-fragile notify asserts), or the P1/P2/P3 site triage for what a single-message seam can absorb |
| pi | [tui-surfaces.md](pi/tui-surfaces.md) | Touching the surfaces module (surfaces.ts+report.ts only sanctioned rich-UI sites), the composed single `perk` status slot, headless full no-op, RPC dual-publish law (factory widgets/setFooter dropped in RPC), setFooter 0.78.1 facts (method-syntax bivariance, render-driven reactivity, once-only install, the single last-wins footer slot load-order clobber hazard), windower/content-line budgets, truncateToWidth `...`-not-`…`, visibleWidth asserts, factory-widget/invokeCommand harness recipes, width-sweep invariant testing, vendoring a pi-tui-touching extension (dual-copy CLASS type clash — top-level pi-tui vs the nested pi-coding-agent copy → value classes with private fields are DISTINCT nominal types; functions/interfaces are structural so fine; fix = structural-slice params + import KeybindingsManager from pi-coding-agent, NO dep bump/deep-path import), `setWorkingMessage` governed-but-permitted seam (headless-no-op over a minimal structural target, whimsical flavors the default indicator), or `/btw` charter bounded-exception (ctx.ui.custom declined by D6 → named SOLE bounded exception, human-invoked/hasUI-gated/never-machine-reachable, lockstep across charter/contracts/AGENTS) |
| pi | [pi-events.md](pi/pi-events.md) | Testing extension logic that bridges `pi.events` (the pure-over-a-fake-bus split, env-var over module-constant injection, getFlags/getShortcuts non-registration asserts), or driving real-runtime under a faux-model (nested pi-ai per-instance registry — resolve through pi-coding-agent, inMemory settings don't layer disk → extensionFactories, seed faux-provider auth, eventSink vs file-sink exclusivity) |
| pi | [tool-param-decode.md](pi/tool-param-decode.md) | Touching tool-parameter decoders in `toolParams.ts` (the tri-state boundary seam — absent vs mistyped, strict-fail bad_input vs native-shape refusals, decode-before-side-effect ordering proofs, exported pure decode for UI tools, never reuse cold-door lenient helpers, fallback-chain optionality — absent flips to `undefined` & the tri-state survives, `idParam`/`idArrayParam` opaque-id params) |
| pi | [context-injection.md](pi/context-injection.md) | Injecting context into prompt flow (conditional inject-and-strip, stage-field disambiguation, stringify-includes once-only dedup, two-flavors-one-customType) |
| pi | [structured-output.md](pi/structured-output.md) | Driving structured-output sessions (pi-ai tool-calling not JSON mode, faux-provider offline tests — top-level vs nested registration by caller, `PERK_NO_LLM` the only offline gate — auth resolution succeeds keyless) |
| pi | [headless-session.md](pi/headless-session.md) | Headless session construction & driving (runtime-factory builds the loader internally, explicit `bindExtensions` `mode:"json"`, `session.subscribe` raw-AgentEvent facts, single-prompt drive + budget watchdog & premature-idle gap, structured run-event stream — additive `RunEvent` union, two fail-soft tiers, route-don't-relay, offline model-availability determinism) |
| pi | [subagents.md](pi/subagents.md) | Touching subagent dispatch & config (agentOverrides reach builtin-only & per-call inline model override, child-posts-own-mutation vs read-only-child-parent-mutates (/pr-review reshaped to report-only parent-posts — the reviewer drops `write`, angle passed per-call, read-only fan-out not the posting agent), context fresh/fork, review 422→comment fallback, agent-def consumer delivery realized (frontmatter-derived runtime name (NOT file path; subdir placement free), installed-npm-packages-never-scanned (carrier is wheel + `perk init` not npm), PERK_AGENTS committed managed convergence into `.pi/agents/perk/` (prunes inside, no worktree mirror since tracked) — now four perk agents, `conflict-resolver` newest & first write-capable + context-inheriting (driven by `/submit`), widening-lockstep census, editing a perk agent's prose rubric (`agents/*.md` SSOT → re-run `perk init` → commit both byte-identical; the `.pi/agents/perk/`-not-`.pi/agents/` stale-path gotcha; skills-sync-fails-but-agent-converges-first worktree non-fatal; gitignored `.pi/perk.local.toml` so stage explicitly; no test asserts prose body verbatim — guards check presence+consistency) & the judgment-agent prompt anti-pattern (default-verdict + decide-conclusion-first + uncountered anti-noise framing biases → enumerate-findings-first + derive-verdict + "earned-not-defaulted" balance statement, binary posting bar unchanged, pure prompt-engineering change), fixed-key `[subagents]`, flat agent/seam-keyed config, TS TOML backslash continuation restriction, cross-plane model/prompt parity, exported guidance offline unit-testing, isolated `createAgentSession` bypasses perk's main-session read-only `tool_call` hook → thread tool-gating in (read-only⇒`['read']` only since foreign bash is unsandboxable) + fold gate state into the side-session cache key so a flip recreates it), or the subagent context artifacts `git add -A` hazard (`/pr-review`/`/address` children drop `formatted_context.json`/`pr_diff.diff` in CWD → swept into the commit; stage explicit paths / inspect `git status` / gitignore-candidate) |
| toolchain | [ruff.md](toolchain/ruff.md) | Using ruff check / format (the silent-commit trap, `RUF100` fires on a `# noqa` for a non-enabled rule, `RUF043` raw/escaped regex for `pytest.raises(match=...)` metacharacters, `UP047` PEP-695 inline generic vs `TypeVar(bound=...)` when a generic fn returns the bound type (Callable-return escapes it), template string E501 multiline limits, SIM105 exception suppressions, preview-rule enablement drags cross-bucket fixes forward — ruff bumps too, run_ci green ≠ committable — pre-commit can reformat after CI passed, `RUF002` en-dash in a docstring — use a hyphen (em-dash fine), `RUF022` unsorted `__all__` fails CI — insert new re-exports in isort-alphabetical position in both the import list and `__all__`) |
| toolchain | [biome-and-tsc.md](toolchain/biome-and-tsc.md) | Using Biome check / format / tsc (Biome param-property type-stripping under `node --test`, `organizeImports` assist-only-under-`check`, `let x = undefined` trap, `Omit<Union,K>` collapses a discriminated union — use a distributive Omit, forEach expression-body assertion needs a block body, vendored upstream code + `noUncheckedIndexedAccess` (every `arr[i]`/`str.split("\n")[0]` is `T|undefined` — `?? ""` string-index, `?? messages[0] ?? 'fallback'` random pick, guard-before-discriminant-narrowing an array-walk), new files: `biome check --write` before CI, template-literal backtick mangling under `--write` — review the diff, two-commit TS module mv+sweep recipe (blame survives the sweep, non-import-line audits, quoted justfile `**` glob, two-mode pre-move import resolution — resolve moved files against their OLD dir or a naive resolve-against-current silently no-ops, tsc --noEmit complete oracle, Biome reorders import-brace members)) |
| toolchain | [python-package-splits.md](toolchain/python-package-splits.md) | Splitting a Python module into a package (script-generated slice-by-line-range off the git mv'd original, call-site-only paren-anchored binding rewrites cross-module-before-`_exec`, name-census + per-consumer negative-grep completeness proofs, partially-init package submodule-import fallback & sorted-`__all__` silences F401 & hatchling auto-includes the subpackage; objective-#714 split-arc refinements — seed-generous import loop (over-seed imports, ruff F401 trims/F821 flags, ty confirms → upfront census non-load-bearing), grep class-NAME across ALL function bodies not just `__init__`s, monkeypatch contract keyed on NAME-RESOLUTION (relocated fn reads siblings in OWN namespace; PUBLIC names a blind spot; some break loud others fragile-green), module-OBJECT attr patches survive but NAME-binding patches don't → facade re-imports os/subprocess/git/github in `__all__` (`import x as x`, PLC0414 not enabled), constant-used-only-by-moved-helper travels WITH helper (define-in-`__init__` = cycle; "facade keeps X"=re-export not defined-in-`__init__`), E402 forces a leaf data module (doctor/data.py mirrors linear_backend/_helpers), `(stem,func)`-keyed guards ripple → only full `just ci` catches, `__file__`-source-scan-guard silently narrows to `__init__.py` under a pkg → `.parent.glob("*.py")` + re-verify by plant/revert, AST byte-identity recipe (`ast.get_source_segment` vs `inspect.getsource`; caveat strips decorator line + drops trailing same-line comment), submodule-naming avoids protocol-sibling clash, long-method extraction preserves call-SEQUENCE — pass resolved network values DOWN, pure string recompute fine; folds & cross-package relocations — folding a flat module into a pkg as a NON-re-exported submodule: every `pkg.<sym>` becomes aliased submodule ref + grep INSUFFICIENT (misses type annotations `def f(x: pkg.Type)` import-time AttributeError + module-docstring paths) → `python -c "import <consumer>"` first oracle, git-mv directory-first then files; monkeypatch targets diverge by patch SHAPE across a fold (consumer-attr survives if alias preserved / absolute-module-path breaks / late-bound pkg-attr moves to new owning module); cross-package relocation sweep FOUR-really-FIVE forms (attr-access/single-line-setattr/MULTI-line-setattr regex-must-span-line-breaks=90-failure-miss/string-path-repoint-via-late-binding/two-level `pkg.sub.NAME`) + ty HAS A HOLE on `setattr(mod,"NAME")` string-form (ty catches import repoints+stale attrs but NOT string setattr → never trust green ty, run full pytest), classify moved-vs-stays-put + census counts up front; macOS BSD `sed \b` is SILENTLY a no-op (reports success does nothing) → python `re.sub(r"(?<![.\w])X\.",...)`/`perl`, negative-lookbehind spares qualified names; E402 insert-after-FIRST-`from perk`-import not last (mid-file `# noqa: E402` block trap); source-scan guard goes VACUOUS when its target moves (matches nothing=false-green) → re-anchor to NEW location + ALWAYS self-check plant/FAIL/revert; sibling-submodule imports avoid package-root cycle (import siblings via submodule paths, leave `__init__` byte-unchanged, `import X as old` keeps call sites byte-identical, whole-repo `ty check` completeness oracle); package-organization layering north-star — consult-established-layering-before-reorg, the rule outlives its now-removed planning doc) |
| toolchain | [worktree-node-modules.md](toolchain/worktree-node-modules.md) | Biome `noControlCharactersInRegex` (single-pass alternation replace (two-star alt first) dodges the sentinel), worktree node_modules stale-SDK trap, package-lock churn cleanup, stale-global-`perk`/self-converge smoke trap (use worktree `.venv/bin/perk` for `shared/` changes; init smokes in a scratch dir; fresh worktree = empty node_modules → `npm ci` before tsc/node --test; a -1/143 test exit with no FAILED line is a SIGTERM kill — rerun before debugging; prove pre-existing breakage with the git-stash diagnostic before assuming you caused it; the delete/edit rebase-conflict keep-deletion-verify-moved-copy recipe) |
| toolchain | [ty.md](toolchain/ty.md) | Using ty type checker / language server, ty narrowing of untyped/JSON dict values (cast vs truthiness guard, the `_require_*` narrowing-helper family for deep GraphQL payloads, the heterogeneous list/dict literal → explicit-local-annotation gotcha — `nodes: list[dict[str, object]] = [...]`, not mypy `# type: ignore`; `.items()`-iteration idiom reads a known key off an `object`-narrowed dict cast-free (`next((v for k,v in entry.items() if k=="src"),None)` where `.get`/`[]`/`Mapping.get` all fail Expected-Never) — cast-free alternative to the cast fix; per-element `isinstance` in a loop does NOT refine the container element type (`tuple(raw)` stays `tuple[object,…]`) → typed-list accumulation (`acc:list[str]=[];append`); these are RESTRUCTURES not signature-only; `_opt_*` lenient twin of `_require_*` (cast-if-isinstance-else-None, never raises) + disposition-matching rule — never route a tolerant/skip/default site through a RAISING helper (silently flips fail-loud), `_opt_str(x) or ""` byte-eq to `x if isinstance else ""`; `Any`→`object` isinstance-tightening is body-unchanged-safe ONLY for scalar/list/set returns — dict-returning helpers hit `dict` INVARIANCE (`Top[dict[Unknown,Unknown]]` not assignable to `dict[str,Any]`, neither local-annotation nor non-ternary if/return rescues) need guarded `cast(dict[str,Any],x) if isinstance(x,dict) else {}`; intra-objective inconsistency — siblings chose cast-free `.items()`/typed-accum vs minimal guarded cast, BOTH sanctioned match local site; distrust a plan's "`Any`→`object` body-unchanged no-new-cast" claim — `object`-via-isinstance(dict) `.get(str)` fails Expected-Never + `(cur or {}).get` makes `~AlwaysFalsy|dict` no-`.get` → route every hop through cast-confined `_opt_dict`/`_dicts`/`_opt_str`/`_opt_int` in SHARED LEAF (casts only there, call sites cast-free), budget for these; incidental hardening `_opt_dict` walker more None-safe (→`{}` on non-dict not AttributeError), `_opt_int` REJECTS bool (`isinstance(True,int)`) preventing bool→numeric-id; inline dict literal to `dict[str,object]` param fails ty annotate-the-local; two divergent same-named enums → RENAME to domain-accurate names not unify (unify gives one an unused member), string VALUES unchanged so `__str__`/serialization byte-identical, no `__all__`/facade re-export surface, mechanics perl/python-not-BSD-`sed \b` + longer-idents-ripple-E501/I001 xref python-package-splits/ruff), ty suppression syntax (`# ty: ignore[rule]`, mypy-style `# type: ignore` doesn't suppress), ty enum strictness in tests (pass the member, not the literal — CI-only catch) |
| toolchain | [test-parallelism.md](toolchain/test-parallelism.md) | Test-suite parallelism (pytest `addopts -n auto --dist loadgroup` by default — `-n0` serial escape hatch, build-once = shared `xdist_group` pins consumers to one worker, skip-into-the-fixture; node `--test` parallelizes across files only — intra-file `concurrency:true` UNSAFE (process-global env mutation) so split harness-heavy files at section boundaries, noUnusedLocals/tsc trim loop, count-preservation `grep -cE '\btest\('` proof, `--test-concurrency=nproc*2`) |
